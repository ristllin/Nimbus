// GENERATED by tools/gen_docs_pack.py - edit docs/, re-run. DO NOT EDIT.
// The device's own documentation, embedded as flash rodata so it rides
// firmware.bin (OTA updates it for free; it can never version-skew).
// 13 files, 166 sections, ~210 KB (from 201 KB of markdown).
#pragma once
#include "nimbus/docs_pack.h"

namespace nimbus {
namespace docs {

inline constexpr DocSection kDocsSections[] = {
  // ---- orchestrator-world ----
  {"orchestrator-world#intro", "Orchestrator \"World\" - memory, capability & session architecture",
   R"NIMBUSDOC(The cognitive architecture for Nimbus's Orchestrator mode: what the agent
*has* (memory tiers), what it *knows about itself* (capability manifest,
running sessions), and how it *acts* (tools over an on-device MCP server).

This document describes each subsystem - memory record + tiers, vector memory
semantics, working/context memory, the audit stream, the scratchpad goal tiers,
recall query + injection, the maintenance cadence, the memory tools, the tool
registry/dispatch, sessions (sub-agents), prompt assembly, the dashboard API,
and the provider config schema - on its own terms, adapted for ESP32-S3
constraints. Where a design choice is driven by the hardware's RAM/flash limits,
the constraint and its reason are called out explicitly.)NIMBUSDOC"},
  {"orchestrator-world#locked-decisions-2026-07-02", "Locked decisions (2026-07-02)",
   R"NIMBUSDOC(- **Tool interface = on-device MCP server** (`memory.*` / `session.*` /
  `device.*`), bridged to the LLM via native function-calling; also reachable
  by external MCP clients over the LAN (Ph4, with auth).
- **Episodic index = SQLite on the SD card** + raw JSONL day-files + blob
  sidecars (a SQLite index paired with a `/data/blob/*.jsonl` raw stream).
- **Embeddings configurable but SET-ONCE** - changing provider/model/dims
  wipes + re-embeds the VDB; the web UI must warn loudly (destructive).
- **SD card REQUIRED** for tiers 2–4 (episodic + VDB). Directive + scratchpad
  + sessions work without SD.)NIMBUSDOC"},
  {"orchestrator-world#1-cognitive-tiers", "1. Cognitive tiers",
   R"NIMBUSDOC(The model-facing prompt (the `[HOW YOU RUN]` section, `lib/harness/src/compose.cpp`)
names **four memory tiers the model itself reads and writes**, in escalating
durability: **running memory** (the `memory` response field - short-lived working
notes for the NEXT turn only), the **scratchpad** (§1.2 - persistent goal tiers that
survive across turns and reboots), **long-term vector memory** (§1.4 - durable
embedded facts), and the **episodic log** (§1.3 - auto-captured conversation history).
The **directive** (§1.1) sits above all four: human-owned, the model cannot write it.)NIMBUSDOC"},
  {"orchestrator-world#1-cognitive-tiers-1-1-directive-exists", "1. Cognitive tiers - 1.1 Directive (exists)",
   R"NIMBUSDOC(User-owned, immutable to the model. `OrchMemory::directive` from
`agent::store::sysPrompt` (600 B cap).
Editable only by the human (web UI / provisioning).)NIMBUSDOC"},
  {"orchestrator-world#1-cognitive-tiers-1-2-scratchpad-model-owned-working-goals", "1. Cognitive tiers - 1.2 Scratchpad - model-owned working goals",
   R"NIMBUSDOC(A set of goal tiers (short-term / mid-term / long-term goals plus an
active-task line), which the model rewrites as it works:

```
scratchpad = {
  active: string                 // what I'm doing right now
  short: [string]                // this-session tasks
  mid:   [string]                // this-week intentions
  long:  [string]                // standing projects/aspirations
}
```

Device caps (enforced because of NVS/RAM limits): 1 active line
(240 B), 8 items/tier, 160 B/item, UTF-8-safe truncation
(`caps.h`: `kScratchActiveMax` / `kScratchItemMax` / `kScratchTierItems`, via
`capUtf8`). The model proposes, the device enforces - the same contract as the
memory caps. Persisted in NVS, so it **survives across turns AND reboots** and
works without SD.

**Write path (v4.1.0): the scratchpad is a first-class `orch_turn` RESPONSE
field, not a tool.** It rides the turn contract
(`orch_schema.h::ORCH_D_SCRATCH`) as a nullable object
`{active, short, mid, long}` - `active` a string, the three tiers string arrays.
The model returns it to update: a non-null tier REPLACES that tier's items
(≤8 each), a non-null `active` sets the active line, a null field leaves that part
unchanged, and returning the whole field null means no change
(`orch_turn.cpp` parse step 10). It is a **free write** - no tool round, returned
every turn by every provider alongside `reply`/`memory`. The device applies it
under the memory Lock, replacing only the returned tiers, then persists
(`orchestrator.cpp::applyScratch` → `memory::scratchpad()`; the harness path is
`lib/harness/src/apply.cpp`). Before v4.1.0 the scratchpad had no write trigger
the model could reach on its own, so it went unused.

It is rendered back into every prompt as a `## SCRATCHPAD` block **with its write
instruction printed directly beneath the data** (`orch_world.cpp`) - the fix for
"rendered but never written to." The `memory` field is a SEPARATE tier
(running memory, next-turn-only working notes), not a fallback for the scratchpad.
The admin-only `memory.scratchpad` MCP tool (actions
`view`/`set_active`/`add`/`replace`/`clear`) remains as a secondary read/edit
surface over the same single scratchpad; it is admin-gated because the scratchpad
is injected into every turn's prompt, so a non-admin writing it is a
prompt-injection channel aimed at the owner (`orch_memory_tools.cpp`).)NIMBUSDOC"},
  {"orchestrator-world#1-cognitive-tiers-1-3-episodic-store-sd-everything-that-ever-happened", "1. Cognitive tiers - 1.3 Episodic store (SD) - everything that ever happened",
   R"NIMBUSDOC(> **Shipped (v4.0.0):** the SQLite index sketched below was evaluated and
> **retired**. The episodic store is an **append-only JSONL day-stream store with
> an in-RAM offset index** (`AppendLogEpisodicStore`) - the system of record.
> History is uncapped on SD and **reaches months back**: the boot scan is
> deliberately budget-bounded (so a busy month cannot make the device unbootable),
> which leaves older rows on the card *below* the index; a deep query
> (`memory.episodic` cold-scan) pages down to them with a resume cursor and reports
> an honest floor. The storage tiers, the boot-scan bound, and the deep-history
> search are the canonical record in
> [`orchestrator-storage.md`](orchestrator-storage.md) §3a. The original
> design below is kept for reference.

The original design splits this into (a) an **SQLite DB** for structured rows
and (b) an **append-only JSONL day-file stream** (`/data/blob/YYYY-MM-DD.jsonl`)
for the raw audit trail, plus adapter transcripts. Nimbus keeps that exact split
because it satisfies both requested query modes - SQL queries AND grepping
raw files:

```
/sd/mem/nimbus.db          SQLite index
  sessions(id, started_at, agent_type, provider, model, title, status,
           turns, total_tokens)
  messages(id, session_id, ts, role, kind, text, blob_path, tags)
    role: creator | assistant | system | tool
    kind: message | tool_output | llm_response | file | image | audio |
          transcript | log            // event type + media
/sd/mem/blob/YYYY-MM-DD.jsonl   append-only raw event stream (grep-able)
/sd/mem/blobs/<sha1>.<ext>      binary artifacts (voice notes, images, files)
                                referenced by messages.blob_path
```

Retention: day-files older than 30 days move to `/sd/mem/blob/archive/`
(`retention_days=30`); the SQLite index is permanent (it is small relative to
the SD).)NIMBUSDOC"},
  {"orchestrator-world#1-cognitive-tiers-1-4-associative-memory-vdb-sd", "1. Cognitive tiers - 1.4 Associative memory (VDB, SD)",
   R"NIMBUSDOC(Record:

```
{ id, content, importance_score (0-1, default 0.5),
  ttl_hours (default 720 = 30 d; -1 = none),
  created_at, source, creator_flag, permanent_flag, metadata }
```

**Semantics** (constants included):
- **Dedup on write:** nearest neighbor at cosine distance < **0.05** ⇒ skip
  the write but bump the existing entry's importance to max(old, new).
- **Decay:** each maintenance cycle multiplies non-permanent importance by
  **decay_factor (default 0.95)**, floor 0.01.
- **Prune:** delete when importance < **0.05** or age > ttl_hours;
  `permanent_flag` and `creator_flag` entries are exempt.
- **Browse/flush/dedupe-scan:** `get_all` (importance-sorted, paged),
  `delete(id)`, `flush_all`, `flush_non_permanent`, pairwise `deduplicate`
  keeping the higher-importance twin - all exposed to the dashboard.

**ESP32 delta - the embedding engine.** An ESP32 cannot run a local
embedder. Nimbus embeds via provider API
(default **OpenAI `text-embedding-3-small`, `dimensions=256`**, int8-quantized
≈ 256 B/vector + text) through a new adapter method on the existing HTTPS
path. Storage: vectors + metadata in SQLite (`vdb` table), recall =
brute-force cosine over int8 (thousands of entries ≈ ms on the S3, PSRAM).
The embed config `{provider, model, dims}` is stored beside the table;
mixed-config vectors are refused; changing config = wipe + re-embed from the
episodic store (background job) behind the loud UI warning.)NIMBUSDOC"},
  {"orchestrator-world#2-per-turn-context-assembly", "2. Per-turn context assembly",
   R"NIMBUSDOC(The system prompt is assembled in this order (each section byte-capped; total
default **32 KB**, tunable - the budget is in bytes rather than tokens because
the constraint is device RAM):

1. **Immutable rules** - safety + device rails (model cannot touch keys/
   priority/host; see §6).
2. **Identity/version** - "You are Nimbus vX on an ESP32-S3 desk device…"
3. **DIRECTIVE** - user text, verbatim.
4. **Capability/world manifest** (§3) - hardware + tool list generated from
   the registry into a `## TOOLS` section.
5. **Scratchpad** (§1.2) - active line + three goal tiers, rendered as a
   `## SCRATCHPAD` block with the write instruction printed directly beneath the
   data (`orch_world.cpp`) so the model always sees how to update it via the
   `scratchpad` response field.
6. **Running-sessions digest** (§4) - a summary of the active sessions.
7. **RELEVANT MEMORIES** - top-K recall (§2.1).
8. **MEMORY explainer** - a `## MEMORY` section telling the model how its memory
   works and that `memory.config` can tune it.
9. Recent conversation (rolling, trimmed front-first when over budget;
   a summarize-and-compress step keeps the last 2 messages + a summary block).

### 2.1 Recall
```
query = active_task + " " + latest_user_messages + " " + all_scratchpad_goals
hits  = vdb.search(query, k = retrieval_count)          # default 10
hits  = [h for h in hits if (1 - h.distance) >= relevance_threshold]  # default 0
inject as "## RELEVANT MEMORIES" bullets
```
`retrieval_count` (1–100), `relevance_threshold` (0–1), `decay_factor`
(0.5–1), `max_context_bytes` are the **memory config** - adjustable by BOTH
the model (`memory.config` tool, clamped to those ranges) and the user
(dashboard PUT `/api/mem/config`).

### 2.2 Maintenance
Periodically (each N turns / daily): `vdb.decay_importance(decay_factor)` then
`vdb.prune_expired()`; blob day-file archival monthly. Tool results worth
keeping are written back via `memory.write` (the model decides what to keep).)NIMBUSDOC"},
  {"orchestrator-world#3-capability-world-manifest", "3. Capability & world manifest",
   R"NIMBUSDOC(Generated, never hand-written (so it can't drift): hardware inventory (ring,
e-ink, encoder, mic/speaker, battery state), connectivity (Wi-Fi/BLE/Telegram
up?), SD present?, and the tool list pulled from the MCP registry with
one-line descriptions. The model knows its verbs and its body.

The manifest also states the model's **self-model in plain language**
(`orch_world.cpp::renderCapabilities`) - what it can actually do, so it neither
invents capabilities it lacks nor refuses ones it has:

- **Sub-agents** run in the background and are **fire-and-forget** - the model
  cannot talk to a running one. Each result is delivered to the owner as it
  lands, and once they have all finished the model is automatically given one
  more turn (a `[FRESH RESULTS]` block, with no new owner message) to synthesize
  them and update memory.
- **Skills authoring (v4.0.0):** the model **can write its own reusable skill
  capsules** (`skill.save` / `skill.delete`, admin conversations only). Approval
  is **asynchronous** - a saved capsule is server-stamped `created_by: agent`,
  `approved: false`, and stays **inert** (never injected into a spawn brief)
  until the owner approves it in the web UI or via Telegram `/skill approve`. So
  saving a skill is an investment for FUTURE runs, never a step of the current
  task; the model may delete only capsules it authored. Rails in §6.
- **Honesty:** it claims to have logged, saved, or remembered something only when
  it actually called the corresponding tool that turn; owner-only knobs (keys,
  routing) are named as owner-only so it can explain the boundary instead of
  silently trying and failing.)NIMBUSDOC"},
  {"orchestrator-world#4-sessions-as-conversational-sub-agents", "4. Sessions as conversational sub-agents",
   R"NIMBUSDOC(A conversation-manager + agent-adapter layer over the existing heavy fabric +
6-slot `Journal`:

- **Adapter seam**: `create_session(title, cfg)`,
  `send_message(id, text)`, `get_status(id)`, `abort(id)`, `cleanup(id)`.
  Provider adapters (openai/anthropic/custom) implement it for remote
  sub-sessions; the Journal keeps reboot re-attach exactly as today.
- **Digest injected each turn**: `[{id, agent_type/provider, model, title,
  turns, tokens, state, pending_reply}]`.
- **Model surface** (`session_ops`, v4.0.0): the turn contract advertises
  exactly **`spawn`** and **`terminate`** - "advertised == callable" is a
  standing rule, so the conversational ops (`tell`/`poll`/
  `list`) are **deliberately not offered**: the fabric is fire-and-forget, and
  output returns through the running-sessions digest plus an automatic synthesis
  turn, not a model-issued poll. Beyond `task`/`provider`/`model`, a `spawn`
  carries `skill` (an approved capsule id injected into the brief), `name`
  (display name, also the sub-agent's saved-document name), `project` (a run tag:
  each sub-agent's full result auto-saves as `<project>/<name>-<tag>`), and
  **`attach`** - up to 4 `<project>/<name>` documents the firmware reads from the
  file store and splices into the brief, so a sub-agent can read files it has no
  tools to open. The full brief format, per-provider wire, and what the head can
  and cannot do with a running or finished session are in
  [`sub-sessions.md`](sub-sessions.md).
- **Hygiene**: stale active sessions time out; idle sessions are cleaned
  after 30 min; every turn is persisted to the
  episodic store (transcript + blob emit).)NIMBUSDOC"},
  {"orchestrator-world#5-on-device-mcp-server-tool-registry", "5. On-device MCP server & tool registry",
   R"NIMBUSDOC(Registry semantics: every tool = name +
description + JSON schema + timeout; dispatch validates against the safety
rails, executes with a timeout, **audits every call to the blob stream**
(tool, params ≤500 B, success, duration, error), and returns a uniform
`{success, output, error, error_type}`.

- **Namespaces:** `memory.*` (write, search, update, pin, config, `scratchpad`
  - the admin-only read/edit surface; the model's normal scratchpad write is the
  `scratchpad` response field, §1.2 - and `episodic`, the deep-history search,
  §1.3), `session.*` (§4), `skill.*`
  (list, get, and **save/delete** - authoring, v4.0.0, admin-only and
  owner-approval-gated), `files.*`/`artifact.*` (the durable SD file store),
  `system.health`, `device.*` (existing validated device actions - led/tts/
  screen; secret-block preserved).
- **Transport:** JSON-RPC 2.0 (MCP). In-process for the orchestrator bridge
  (Ph1); WebSocket on the LAN for external clients (Ph4, token-auth).
- **Bridge:** the orchestrator advertises the registry to the LLM through
  native function-calling (OpenAI tools / Anthropic tool_use) and runs the
  tool-use loop (bounded iterations/turn), feeding results back - planning and
  execution collapse into this one loop on-device.)NIMBUSDOC"},
  {"orchestrator-world#6-security-rails-unchanged-now-enumerated-in-the-immutable-rules-block", "6. Security rails (unchanged, now enumerated in the immutable-rules block)",
   R"NIMBUSDOC(- The model may NEVER read or set provider keys, `providerPriority`,
  `orchHost`, or the Telegram token - no tool exposes them (redirecting its
  own brain / exfiltrating credentials). The HUMAN sets them via the web UI.
- `device.*` keeps the existing validator (clamps, allowlist).
- **Skill authoring is admin-only and approval-gated** (v4.0.0): `skill.save` /
  `skill.delete` require an admin conversation; a capsule the model writes is
  server-stamped `created_by: agent`, `approved: false`, and stays inert (never
  injected into a spawn) until the owner approves it - an agent-written
  instruction blob is a prompt-injection channel until a human signs off. Both
  ops are **refused in scheduled/unattended turns** (a routine firing, or a
  synthesis turn chewing on untrusted sub-agent output, must never persist a
  skill), the pending-approval queue is capped, and the model may delete only
  what it authored.
- Episodic and memory reads are typed tools (no free-form query language); the
  earlier `memory.sql` SELECT surface was dropped when SQLite was retired (§1.3).
- LAN MCP (Ph4) requires a bearer token minted on the device; off by default.)NIMBUSDOC"},
  {"orchestrator-world#7-dashboard-web-ui-memory-providers-routes", "7. Dashboard (web UI) - `/memory/*` + `/providers*` routes",
   R"NIMBUSDOC(- `GET /api/mem/stats` - VDB count, blob file count/MB, DB size.
- `GET /api/mem/vector?query&limit&offset` - browse (importance-sorted) or
  semantic search; row actions: delete, mark-permanent, importance shown.
- `POST /api/mem/vector/flush?keep_permanent` · `POST /api/mem/vector/dedupe`.
- `GET /api/mem/blob?kind&limit` - filtered episodic browse by day/kind.
- `GET /api/mem/working` - **the prompt-sections snapshot**: exactly what the
  model saw last turn, section by section, with byte/token estimates - the
  single most useful debug view.
- `PUT /api/mem/config` - retrieval_count / relevance_threshold /
  decay_factor / max_context_bytes (same clamps as the model's tool).
- `GET/POST /api/sessions` + transcript view.
- Embed-config panel with the **set-once destructive-change warning**.
- Scratchpad + directive editors (directive human-editable; scratchpad
  human-viewable, clearable).)NIMBUSDOC"},
  {"orchestrator-world#8-phasing", "8. Phasing",
   R"NIMBUSDOC(- **Ph1 (portable, no SD/network):** scratchpad module · capability manifest ·
  sessions digest · context assembler (§2 ordering + budgets) · tool registry
  + in-process MCP dispatch · memory-config clamps. All host-tested.
- **Ph2 (SD):** SQLite episodic store + JSONL blob stream + artifact sidecars
  + `memory.sql`/`memory.grep` tools + retention/archival.
- **Ph3 (SD+net):** embedding adapter (set-once) · int8 VDB + recall +
  dedup/decay/prune · `memory.write/search/config` tools · dashboard memory
  panels.
- **Ph4:** conversational session control (`tell`/`poll` as user) · LAN MCP
  + auth · dashboard session console.)NIMBUSDOC"},
  {"orchestrator-world#9-open-items", "9. Open items",
   R"NIMBUSDOC(- SQLite-on-ESP32 lib (`siara-cc/esp32_arduino_sqlite3_lib`) flash/RAM cost on
  the N16R8 - validate early in Ph2; fallback: typed flat-file index.
- int8 quantization recall accuracy at 256 dims (bench vs f32 on host).
- Tool-use loop cap per turn (start with 8 actions).
- Re-embed UX (background job progress on dashboard).
- Whether `memory.sql` free-form SELECT is worth the parser risk vs. canned
  query templates (by-date, by-kind, by-session, full-text).)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03", "Build status (2026-07-03)",
   R"NIMBUSDOC(Legend: **done** = built + host-tested · **hw** = hardware-verified on-device ·
**compile** = device code built + compile-verified · **seam** = portable core done,
device seam pending · **designed** = designed only

**307 native tests green + Part B MERGED to main (merge `f9baf9e`) + HARDWARE-
VERIFIED E2E** on the bench device. The UX2/BLE work is
committed + tagged `agent2-ux2-ble`. On-device E2E (11/11): live OpenAI embedding
→ int8 → store → cosine recall (the "teal" memory ranked first for a color query)
over `POST /mcp`, `session.list`, the memory dashboard endpoints, the set-once
embed lock, AND durable persistence across reboot (after fixing a LittleFS
`/data`-dir bug, commit `0d1bf95`).)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03-ph1-portable-cognitive-core-done-28-host-tests", "Build status (2026-07-03) - Ph1 - portable cognitive core  [done] (28 host tests)",
   R"NIMBUSDOC(- [done] Scratchpad (`orch/scratchpad`): short/mid/long goal tiers + active task,
  device-capped, NVS-serializable.
- [done] MemConfig (`orch/mem_config`): retrieval/decay clamps, model+user tunable.
- [done] Capability manifest + running-sessions digest + context assembler (`orch/world`).
- [done] Tool registry + MCP JSON-RPC 2.0 dispatcher (`orch/tool_registry`).
- [seam] Wiring into the live orchestrator turn loop - deferred: the console `TURN`
  path reboots the device (watchdog), so live-turn changes can't be hardware-
  verified yet; the memory server runs decoupled (web + MCP), so this is additive.)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03-ph2-episodic-store-seam-7-host-tests", "Build status (2026-07-03) - Ph2 - episodic store  [seam] (7 host tests)",
   R"NIMBUSDOC(- [done] Records (session/message + kind column) + query model (session/kind/time/
  text) + `InMemoryEpisodicStore` reference (`orch/episodic`), used on-device now.
- [designed] Device SQLite-on-SD impl behind the same `EpisodicStore` interface +
  JSONL day-stream + blob sidecars - needs an SD card + the esp32 sqlite lib.)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03-ph3-associative-vector-memory-hw-hardware-verified-24-host-tests", "Build status (2026-07-03) - Ph3 - associative vector memory  [hw] hardware-verified (24 host tests)",
   R"NIMBUSDOC(- [done] `VectorMemory` (`orch/vector_memory`): int8 vectors, cosine recall,
  dedup/decay/prune/TTL/permanence; + binary serialize/
  deserialize for the SD/flash blob.
- [done] `memory.*` MCP tools (`orch/memory_tools`): write/search/config/scratchpad,
  host-tested E2E over JSON-RPC with an injected fake embedder.
- [done] Portable embeddings wire format (`orch/embedding`): build request + parse
  response (OpenAI shape), host-tested.
- [hw] **Device (hardware-verified)**: `agent::embeddings` (real OpenAI
  `/embeddings` TLS call → int8), `agent::memory` subsystem (engines + real
  embedder + LittleFS/NVS persistence + registered tools), web dashboard
  (`net/web_memory`: browse/search/tune/embed-config-set-once). Live embed →
  store → recall → persist-across-reboot all confirmed on the bench device.)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03-ph4-sessions-lan-mcp-hw-device-compile-bidirectional-pending-9-host-tests", "Build status (2026-07-03) - Ph4 - sessions + LAN MCP  [hw] device / [compile] bidirectional pending (9 host tests)",
   R"NIMBUSDOC(- [done] `session.*` tools (`orch/session_tools`): spawn/tell-as-user/poll/terminate/
  list, host-tested with injected handlers + over MCP.
- [hw] **Device**: `POST /mcp` LAN JSON-RPC endpoint + `session.list` wired to the
  journal - hardware-verified (session.list returns over `/mcp`).
- [compile] Bidirectional `spawn/tell/poll/terminate` handlers are documented null seams
  (report "not supported") until the fabric exposes conversational sub-session
  control; MCP LAN auth is an open item.)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03-what-s-proven-where", "Build status (2026-07-03) - What's proven where",
   R"NIMBUSDOC(- **Part A control surface**: hardware-verified E2E (the bench device,
  live OpenAI key verify=1).
- **Part B memory system**: hardware-verified E2E (11/11 on the bench device -
  live embed→store→recall→persist over `/mcp` + dashboard) + 307 native tests,
  merged to main. Remaining: SQLite-on-SD episodic impl, live-turn-loop recall
  injection (blocked by the console-`TURN` watchdog reboot), bidirectional
  session control, MCP LAN auth.)NIMBUSDOC"},
  {"orchestrator-world#build-status-2026-07-03-hardware-notes-2026-07-03-bring-up", "Build status (2026-07-03) - Hardware notes (2026-07-03 bring-up)",
   R"NIMBUSDOC(- **A full `esptool write-flash` resets NVS** (mode → Notifier, Wi-Fi creds + API
  keys wiped) - re-provision after every flash: `WIFI <your-ssid>|<pass>` over
  the console, then the OpenAI key via `/api/orch`. A plain reboot keeps NVS.
- **Wi-Fi STA rejoin is slow/flaky right after a flash or reboot** (tens of
  seconds, sometimes needs a re-provision); it joins reliably from a clean boot.
  Suspected Wi-Fi/BLE radio contention in the merged firmware (BLE is on in
  Notifier mode).
- **LittleFS won't auto-create parent dirs** - the vector blob at
  `/data/orchvec.bin` needs an explicit `mkdir("/data")` (fixed, `0d1bf95`);
  same caveat applies to any new `/data/...` file.)NIMBUSDOC"},
  // ---- tool-catalog ----
  {"tool-catalog#intro", "On-device tool catalog",
   R"NIMBUSDOC(<!-- audience: dev -->

The Nimbus Orchestrator exposes a single, authoritative set of tools through an
on-device MCP (Model Context Protocol) server. This is the reference for every
tool: its namespace, name, one-line purpose, arguments, and current state.

**Single source of truth.** Every tool is registered on one `ToolRegistry`
(`lib/core/include/nimbus/orch/tool_registry.h`): `name` + one-line description
+ JSON input-schema + handler. Two consumers read that one registry, so they can
never drift:

- **The live orchestrator turn** - the tool list is pulled from
  `memory::registry().manifest()` and rendered into the composed World prompt's
  `## CAPABILITIES` section, so the LLM sees exactly what it can call. (See the
  turn-side dispatch in the [turn contract](turn-contract.md):
  `mem_write[]` → `memory.write`, `mem_query[]` → `memory.search`,
  `session_ops[]` → `session.*`.)
- **External MCP clients** - a JSON-RPC 2.0 client on the LAN reaches the same
  registry over `POST /mcp` (`handleMcp()` → `ToolRegistry::handleRpc()`,
  `src/agent/memory_subsystem.cpp`). Supported methods: `tools/list`,
  `tools/call`, `ping`. LAN MCP auth is an open item.

Both paths execute through the same `dispatch()`, so the capability manifest, the
prompt's tool list, and what actually runs are the same thing. Architecture:
[Orchestrator World](../orchestrator-world.md)
(§5 On-device MCP server & tool registry).)NIMBUSDOC"},
  {"tool-catalog#the-human-web-only-rail", "The human-web-only rail",
   R"NIMBUSDOC(Secrets, provider keys, provider **priority/routing**, and the device's own auth
are **never tools** - no registry tool and no `device.config` action can set
them, and the protected-config denylist blocks them by name. Only the human web
UI may change them. Specifically off-limits to the model:

- provider keys (`oaiKey` / `antKey` / `mistralKey` / `tavilyKey`), `tgToken`,
  `tgAllow` - SECRETS, blocked entirely; changing them needs owner confirmation
  via the web UI / Telegram.
- provider `priority` / routing (`providerPriority` and its aliases),
  `orchHost` / `fabric` - letting the model rewrite these would let it redirect
  its own brain / sub-agent backends.
- `allowlist` - the device's own auth gate; the model must not grant itself
  access.
- the Local Loops blob (`loops`) and the budget/governor knobs - only the
  dedicated `loop.*` tools and the human web UI write these.

The denial reports only a policy tag (`protected-BLOCKED`), **never** the blocked
value, so a secret can never leak into a device log or the next-turn context
(`lib/core/include/nimbus/orch/device_actions.h`, `kProtectedConfigKeys`). The
one deliberate exception is the `orch_model` device action: the model may
repoint its **own** head host + model at runtime.)NIMBUSDOC"},
  {"tool-catalog#state-legend", "State legend",
   R"NIMBUSDOC(| Mark | Meaning |
|---|---|
| live | registered and executing on hardware today |
| wiring | schema/handler shape landed; device handler pending in a named phase |
| planned | not built yet |

**Access.** Every tool call carries a `Principal` - the caller's namespace,
role, and quota. Tools marked *admin-only* require an admin conversation (the
`manageTenants` permission) and additionally refuse in scheduled/unattended
turns; the rest are open to any approved conversation. Hiding a tool from the
advertisement is never the boundary - each handler re-checks the caller.

**Advertisement is principal-scoped (v4.1.0).** An admin-only tool is not
listed in a member's or guest's prompt: previously every conversation was told
the assistant could call `skill.save`, `tenant.set_role` or `loop.create`, so it
offered a capability the handler then refused (a wasted round and a confusing
walk-back) and described the owner's admin surface to a guest. The registry
carries an `adminOnly` mark (`ToolRegistry::setAdminOnly`) and the turn engine
advertises `toolSpecsFor(who)`, restoring *advertised == callable*. This is
**visibility only** - the handler check is unchanged, so a hidden tool reached
another way (for example over `/mcp`) still refuses exactly as before.)NIMBUSDOC"},
  {"tool-catalog#memory-associative-working-memory", "`memory.*` - associative & working memory (1/2)",
   R"NIMBUSDOC(Registered by `registerMemoryTools()`
(`lib/core/src/orch_memory_tools.cpp`). Backed by the portable engines
(`VectorMemory`, `Scratchpad`, `MemConfig`, optional `EpisodicStore`) plus an
injected `Embedder` (text → int8 vector via the provider `/embeddings` API).

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `memory.write` | Store a fact in long-term associative memory so you can recall it later. | `content` (string, **required**), `importance` (number, 0–1, default 0.5), `permanent` (boolean, default false), `ttl` (enum: `session` \| `days` \| `weeks` \| `months` \| `permanent`), `source` (string, default `"self"`) | live |
| `memory.search` | Search your long-term memory for facts relevant to a query. | `query` (string, **required**), `n_results` (integer, default = `cfg.retrievalCount`, i.e. 5) | live |
| `memory.update` | Replace a fact that changed: store new `content` and remove the closest stale match. | `content` (string, **required**), `old` (string) or `id` (string), `importance` (number), `permanent` (boolean), `ttl` (enum) | live |
| `memory.pin` | Pin a memory (never forgotten), unpin it, or delete it. | `action` (**required**, `pin` \| `unpin` \| `delete`), `content` (string) or `id` (string) | live |
| `memory.config` | View or tune how memory is retrieved. | `action` (**required**, `view` \| `update`); on `update`: `retrieval_count` (int), `relevance_threshold` (number), `decay_factor` (number), `max_context_bytes` (int), `max_vectors` (int), `recency_half_life_hours` (int), `mmr_lambda` (number) | live (update admin-only) |
| `memory.scratchpad` | Read or edit your working scratchpad (active task + short/mid/long-term goal tiers). | `action` (**required**, `view` \| `set_active` \| `add` \| `replace` \| `clear`), `tier` (`short` \| `mid` \| `long`), `text` (string), `items` (array of string) | live (admin-only) |
| `memory.episodic` | Search your episodic history by kind, session, text, or a time window. | `kind` (enum: `message`, `tool_output`, `llm_response`, `file`, `image`, `audio`, `transcript`, `log`), `session` (string), `text` (string), `limit` (int, clamped 1–100), `since_hours` (int), `before_hours` (int), `before` (paging cursor) | live (device only) |)NIMBUSDOC"},
  {"tool-catalog#memory-associative-working-memory-2", "`memory.*` - associative & working memory (2/2)",
   R"NIMBUSDOC(Notes:
- `memory.write` derives a deterministic content id (djb2 hash), embeds the
  content, and inserts; a duplicate bumps importance instead of storing again.
- `memory.search` filters hits below `cfg.relevanceThreshold`
  (similarity = 1 − distance) and formats each as `- [NN%] content`.
- `memory.update` and `memory.pin` identify the target by exact content or by id.
- `memory.config` knobs are device-global (a member changing `max_vectors` would
  evict the admin's memory), so only an admin may `update`; viewing is open to
  any approved conversation.
- `memory.scratchpad` is injected into every turn's prompt, so it is admin-only
  until it is per-person. **Since v4.1.0 the model's normal way to write the
  scratchpad is the top-level `scratchpad` response field of the turn contract**
  - a free write (no tool round), persistent across turns and reboots (see
  [turn-contract.md](turn-contract.md)). This tool remains as the admin
  read/edit path (`view` / `set_active` / `add` / `replace` / `clear`), also
  reachable over `/mcp`.
- `memory.episodic` reaches past the startup index - a cold scan over the full SD
  history - and answers one page at a time: when a result ends with a continue
  token, pass it back as `before` to read further into the past. Session
  `system` holds the device event timeline (restarts with reset reason, firmware
  updates with release notes, mode switches, storage changes). Registered only
  when an episodic store is bound (device).)NIMBUSDOC"},
  {"tool-catalog#session-sub-agent-control", "`session.*` - sub-agent control",
   R"NIMBUSDOC(Registered by `registerSessionTools()`
(`lib/core/src/orch_session_tools.cpp`). Each tool's device behavior depends on
its injected `SessionHandlers` handler; a missing handler makes the tool report
`"not supported on this device"`.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `session.spawn` | Start a sub-agent to work on a task in parallel. Returns its session id. | `task` (string, **required**), `provider` (string, optional), `model` (string, optional) | live |
| `session.list` | List your running sub-agents and their state. | (none) | live |
| `session.terminate` | Stop a running sub-agent. | `id` (string, **required**) | live |
| `session.tell` | Send a message to a running sub-agent, as if you were its user. | `id` (string, **required**), `message` (string, **required**) | wiring: adapter continuation |
| `session.poll` | Read the latest reply from a sub-agent (like reading its message to you). | `id` (string, **required**) | wiring: adapter continuation |

Notes:
- Sub-agents are started **only** via the spawn op; there is no separate
  `spawn[]` turn-contract array. The turn wire (`session_ops[]`) offers only the
  `spawn` and `terminate` ops - `tell`/`poll`/`list` are not on it (they were
  advertised before and hard-failed at runtime), though they remain registry
  tools reachable over `/mcp`.
- The turn's `session_ops[]` **spawn** op accepts, beyond `task`/`provider`/`model`:
  - `skill` - an **approved** skill capsule id, injected into the sub-agent's brief.
  - `name` - a short display name (also the sub-agent's saved-document name).
  - `project` - a run tag: the sub-agent's full result **auto-saves** to the
    durable file store as `<project>/<name>-<tag>.md`. Use one project per
    fan-out run.
  - `attach` - up to 4 `<project>/<name>` documents whose full content the device
    splices into the sub-agent's instructions. It must be the JSON array field on
    the op; naming docs in the task text attaches nothing.
- `session.tell` / `session.poll` report the fire-and-forget boundary honestly
  today. Making them real needs adapter **continuation** - persisting each
  sub-session's conversation id (OpenAI `previous_response_id`, Anthropic
  multi-turn) so the orchestrator can drive a back-and-forth. The handler shape
  and schemas are in place; the fabric API is not yet exposed.)NIMBUSDOC"},
  {"tool-catalog#skill-knowledge-capsules-read-author", "`skill.*` - knowledge capsules (read + author)",
   R"NIMBUSDOC(Registered by the memory subsystem (`src/agent/memory_subsystem.cpp`; store in
`src/agent/skills.cpp`). On-device markdown playbooks: four built-ins
(`architecture`, `deep-research`, `hardware`, `tools-guide`) plus owner- or
agent-authored capsules on the SD card.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `skill.list` | List the on-device knowledge capsules you can read (id + title). | (none) | live |
| `skill.get` | Read a knowledge capsule by id (from `skill.list`). | `id` (string, **required**) | live |
| `skill.save` | Save (or update) a reusable skill capsule you authored. | `id` (string, **required**, `a-z0-9-_`, max 23 chars), `md` (string, **required** - the SKILL.md text, optional `title:` / `inject:` front matter) | live (admin-only) |
| `skill.delete` | Delete a skill capsule you authored (`created_by: agent`). | `id` (string, **required**) | live (admin-only) |

Notes:
- `skill.save` and `skill.delete` require an admin conversation and are refused
  in scheduled/unattended turns.
- A capsule the model saves is server-stamped `created_by: agent`,
  `approved: false`, and stays **inert**: it can be read, but is never injected
  into a sub-agent brief until the owner approves it - in the web UI
  (Capabilities → Skills) or via Telegram `/skill approve <id>`. Approval is
  **asynchronous**: save a skill as an investment for future runs, never as a
  step of the current task.
- Reserved built-in ids cannot be shadowed. At most 4 agent-authored capsules
  may await approval at once.
- `skill.delete` removes only agent-authored capsules; owner-created ones are
  deleted from the web UI.)NIMBUSDOC"},
  {"tool-catalog#docs-the-device-s-own-documentation", "`docs.*` - the device's own documentation",
   R"NIMBUSDOC(Registered by the memory subsystem (`src/agent/memory_subsystem.cpp`; portable
access layer in `lib/core/src/docs_pack.cpp`). A curated set of these docs -
capability, tool, hardware, and privacy references - is embedded in the firmware
image as a table of small sections, so the model can look up what it can do
instead of guessing. Read-only, open to every principal.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `docs.list` | Browse the documentation index - the doc files, or one file's sections. | `file` (string, optional - a file slug from the top-level listing) | live |
| `docs.search` | Keyword AND-match over section titles + bodies; returns ids + a snippet. | `query` (string, **required**) | live |
| `docs.read` | Read one section by id; an unknown id fails honestly with near-miss ids. | `id` (string, **required** - `<file-slug>#<heading-slug>`) | live |

Notes:
- The pack is **generated**: edit the markdown under `docs/`, then re-run
  `python3 tools/gen_docs_pack.py` (it rewrites
  `lib/core/include/nimbus/docs_pack_data.h`, which is committed). The pack
  ships inside the firmware image, so an OTA update carries matching docs -
  no SD sync, no version skew.
- Sections are split at build time (`##`/`###` headings, then paragraphs) so
  every `docs.read` result fits under the tool-loop's per-result clamp.)NIMBUSDOC"},
  {"tool-catalog#files-artifact-save-durable-artifact-store", "`files.*` & `artifact.save` - durable artifact store (1/2)",
   R"NIMBUSDOC(Registered by the files subsystem (`src/agent/files_subsystem.cpp`). Backed by
the durable artifact store on the SD card (`/mem/files/<project>/<name>`);
absent when no SD card is present. Reads honor per-person visibility (RBAC
`readableBy`).

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `files.list` | List the device's durable artifact store: projects and files. | `project` (string, optional filter) | live |
| `files.stat` | Metadata for one stored file: size, kind, created time, provider file id. | `project` (**required**), `name` (**required**) | live |
| `files.read` | Read a stored TEXT document's content, paged. | `project` (**required**), `name` (**required**), `offset` (number) | live |
| `files.search` | Search the CONTENT of saved text documents for words; returns the best matches with a snippet, ranked by term frequency. | `query` (**required**), `project` (optional filter) | live |
| `artifact.save` | Persist a TEXT artifact (report, summary, document) into the durable store. | `project` (**required**), `name` (**required**, with extension `.md`/`.txt`/`.csv`/`.json`), `text` (**required**, max ~24 KB per call) | live |
| `files.share` | Share one of your saved files read-only with everyone, or stop sharing it. | `project` (**required**), `name` (**required**), `share` (boolean, default true) | live |
| `files.send` | Send a stored file to the owner on Telegram - images go as an inline photo, other files as a document. | `project` (**required**), `name` (**required**), `chat_id` (optional) | live |
| `image.generate` | Generate an image from a text prompt and save it as a PNG in the durable store; returns the saved `project`/`name`. | `prompt` (**required**), `project` (default `images`), `name` (optional stem), `size` (optional), `quality` (`low`/`medium`/`high`, default `low`), `model` (optional, default `gpt-image-1`), `describe` (boolean, default false) | live |)NIMBUSDOC"},
  {"tool-catalog#files-artifact-save-durable-artifact-store-2", "`files.*` & `artifact.save` - durable artifact store (2/2)",
   R"NIMBUSDOC(Notes:
- `files.read` serves text extensions only (`.md` / `.txt` / `.json` / `.csv` /
  `.log`); binary documents are refused (use `files.share` or the web viewer).
  It is **allowed in scheduled turns**.
- `artifact.save` overwrites the same `project`+`name`, refuses a write onto
  another person's file, and counts against the caller's file-storage quota.
- `files.share` is owner-controlled and read-only; guests cannot share.
- `files.send` is a turn-output tool - refused from the LAN `/mcp` path. A file
  another person owns is invisible: the refusal deliberately matches "not found"
  so its existence is not disclosed.
- `image.generate` uses OpenAI **gpt-image-1** (it returns base64 natively;
  dall-e-* need a `response_format` param the current API rejects, or hand back a URL
  a one-TLS-slot device can't re-fetch). The whole image decodes into **PSRAM**
  during the download, then lands on SD under a brief lock - it never holds the
  internal heap, and never the SD bus for the long TLS wait (which would either trip
  the main-loop watchdog or race other SD users; both were seen live). It is a
  **turn-only** tool (like `files.send`): it blocks ~1-2 min, so it runs on the turn
  task, never the AsyncTCP `/mcp` path. `quality` defaults to **low** (fast, small,
  cheap); SD- and OpenAI-key-gated. The saved PNG is a first-class `Image` artifact:
  `files.list`/`files.search`/`files.send` all see it, and `files.send` delivers it
  as an inline Telegram photo. `describe:true` adds a vision read-back (a second
  provider call). **Hardware-verified** end-to-end on an e-ink board (a 1024×1024
  gpt-image-1 PNG generated → saved intact → delivered). **Out of scope on this
  device:** embedding images into PDFs (no PDF engine), email attachments (no
  outbound email send exists), and in-turn multimodal vision beyond describe-once - a
  generated or received image enters the conversation as a *description*, not pixels,
  for the heap reasons in `image_vision.h`.)NIMBUSDOC"},
  {"tool-catalog#results-recent-tool-outputs", "`results.*` - recent tool outputs",
   R"NIMBUSDOC(Registered by `registerResultTools()` (`lib/core/src/orch_result_store.cpp`).
A bounded ring of recent tool outputs and overflowed sub-agent results, so a
truncated payload can be read back in full.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `results.get` | Fetch the full text of a stored result by tag, paged. | `tag` (string, **required**), `offset` (number) | live |
| `results.list` | List the stored recent results (tag, kind, name, size). | (none) | live |

Note: a tag that has aged out of the ring is not an error to retry - fall back to
`memory.episodic` with the tag as `text`.)NIMBUSDOC"},
  {"tool-catalog#loop-routines-scheduled-turns", "`loop.*` - routines (scheduled turns)",
   R"NIMBUSDOC(Registered by the memory subsystem. Scheduled or recurring tasks ("routines")
that fire as future Orchestrator turns.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `wakeup.set` | Schedule ONE future wakeup for the assistant itself - no owner approval (a bounded single fire under the same daily governor as routines; at most 4 armed; chaining allowed). In `minutes` (2 min–7 days) it gets a single automatic turn carrying its `note`, then the wakeup retires (one short retry if the turn itself failed, then an owner alert). Visible in /loops and web Routines; cancel with `loop.cancel`. | `minutes` (**required**), `note` (**required**) | live (admin-only) |
| `loop.create` | Schedule a recurring or timed task that fires as a future turn. | `name` (**required**), `prompt` (**required**), `chat_id` (optional, must be allow-listed), `schedule` (**required** object: `{kind:"interval", every_seconds: N≥300}` or `{kind:"daily"\|"weekly", at:"HH:MM", days:[...]}`) | live (admin-only) |
| `loop.list` | List all routines with schedule, next run, last result, enabled, and approval. | (none) | live (admin-only) |
| `loop.cancel` | Cancel (delete) a routine by its id. | `id` (**required**) | live (admin-only) |

Notes:
- All three require an admin conversation. `loop.create` is additionally refused
  in scheduled turns (a routine cannot create routines).
- A routine the model creates is saved **pending** the owner's approval and
  cannot fire until approved (web Routines tab, or Telegram `/loop approve <id>`).)NIMBUSDOC"},
  {"tool-catalog#reply-output-channels", "`reply.*` - output channels",
   R"NIMBUSDOC(Registered by the memory subsystem. Turn-output tools: the model chooses how to
reach the person. Both refuse outside a live turn.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `reply.speak` | Read text aloud on the device speaker (text-to-speech). | `text` (**required**) | live |
| `reply.telegram` | Send a Telegram message; `voice:true` delivers it as spoken audio. | `text` (**required**), `chat_id` (optional), `voice` (boolean) | live |

Note: `reply.speak` degrades honestly when voice replies are switched off or the
speaker volume is 0. With voice replies off, `reply.telegram` `voice:true` falls
back to text - the content is never dropped.)NIMBUSDOC"},
  {"tool-catalog#web-live-web-search", "`web.*` - live web search",
   R"NIMBUSDOC(| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `web.search` | Search the live web; returns an answer plus top results (title, url, snippet). | `query` (string, **required**), `max_results` (integer, default 5) | live (when a web-search key is set) |

Note: registered only when a web-search (Tavily) key is configured - the key is
human-set, never model-writable. A monthly call budget can gate it.)NIMBUSDOC"},
  {"tool-catalog#tenant-people-roles", "`tenant.*` - people & roles",
   R"NIMBUSDOC(Registered by the memory subsystem. Manage who may talk to the device and what
they may store. Admin-only, and refused in scheduled turns.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `tenant.list` | List the people who can talk to this device: chat id, role, quotas. | (none) | live (admin-only) |
| `tenant.set_role` | Approve someone or change their role: `admin` \| `user` \| `guest` \| `unknown` (revokes access). | `chat` (**required**), `role` (**required**, enum) | live (admin-only) |
| `tenant.set_quota` | Set one person's storage limits. | `chat` (**required**), `vectors` (int), `bytes` (int), `ttl_hours` (int), `pins` (int) - 0 restores the role default | live (admin-only) |

Note: the last admin cannot be demoted. Roles and quotas are the model-callable
half of the people surface; the auth allow-list itself stays human-only (see the
rail above). See [People & privacy](../people-and-privacy.md).)NIMBUSDOC"},
  {"tool-catalog#device-system-introspection", "Device & system introspection",
   R"NIMBUSDOC(Read-only registry tools that report live state; none mutate durable state or
(except `device.selftest`'s audible items) actuate hardware. Registered in
`src/main.cpp` (`registerDeviceTools`) and the memory subsystem.

| Tool | Description (as registered) | Arguments | State |
|---|---|---|---|
| `system.health` | Report live hardware + subsystem health (ring, display, mic, speaker, SD, memory, PSRAM, Wi-Fi, Telegram). | (none) | live |
| `files.fetch` | Download a document from an https URL into the file store, governed by the owner's URL-download trust policy (off / ask-per-link / AI scan / full trust; web: Usage → Downloads). Runs in the background; per-file size cap applies; under "ask" the owner approves each link (Telegram `/fetch approve <id>` or the web card); under "scan" the file is quarantined and promoted only on a SAFE AI verdict. | `url`, `project`, `name` | live |
| `device.status` | Read the complete live device state: firmware/build, update engine + slot + auto-update, local time + timezone + clock-sync, free heap/PSRAM, SD (including live `sdLost`) + internal flash usage, battery, Wi-Fi signal, Bluetooth bonds, fault mask, sound tier + sound-pack sync, ring posture + battery mode (user and effective) + attention hold + device name + sub-agent provider priority, memory counts vs live caps (vectors, episodic, scratchpad), file-store count/bytes/free, sub-agent capacity (running/queued vs limits), real billed token usage (last turn + session) including `usage.budget[]` - per-provider spend this billing period (tokens in/out, calls, estimated dollars from the owner's rates) against the owner's dollar and token ceilings plus the reset day (v4.1.2: plan work against the budget instead of discovering a refusal at dispatch), and the current value of every config knob (v4.1: the knobs the model can set are all readable here). | (none) | live |
| `device.selftest` | Run a hardware self-test; returns per-item PASS/FAIL/SKIP. | `audible` (boolean - also runs the speaker/mic acoustic loopback) | live |
| `ota.status` | Read the firmware-update engine: state, latest version seen + its release notes, last install outcome. | (none) | live |

Note: `device.status` is silent (never makes a sound); the battery reading is a
voltage **trend** only - the device has no charge-detect hardware.
`device.selftest`'s audible items are refused when the device is on silent or the
owner disabled hardware tests. The model cannot **install** updates - only the
owner can (Telegram `/update` or the web UI).)NIMBUSDOC"},
  {"tool-catalog#device-physical-device-actions-turn-array", "`device[]` - physical device actions (turn array)",
   R"NIMBUSDOC(Device actions are **not** registry tools - they arrive as the turn's
`device[]` array and are validated by the portable half in
`lib/core/include/nimbus/orch/device_actions.h`
(`validateAction()`), then executed by `src/agent/device_actions.cpp` only for
items with `allowed == true`. Each `device[]` element is exactly one of:

| Action | What it does | Params | State |
|---|---|---|---|
| `led` | ring pattern + color + brightness | `mode` (`solid` \| `spinner` \| `pulse` \| `flash` \| `rainbow`), color `r`/`g`/`b` (0–255), `brightness` (0–255 or null) | live |
| `lights` | ring on/off shorthand | `value` (`off` \| `full`) | live |
| `config` | benign, owner-friendly knobs (allow-listed keys only) | e.g. `ledBrightness`, `priority` (routing *preference*), `posture`, `profile`, `theme`, `attnHoldMs`, `sfxLvlN`/`sfxLvlO`, `sfxVol`, `sfxTheme`, `sttProv`, `ttsProv`, `ttsVoice`, `ttsOn`, `sleepOvr`, `brightOvr`, `devName` | live (rails enforced) |
| `orch_model` | switch the head's own provider + model at runtime | `provider` (`openai` \| `anthropic` \| `mistral`), `model` (string) | live |
| `reboot` | restart the device | (none) | live |
| `tts` | speak text | `text` | wiring: speaker path (see below) |

Rails: a `config` action carrying any protected key (secrets, the auth
allow-list, provider routing/priority, the Local Loops or budget governors - see
the human-web-only rail above) is refused as a **whole** with reason
`protected-BLOCKED`. `orch_model` is the one deliberate exception to the routing
rule: the model may repoint its own head host + model. Session ops (spawn /
terminate) are **not** device actions - they go through the turn's
`session_ops[]` / `session.*`, never `device[]`.

On-device speech is available now through the `reply.speak` tool (and Telegram
audio via `reply.telegram` `voice:true`); the `device[]` `tts` action's speaker
path is still being wired.)NIMBUSDOC"},
  {"tool-catalog#planned-surfaces", "Planned surfaces",
   R"NIMBUSDOC(| Namespace | Tool | What it will do | State |
|---|---|---|---|
| audio | `audio.transcribe` | mic → STT text as a model-callable tool | planned |

Inbound audio is already transcribed by the Telegram/voice pipeline, and Telegram
media **send** is already exposed as tools - spoken audio via `reply.telegram`
(`voice:true`) and documents via `files.send` - so only a model-callable
mic-transcription tool remains planned.)NIMBUSDOC"},
  {"tool-catalog#adding-tools", "Adding tools",
   R"NIMBUSDOC(Beyond the on-device set, capabilities can be extended two ways, both surfaced
honestly in the web "Tools" panel (`GET /api/tools`, read live from
`memory::registry().manifest()`):

- **External MCP clients** register/reach tools over `POST /mcp`.
- **Provider-side tools** (e.g. hosted web-search / deep-research) run on the
  spawn target, not on the device.)NIMBUSDOC"},
  {"tool-catalog#see-also", "See also",
   R"NIMBUSDOC(- [Orchestrator World](../orchestrator-world.md)
  - the World memory system and the MCP server (§5).
- [Turn contract](turn-contract.md) - the single-shot turn JSON
  and how `mem_write[]` / `mem_query[]` / `session_ops[]` dispatch to these tools.
- [People & privacy](../people-and-privacy.md) - roles, quotas, and the RBAC
  gating the `tenant.*` and admin-only tools enforce.)NIMBUSDOC"},
  // ---- tools-and-commands ----
  {"tools-and-commands#intro", "Tools & Commands (1/2)",
   R"NIMBUSDOC(<!-- audience: dev -->

The catalog of every script, build environment, console command, and remote
owner command in the repository - what each one does and when you reach for it.

Two audiences, one page:

- **User tools** - what you need to install, recover, and manage a device you
  own. Setting one up needs only these.
- **Contributor tools** - everything for working on the firmware itself:
  golden-image review, vector generators, CI gates, build environments, and
  the serial test console.

All host scripts live in `tools/` and run with `python3` from the repository
root unless noted. Most need only the standard library. For a one-line-per-tool
quick index grouped by audience - and which scripts are load-bearing for CI or
the build - see [`tools/README.md`](../tools/README.md).

These are workshop tools for people building or servicing boards - a device
owner configures connectors, credentials, and everything else from the device's
web page (Capabilities → Connectors), never from this repository.

All scripts live in `tools/` and run with `python3` from the repository root
unless noted. Most need only the standard library.

### Device setup & recovery

| Tool | What it does | When you need it |
|---|---|---|
| `setup_device.py` | Guarded firmware installer over the UART port. Reads the board's NVS, reports the immutable factory MAC, and requires that MAC to be typed back before writing anything. On a new board it also asks for the fitted display and operating mode, seeds those two settings, then installs production firmware. Never erases NVS or factory-resets a configured device. | Installing firmware on any board - especially a factory-fresh one, which can only be flashed through the UART port. |
| `usb_reset.py` | Programmatic unbrick for a wedged ESP32-S3 USB-serial port: a libusb bus-level reset, protocol-equivalent to unplugging and replugging the cable. Supports `--skip`, `--serial`, and `--all` to pick the right board when more than one is attached. Needs `pyusb` + Homebrew `libusb`. | Serial has gone silent and esptool cannot connect. Run this instead of physically replugging. |
| `tcal_wizard.py` | Touch calibration wizard for the TFT variant: prompts you to press each of four corners, samples the raw ADC, derives the axis-swap and orientation flags, applies the result over the `TCAL` console command, and verifies it. Needs an `[env:test]`-family build. | A touch panel where taps land away from where you aimed - which is otherwise indistinguishable from broken touch. |
| `connectors_setup.py` | Pushes connector credentials from the repository-root `.env` (gitignored) to a board via the token-gated `POST /api/connectors` upsert. Idempotent; never prints a secret. `--list` shows the board's current connector state. | Configuring or re-configuring a board's external connectors after a flash or NVS change. |

### Telegram owner commands

In Orchestrator mode the device answers a few fixed commands over Telegram
before the model sees the message, so the owner can manage a deployed board
without waiting on (or paying for) a turn. Every command below except `/help`
is owner-only - an allow-listed member can converse but cannot run them - and
each reply names the device and firmware so two boards sharing one bot are never
confused.)NIMBUSDOC"},
  {"tools-and-commands#intro-2", "Tools & Commands (2/2)",
   R"NIMBUSDOC(| Command | What it does |
|---|---|
| `/help` (or `/start`) | Greet and list the owner commands. Available to anyone who can chat with the bot. |
| `/update` | Install a pending firmware update. |
| `/remind <when> <what>` | Set a one-time reminder - e.g. `/remind 30m take the cake out`. `<when>` is a span (`45s`, `30m`, `2h`, `1d`; a bare number is minutes), 2 minutes to 7 days. At that time the assistant delivers the reminder in this chat. Cancel with `/loop deny <id>`. |
| `/loops` | List the device's routines (and any reminders you've set). |
| `/loop approve\|deny\|off\|on <id>` | Approve, remove, pause, or resume one routine. |
| `/compact` | Summarize this conversation into memory. Runs in the background. |
| `/clear` | Forget the current conversation and its active task, and start fresh. Long-term memory and files are kept. Send `/clear` to confirm, then `/clear yes` to do it. The web page has the same action (Memory, then Clear conversation). |
| `/skill approve\|deny <id>` | Approve a saved skill so it can be used, or remove it. |
| `/mcp approve\|deny <name>` | Approve a tool server (MCP) the assistant discovered, so its tools can be used, or remove that approval. The same action is on the web page. |
| `/play [track\|stop\|pause]` | Play music from the SD card's `/music` folder on the speaker. `/play` plays every track, `/play song.mp3` plays one, `/play stop` and `/play pause` control playback. |

A routine or skill the assistant creates on its own stays inactive until the
owner approves it here (or from the web UI). The one exception is a one-time
wakeup the assistant sets for itself (a single follow-up turn, minutes to days
out): it fires once without approval, shows up in /loops like any routine, and
can be cancelled the same way. Managing people, keys, and other settings stays
web-only - see the web control surface, not the bot.)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools", "Contributor tools",
   R"NIMBUSDOC(Everything below is for working on the firmware or driving a bench board -
everyday use of a device needs none of it.)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-rendering-golden-image-review", "Contributor tools - Rendering & golden-image review",
   R"NIMBUSDOC(| Tool | What it does | When you need it |
|---|---|---|
| `golden.py` | Converts the e-ink golden test buffers (296×128 1-bit framebuffers in `test/golden/*.bin`) to PNGs for human review, and pixel-diffs two buffers (differences in red, exit 1 on mismatch). Standard library only. | Reviewing or debugging a red e-ink golden test - `render` to look at a screen, `diff` to see exactly what changed. |
| `tftpreview.py` | The color counterpart of `golden.py` for the TFT golden suite (240×320 RGB565 buffers in `test/golden_tft/*.bin`). Adds `contact` (tile every blessed screen into one labelled sheet) and `regions` (overlay tap targets and fail on targets under the 44 px minimum or on overlaps). | Reviewing TFT screens after a layout change - `contact` shows the whole UI at a glance; `regions` catches the two bugs a pixel diff cannot see. |
| `logo/gen_logo.py` | Generates the Nimbus logo (the dotted teal ring) and every artifact derived from it: `assets/logo*.svg`, the website logo, favicon, social card, and the PROGMEM mark served at `GET /logo.svg`. | Any logo change. Edit the generator and re-run it - never hand-edit its outputs. |)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-test-vector-artifact-generators", "Contributor tools - Test-vector & artifact generators",
   R"NIMBUSDOC(| Tool | What it does | When you need it |
|---|---|---|
| `gen_nsn_vectors.py` | Regenerates the nsn wire-protocol test vectors from the reference encoder in the sibling `nsnotify` repository, writing `tools/nsn_vectors.json` and the embedded header for the native test suite. | Whenever the reference encoder in `nsnotify` changes - the device codec is byte-locked to it through these vectors. |
| `gen_qr_vectors.py` | Regenerates the QR known-answer vectors from Project Nayuki's reference `qrcodegen` (the algorithm `lib/core/src/qr.cpp` reimplements), pinning the exact configuration the device uses. Needs `pip install qrcodegen`. | Whenever the QR vector list changes. |
| `make_manifest.py` | Builds and signs the OTA release manifest (ECDSA P-256 over a canonical message that must stay byte-identical to `nimbus::ota::buildSigMessage()` - the pairing is golden-tested). Also prints the unsigned message for cross-checks. | Used by the release workflow with the real signing key, and by `tests/hil/test_ota.py` with the committed test key. Run by hand only when debugging a signature mismatch. |
| `git_version.py` | PlatformIO extra script - injects `git describe` as the `NIMBUS_FW_BUILD` build id so `/api/state` pins the exact commit a running unit was flashed from. | Runs automatically on every build; nothing to invoke. |
| `gen_docs_pack.py` | Regenerates the embedded docs pack (`lib/core/include/nimbus/docs_pack_data.h`) from the curated docs the Orchestrator model can read via `docs.list` / `docs.search` / `docs.read`. The pack ships inside the firmware image, so OTA updates it. | After editing any curated file under `docs/` - the header is committed, so CI builds without python. |)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-ci-gates-consistency-checks", "Contributor tools - CI gates & consistency checks",
   R"NIMBUSDOC(| Tool | What it does | When you need it |
|---|---|---|
| `check_param_consumers.py` | Fails the build if any editable settings parameter has no functional consumer in the code - a knob that appears in the menu and web UI but changes nothing. | Runs in CI. Run locally after adding or removing a parameter. |
| `check_status_doc.py` | Fails the build when the status→animation table in `docs/notifier-status-language.md` drifts from the code single-source (`lib/core/src/status_style.cpp`). | Runs in CI. Run locally after touching either side; edit the code first, then fix the doc. |
| `webui_concat_check.py` | Asserts the web UI fragment set (`include/web/ui_*.h`) re-concatenates into a well-formed page, and optionally byte-compares against a blessed snapshot (`--bless` after an intentional page change). | After any web UI fragment edit. |)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-hardware-validation-qa", "Contributor tools - Hardware validation & QA",
   R"NIMBUSDOC(| Tool | What it does | When you need it |
|---|---|---|
| `nsn_send.py` | End-to-end hardware validation: encodes nsn frames with the real broker encoder from `nsnotify` and sends them to a device over Bluetooth, optionally reading back the device's debug status echo over serial. Needs `bleak` with a working macOS Bluetooth entitlement. | Proving the full broker→Bluetooth→device pipeline against real hardware. |
| `ring_check.py` | Quick LED-ring smoke test over serial: drives `LEDTEST` through the console and reports whether the ring data path acknowledges. Errors are console/port problems, not ring verdicts. Needs an `[env:test]`-family build. | A fast eyes-on check that the ring is being driven at all. |
| `backup_device.py` | Pulls a board's user data - artifact-store files, the full vector DB, scratchpad, and state/usage snapshots - over the token-gated web API into a dated folder. Read-only (GETs only); provider keys and tokens are deliberately never exported. | Backing up a device whose SD card is a single point of failure, before a reflash or as routine insurance. |
| `build_all.sh` | Compiles every build environment and flag path in one command - a compile-only gate that proves each target still links. Never uploads or opens a serial port. | Before a release, or after touching flag-gated code that only some environments compile. |

Board-specific bench scripts (a live connector-QA harness, the quality
benchmark, panic-capture, and the connector-shape grid) live in the private
`ops/` tree, not the public repo - they name bench boards and burn real provider
credits, so they are maintainer-only.)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-tool-subprojects", "Contributor tools - Tool subprojects",
   R"NIMBUSDOC(Each of these is a self-contained project with its own README:

- **`tools/sounds/`** - the sound-pack pipeline: synthesizes the wordless tone
  set (`gen_tones.py`, seeded + reproducible), curates pools (`palette.py`),
  builds `dist/` + the manifest (`build_assets.py`), regenerates the embedded
  tier (`embed_basic.py` → `src/sfx/sfx_basic_data.h`) and the action→audio map
  (`gen_sfx_map.py` → `docs/sfx-map.md`). Edit the generators, re-run, never
  hand-edit outputs.
- **`tools/harness-lab/`** - runs the real agent harness on the Mac instead of
  the device, for fast iteration on agent behavior. See its `README.md` for the
  build and usage.

The **Battery Lab** - the local dashboard (FastAPI + React) for running
recorded, graphed, comparable battery drain experiments, with thermal protection
and a device simulator - is its own project at
[ristllin/nimbus-battery-lab](https://github.com/ristllin/nimbus-battery-lab).
It drives the firmware's `DRAIN`/`STORAGE` console commands and the token-gated
drain endpoints, which remain in this firmware.)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-build-environments", "Contributor tools - Build environments",
   R"NIMBUSDOC(Firmware variants are PlatformIO environments in `platformio.ini`. Build with
`pio run -e <env>`, install with `pio run -e <env> -t upload`. The comments in
`platformio.ini` are the authoritative source; this table summarizes them.

| Environment | What it is |
|---|---|
| `native` | Host-side unit tests (Unity + golden images), no hardware needed: `pio test -e native`. Keep it at 100%. |
| `esp32s3` | Production firmware. Serial stays silent (power and flashing only); no test or debug code. |
| `notifierdbg` | Production firmware plus a device→host status echo over serial, for end-to-end Notifier validation. |
| `test` | Production firmware plus the serial test console (`NIMBUS_TEST`) - what the HIL harness drives. Never the flash target for a production unit. |
| `testuart` | Same as `test`, but the console lives on the UART USB-C port instead of the native USB port (which drops bytes on the V0.1 board). |
| `bttest` | Same as `test`, but advertises over Bluetooth as "Nimbus-BT" so a bench board is unambiguous next to a production "Nimbus". |
| `p2bringup` | Standalone hardware bring-up sketch (no `main.cpp`). |
| `beep` | Minimal speaker diagnostic - isolates the amp and speaker from the full firmware. |
| `mictest` | Minimal I²S microphone bring-up - verifies the mic read path and concurrent speaker+mic loopback. |
| `tftmin` / `tftmin-uart` | The smallest program that puts anything on the TFT panel: init, one solid fill, nothing else. Exists to take every variable out of a blank-screen fault. |
| `tfttouch` / `tfttouch-uart` | Five on-screen buttons that prove display, touch, and calibration together - a swapped or mirrored axis is obvious with five buttons and invisible with one. |
| `tftbringup` / `tftbringup-uart` | Full TFT + touch bring-up: panel init, color order, backlight PWM, and raw touch coordinates, decoupled from the full firmware. |
| `provision` | Standalone serial network diagnostic (not first-time setup - it has no setup network or web UI). |
| `provision-uart` | UART-console variant used internally by `setup_device.py` to seed a new board's display and operating mode. |

The `-uart` variants exist because the DevKit has two USB-C ports: the native
USB port and the UART port. A factory-fresh board can only be flashed through
the UART port, and on some boards the native USB console drops bytes - the
`-uart` environments put the console on the UART port so one cable can both
flash and report.)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-console-commands", "Contributor tools - Console commands (1/3)",
   R"NIMBUSDOC(Builds in the `[env:test]` family compile in a serial command console
(`src/test_console.cpp`). It exists so the HIL harness - and you, at a bench -
can drive and observe the device without a camera or a finger. Production
builds do not include it.

Open the port carefully: opening a serial session resets the board, so hold one
session open rather than reconnecting per command (see `tests/hil/device.py`).

Commands grouped by purpose, one line each. Arguments in angle brackets.

#### Status & diagnostics

| Command | What it does |
|---|---|
| `PING` | Liveness check. |
| `STATUS` | One-line device summary (firmware, mode, network, battery, faults, SFX tier). |
| `RENDER?` | What the screen, posture, and ring are composed to show - the harness's camera substitute. |
| `MENU?` | The current settings-menu view. |
| `LEDSTATE` | What the LED driver is physically driving (raw frame layer vs a fallback pattern) - the physical counterpart of `RENDER?`. |
| `LEDTEST` | Eyes-on LED data-path test: cycles red, green, blue through the real driver. |
| `RAWFRAME?` | Whether the animator currently owns the ring via the raw frame path. |
| `INPUTLOG on\|off` | Echo knob/button/touch events to serial. |
| `SELFTEST [FULL]` | Run the aggregated self-test (FULL adds the audible items). |
| `FAULT <cap> <on\|off>` / `FAULT?` | Inject or clear a capability fault (sd, memory, mic, speaker, led, screen) to prove graceful degradation; query the mask. |
| `SDCHECK` | Probe SD card health and report the current storage tier. |
| `TEST <name>` | Pass a test name through to the board-support self-test. |
| `REBOOT` | Software restart - the only sanctioned way to restart from the host. |
| `HANG` | Deliberately hang the main loop to prove the watchdog recovers (~8 s). |
| `MEMFILL <epi\|vec> <n> [bytes]` | Fill episodic or vector memory with test rows to exercise caps and pruning. |
| `WEBTOK?` | Print the device's web access token (for the HIL harness). |

#### Orchestrator & memory

| Command | What it does |
|---|---|
| `TURN <text>` | Run one live Orchestrator turn; the reply echoes to serial. |
| `VOICE <text>` | Inject a simulated voice transcript so the reply renders on-screen exactly like hold-to-talk - no microphone needed. |
| `PROMPT?` | Dump the last composed system prompt. |
| `CTX? [chat]` / `COMPACT <chat>` | Inspect a conversation's context size; force compaction now. |
| `EPIQ [@<cursor>] <text>` | Run a cold episodic-memory query over deep history and time it; page further back by passing `@<cursor>` from the previous result. |
| `DREAM` | Fire the reserved maintenance-and-reflection loop immediately. |
| `MODE <0\|1>` | Switch operating mode (0 = Notifier, 1 = Orchestrator); persists and restarts. |

#### Display & screen)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-console-commands-2", "Contributor tools - Console commands (2/3)",
   R"NIMBUSDOC(| Command | What it does |
|---|---|
| `SCREEN <eink\|tft>` / `SCREEN?` | Set the display preference; report both the stored preference and the driver that actually bound. |
| `SAVER [min]` | Force the e-ink screensaver now, or set and persist its idle threshold (0 = off). |
| `DEGHOST` | Force the next e-ink refresh down the full de-ghost path (exercises the stuck-red-plane fix on demand). |
| `TFTHEALTH?` | TFT panel configuration check, heal count, and live backlight state. |
| `TFTHZ [hz]` | Sweep or set the panel SPI clock, measuring pixel-path errors at each step. |
| `TFTFILL?` | Push whole frames through the real blit path and read back the far corners - proves the pixel path with nobody looking at the glass. |
| `TFTID?` | Panel register readback over the shared MISO line. |
| `TFTPWR?` | Read the panel's power/display-on register to tell "powered and displaying" from "merely storing pixels". |
| `TFTFLIP <0\|1>` | Which end of the landscape panel is up - decided by the mounting, so it is dialable live. |
| `TFTBREAK` | Drill: reset the panel behind the driver to reproduce the white screen on demand and prove the watchdog heals it. |
| `PANELPROBE <0\|1>` | Enable or disable the panel health probe. |

#### Input - knob & touch

| Command | What it does |
|---|---|
| `ENC <code>` | Inject a synthetic encoder event so the menu is driveable with no physical knob. |
| `SW?` | Raw debounced button level - isolates a physical-button fault from a decode fault. |
| `TAP <x> <y> [HOLD]` / `TAPUP` | Inject a synthetic tap (or press-and-hold, released by `TAPUP`) - the touch counterpart of `ENC`. |
| `TOUCH?` | Raw touch-controller state, for calibration. |
| `TCAL [minX,maxX,minY,maxY[,flags]]` | Read or set the touch calibration (`tcal_wizard.py` derives it for you). |
| `TOUCHISO?` | Read touch with the panel held in reset - isolates the two devices sharing the MISO line. |

#### Battery & power

| Command | What it does |
|---|---|
| `PROFILE <0\|1\|2>` | Set the battery mode (Dark/Balanced/Full) through the same path the menu and web use. |
| `BATTCAL` | Anchor 100% to the current reading - the owner asserts the pack is full right now. |
| `BATTRESET` | Discard everything the battery model learned by observation (keeps the `BATTCAL` anchor). Needed after a drain campaign. |
| `SLEEPMV [mv]` | Read or set the low-battery deep-sleep threshold (0 = off). A safety knob: 0 disarms the protection persistently - restore the default the moment a pack is connected. |
| `SLEEP` | Enter low-battery deep sleep immediately to test the wake mechanics (knob rotation or the 5-minute timer wakes it; the USB console dies with it). |
| `DRAIN on\|off [deep]` | Battery drain campaign load: pin the ring to a heavy solid-white draw; `deep` runs past the clean shutdown to the real cutoff. |
| `STORAGE <pct\|off>` | Discharge to a storage charge level (~70%) and hold. |

#### Audio & media)NIMBUSDOC"},
  {"tools-and-commands#contributor-tools-console-commands-3", "Contributor tools - Console commands (3/3)",
   R"NIMBUSDOC(| Command | What it does |
|---|---|
| `MICREC` | Record 4 seconds, transcribe it, print the transcript. |
| `SPKSAY <text>` | Synthesize the text and play it on the speaker. |
| `TTSTG <text>` | Synthesize the text and send it to the owner as a Telegram audio message - audible even with a dead bench speaker. |
| `TGSEND` | Telegram document-send smoke test. |
| `SFX <slug>` / `SFXVOL <0-100>` | Play a sound effect by slug; set the master volume. |
| `PLAY [name\|stop\|pause]` | Music player: `PLAY` plays all of SD `/music`, `PLAY song.wav` one track, `PLAY stop`/`PLAY pause` control it. |
| `MEDIATEST` | End-to-end test of the durable media path on the real SD card (write, capture, verify over HTTP). |

#### Music playback

The device plays music from the SD card's `/music` folder over the speaker, on top
of the same volume control as sound effects (Settings, Sound). Reach it from the
`media.play` / `media.pause` / `media.stop` / `media.list` tools (the assistant), the
`/play` Telegram command, or the `PLAY` console command.

- **Formats.** 16-bit PCM **WAV** and **MP3** both play (MP3 decode uses the
  bundled public-domain minimp3 decoder). A file whose format is not recognized is
  skipped, never played as noise.
- **Queue behavior.** Music is foreground audio, so a track is never dropped and
  plays to completion (unlike the sound-effect cues, which drop when busy). The
  player holds one now-playing track plus an ordered list: `media.play song.wav`
  (or `/play song.wav`) replaces the queue and plays now; `media.play` with
  `queue: true` appends instead; a bare `/play` queues the whole folder in name
  order. `media.pause` holds the position, `media.play` resumes, and `media.stop`
  halts and clears the queue.

#### Network & Bluetooth

| Command | What it does |
|---|---|
| `WIFI <ssid>\|<pass>` | Store Wi-Fi credentials and join. |
| `WIFISCAN` | Scan and print what the radio can actually see. |
| `WIFIKNOWN?` | The saved-network list - names only, never a password. |
| `WIFIRENAME <old>\|<new>` | Correct a mistyped network name while keeping its password. |
| `WIFIAP [on\|off]` | Force the setup network reachable now (stops the station), or resume joining. |
| `BLE?` / `BLEMAC?` | Bluetooth advertising/connection state; the factory Bluetooth address (the ground truth for telling two boards apart). |
| `BONDS?` / `FORGETBONDS` | Bonded-central count and pairing state; erase all bonds. |
| `NSNFEED <hex>` | Feed a synthetic nsn frame through the same decoder path a real Bluetooth frame takes - drives the Notifier UI with no broker and no Bluetooth. |

#### OTA updates

| Command | What it does |
|---|---|
| `OTA?` | Current OTA state and pending update, if any. |
| `OTACHECK` | Poll the release feed now. |
| `OTAAPPLY [dry] [force]` | Apply the pending update (`dry` verifies without installing). |
| `OTAURL <url>` | Point the updater at a different manifest URL. |
| `OTASIM arm <ver>` | Drill: simulate an available update to exercise the whole approval-and-install flow. |

For the full OTA story, see [ota.md](ota.md) and the operations runbook
[ota-operations.md](ota-operations.md).

The device also answers a few fixed owner commands over Telegram - see
[Telegram owner commands](#telegram-owner-commands) under User tools above.)NIMBUSDOC"},
  // ---- hardware ----
  {"hardware#intro", "Nimbus Hardware Reference",
   R"NIMBUSDOC(<!-- audience: dev -->

Project-level pinout + wiring for the **Solide S3** board (ESP32-S3-DevKitC-1 N16R8).
The board pin map's source of truth is
`board_solide_s3.h` in the public
[solide-drivers](https://github.com/ristllin/solide-drivers) board-support
repository; the deeper board build/BOM guide is its
[docs/build.md](https://github.com/ristllin/solide-drivers/blob/main/docs/build.md) +
[docs/hardware.md](https://github.com/ristllin/solide-drivers/blob/main/docs/hardware.md).
This page is the Nimbus-side consolidated view.)NIMBUSDOC"},
  {"hardware#board-configurations", "Board configurations",
   R"NIMBUSDOC(A Nimbus device runs in **one of three display/input configurations**. Two are
hand-built on the **Solide S3** board and picked by a single NVS setting,
`screenModel`, read once at boot to select both the display driver and the input
driver (they share the SPI3 pads and the TFT consumes the encoder's GPIOs, so a
hand-built board is only ever one or the other). The third is an off-the-shelf
all-in-one module, the **Freenove ESP32-S3 Display (CYD)**, that arrives as a
single part and runs its own firmware image.

| Configuration | Display | Input | `screenModel` | Board | Details |
|---|---|---|---|---|---|
| **[E-paper + knob](hardware/eink-knob.md)** *(default, shipped)* | 2.9" SSD1680, 296×128 1-bit | EC11 rotary knob | `eink` | `solide_s3` | [→ pinout & pins](hardware/eink-knob.md) |
| **[Touch TFT](hardware/touch-tft.md)** | 2.8" ILI9341, 240×320 RGB565 | XPT2046 resistive touch | `tft` | `solide_s3` | [→ pinout, wiring, touch cal](hardware/touch-tft.md) |
| **[All-in-one (Freenove CYD)](hardware/all-in-one-cyd.md)** *(lowest effort)* | 2.8" ILI9341, 240×320 capacitive touch | Touch | `tft` | `freenove_s3` | [→ pinout & happy-path build](hardware/all-in-one-cyd.md) |

The two Solide S3 pinouts:

| E-paper + knob | Touch TFT |
|---|---|
| ![e-paper pinout](hardware/diagrams/pinout-eink.svg) | ![touch-TFT pinout](hardware/diagrams/pinout-tft.svg) |

The all-in-one has its own complete pinout on its page:
[All-in-one (Freenove CYD)](hardware/all-in-one-cyd.md).

For a hand-built Solide S3 board, `python3 tools/setup_device.py` asks which
configuration is fitted and stores it over UART before production firmware boots.
This is essential for TFT: it has no knob, and the raw firmware default is e-ink.
Afterward it can be changed in **Settings → Mode & identity → Display** (web) or
`POST /api/orch scrModel=tft`. It is **hardware identity, not a preference**:
read once at boot to pick both drivers, so a change needs a restart, and it is
exempt from "Revert to Defaults". Surfaced as `scrModel` in `/api/state` and
`scr=` in `STATUS`.

The all-in-one is different: its pinout is a **compile-time identity** chosen by
`SOLIDE_BOARD=freenove_s3` at flash time, and `screenModel` is fixed to `tft`.
There is no display setting to pick, and nothing to wire.

Everything below this section is **common to the two Solide S3 configurations**.
The all-in-one carries the same peripherals on a different, fixed pinout, covered
in full on its own page.)NIMBUSDOC"},
  {"hardware#first-flash-of-a-fresh-board-use-the-uart-port", "First flash of a fresh board - use the UART port",
   R"NIMBUSDOC(⚠ **The DevKitC-1 has TWO USB-C ports and they are not interchangeable for
flashing.** Getting this wrong on a factory-fresh board looks like a dead board.

| Port (silkscreen) | Goes to | Auto-reset into download mode? |
|---|---|---|
| **UART** | CP2102N bridge → GPIO 43/44, **and DTR/RTS wired to EN + GPIO 0** | **YES - electrical, always works** |
| **USB** | the ESP32-S3's own USB peripheral (GPIO 19/20) | Only while the ROM owns it |

**For the first flash of a fresh board, plug into `UART`.** esptool toggles DTR/RTS,
the auto-reset circuit physically pulls GPIO 0 low and pulses EN, and the chip
enters download mode. **No buttons, and it does not care what firmware is running**,
because the path is electrical rather than software-cooperative. For a
production install, use the guarded installer:

```bash
python3 tools/setup_device.py
```

It lists the UART adapters, requires an explicit choice, reads the selected
board's immutable factory MAC and NVS state, and asks for the fitted display plus
initial operating mode. It then asks you to type the MAC back before any upload.
On a new board a UART-only diagnostic writes and verifies `scrModel` and
`nimbus_mode`; production firmware is installed last. Existing Nimbus settings
produce a prominent warning, and NVS is never erased. Do not run a bare `pio run
... -t upload` with more than one board connected, because PlatformIO may select
a different serial device than the one you intended.

The full flashing guide - the installer in detail, reflashing a running board,
recovery, and which build environment to use - is
[Flash the firmware](quick-start/flash.md).

`[env:provision]` is a standalone serial network diagnostic, not the product
firmware. It does not serve the Nimbus setup network or web settings. If it was
flashed accidentally, run `python3 tools/setup_device.py` to replace it with the
production firmware.

The board appears as a **CP210x** (`10c4:ea60`, `/dev/cu.usbserial-*` or
`/dev/cu.SLAB_USBtoUART`) rather than an Espressif device.)NIMBUSDOC"},
  {"hardware#first-flash-of-a-fresh-board-use-the-uart-port-why-the-usb-port-can-leave-a-fresh-board-unflashable", "First flash of a fresh board - use the UART port - Why the USB port can leave a fresh board unflashable",
   R"NIMBUSDOC(The native `USB` port talks to the S3's own USB peripheral. Whoever owns that
peripheral decides whether a reset is possible:

- **ROM owns it** → USB-serial-JTAG (`303a:1001`), and esptool can reset it.
- **An app owns it** (any TinyUSB/Arduino USB-CDC firmware, including the demo
  these kits ship with) → a plain CDC-ACM device such as **`303a:4001`**
  ("Espressif CDC Device"). DTR/RTS are then *virtual*: they do nothing unless
  that app implements a reset handler. The shipped demo does not.

In that state there is **no software path to download mode at all** - verified
against a factory-fresh unit: `--before default-reset`, `--before usb-reset`,
`--before no-reset`, the 1200-baud touch (every DTR/RTS polarity), a libusb bus
reset, and racing esptool against repeated resets all reach `Connecting…` and
never sync.

⚠ A libusb bus reset (`tools/usb_reset.py`) resets the **USB link, not the CPU**.
It is the right tool for a *wedged* CDC on a Nimbus board; it cannot restart the
chip or reach download mode.

⚠ **Do not go hunting with DTR/RTS.** AGENTS.md forbids strobing it on this board
and that is not idle advice - a strobe matrix wedged a board's CDC silent here.)NIMBUSDOC"},
  {"hardware#first-flash-of-a-fresh-board-use-the-uart-port-once-nimbus-is-on-it", "First flash of a fresh board - use the UART port - Once Nimbus is on it",
   R"NIMBUSDOC(Nimbus also owns the native USB, but its `[env:test]` build has the `REBOOT`
console command and a task watchdog, so it stays reachable and either port works
for reflashing. The UART port remains the guaranteed path if a build ever wedges.)NIMBUSDOC"},
  {"hardware#shared-peripherals", "Shared peripherals",
   R"NIMBUSDOC(These are present in **both** Solide S3 configurations. The display + input pins are
config-specific - see [E-paper + knob](hardware/eink-knob.md) or
[Touch TFT](hardware/touch-tft.md). The all-in-one carries the same peripherals on
its own fixed pinout - see [All-in-one (Freenove CYD)](hardware/all-in-one-cyd.md).

| Peripheral | Bus | Pins | Rail | Notes |
|---|---|---|---|---|
| **microSD** | SPI2 (FSPI) | CS 10 · MOSI 11 · SCK 12 · MISO 13 | 3.3 V | **MUST be FAT32-formatted** - exFAT (the factory default on >32 GB cards) mounts as `cardType!=0, ok=false`; REFORMAT as FAT32/"MS-DOS (FAT)" before first use (owner-verified fix 2026-07-13). orch `/mem` store. Needs a shared GND (a cold GND joint = `cardType=0`). |
| **LED ring** WS2812B ×45 | RMT | DIN 21 | **5 V** pwr / 3.3 V logic | ~372 mA worst case @ brightness 30 |
| **Speaker** MAX98357A I²S amp | I²S1 (std) | BCLK 7 · LRCLK 8 · DIN 17 | amp **5 V** | Class-D, built-in thermal/over-current protection (V0.1) |
| **Mic** I²S MEMS (INMP441/ICS-43434) | I²S0 (std) | BCLK 15 · WS 18 · SD 16 | **3.3 V only ⚠** | L/R→GND (left slot); VDD/data follow VCC - 5 V damages the S3 |
| **Battery sense** *(optional)* | **ADC1** | **GPIO 4** | - | resistor divider off the pack - see [Battery voltage sampling](#battery-voltage-sampling-how-to-add-it) |

:::caution A warm or hot SD card is an electrical fault
Disconnect power immediately and remove the card. Do not reinsert it while the
device is powered. Formatting and firmware cannot make a card hot: verify the
socket/module orientation and pin numbering, confirm its VDD is **3.3 V (never
5 V)**, check continuity from every ground to system GND, and check that 3.3 V
is not shorted to ground before applying power again. Nimbus runs safely without
the card, using its reduced internal-flash tier.

The SD card uses dedicated **SPI2** pins (`10/11/12/13`). TFT + touch use a
different **SPI3** bus (`42/41/1` plus their selects). They do not share SPI data
lines, but they do share the 3.3 V rail and ground; an SD power fault can therefore
brown out or damage the display even though its bus is separate.
:::

**Reserved (never assign):** 0·45·46 strapping · 19·20 native USB · 43·44 UART0 ·
26–32 flash · **33–37 OCTAL PSRAM** · 48 on-board RGB (used as the encoder switch on the
e-paper build). **Free spares:** 3 · **4 (battery sense)** · 5 · 6 · 9. **(18 = I²S mic WS/LRCLK.)**

> **Nimbus V0.1** moves audio to two separate, well-protected breakouts: a **MAX98357A**
> I²S amp + an **INMP441/ICS-43434** I²S MEMS mic (replacing the old combined PDM-mic +
> NS4168 board). Mic RX (I²S0) and speaker TX (I²S1) are independent controllers, so
> record + play run concurrently.)NIMBUSDOC"},
  {"hardware#power", "Power",
   R"NIMBUSDOC(`USB-C → 2S BMS → 2×18650 (series) → DC-DC → 5 V bus`. The **3.3 V** rail (MCU, display,
SD, audio, mic) is always on; the **5 V** bus (LED ring, amp volume) is switchable - so
the ESP32 + display + SD + input + mic all run on USB alone.)NIMBUSDOC"},
  {"hardware#battery-voltage-sampling-how-to-add-it", "Battery voltage sampling (how to add it)",
   R"NIMBUSDOC(The board has **no built-in gauge**. Two options; the firmware supports both behind
build flags (default = `NullMonitor`, i.e. desk-powered, battery UI hidden):

### Option A - resistor divider + ADC  *(implemented, `NIMBUS_HAS_BATTERY_ADC`)*

Cheapest path, no extra chip. **Tap `BAT+` *before* the DC-DC** - the regulated 5 V rail
is flat and tells you nothing about charge. Wire a divider into a **free ADC1 pin**
(GPIO 1–10; Wi-Fi owns ADC2 = GPIO 11–20, which reads garbage while Wi-Fi is up):

```mermaid
flowchart TD
  BAT["2S pack BAT+<br/>6.0–8.4 V"] --- RT["Rtop 220 kΩ"] --- N["divider node"] --- RB["Rbot 100 kΩ"] --- GND["GND"]
  N --- ADC["GPIO 4 (ADC1_CH3)"]
```

- Ratio: (220k+100k)/100k = **÷3.2**, so 8.4 V → 2.63 V, inside the ADC's ~0–3.1 V range.
- Firmware: `AdcBatteryMonitor` (`src/hw/power_battery_adc.cpp`) averages
  `analogReadMilliVolts(GPIO4)`, multiplies by 3.2 → pack mV, divides by the cell count
  (2) → per-cell mV → `liIonPercent()` SoC curve (host-tested).
- An optional divider that is not fitted leaves the ADC pin floating. Firmware
  accepts telemetry only across a broad **2.5–4.5 V/cell** plausibility window;
  outside it the board behaves as desk-powered with no battery gauge. This keeps
  a USB-only fresh board awake instead of mistaking a floating reading for a dead
  pack and entering low-battery sleep during setup.
- Config in [`include/nimbus_config.h`](../include/nimbus_config.h):
  `NIMBUS_BATT_SENSE_PIN 4` · `NIMBUS_BATT_DIVIDER_X100 320` · `NIMBUS_BATT_CELLS 2` ·
  `NIMBUS_BATT_VBUS_PIN -1`. Enable with `-DNIMBUS_HAS_BATTERY_ADC`.
- **Single-cell LiPo instead?** Use a ÷2 divider (e.g. 100k/100k), set
  `NIMBUS_BATT_DIVIDER_X100 200` and `NIMBUS_BATT_CELLS 1`, on any free ADC1 pin.

**LIVE-VERIFIED (2026-07-12)** on the 2S LiitoKala pack: enabled `NIMBUS_HAS_BATTERY_ADC`,
read **7923 mV** = 3962 mV/cell (2S confirmed). ⚠ **The S3 ADC under-reads near full.**
The ÷3.2 node at 8.4 V is **2.625 V - above the ADC's LINEAR region at 11 dB** (accurate
to ~2.45 V, compresses above), so a *truly full* pack reads ~7.9 V → ~72 %, not 100 %.
Consequences baked into the firmware: charge state comes from the
voltage **trend**, NOT absolute voltage (there is **no VBUS pin** - `NIMBUS_BATT_VBUS_PIN
-1` - so `onExternalPower` can only be inferred); a learned/asserted full-charge anchor
(`BATTCAL`) stretches the compressed top band so full reads 100 %. A lower divider (e.g.
÷4, keeping the full-charge node ≤ ~2.1 V) would make the top of the range linear and
remove the need for the calibration - a candidate for the next board rev.

> **⚠ Cautions:** use ADC1 (1–10) not ADC2; keep the divider high-value (≥100 kΩ legs)
> so it barely drains the pack, and never let
> the divider node exceed ~3.3 V. GPIO 3 is a strapping-adjacent pin - GPIO 4/5/6/9 are
> cleaner choices (the board doc designates **4**).

### Option B - I²C fuel gauge  *(stubbed, `NIMBUS_HAS_FUEL_GAUGE`)*

A MAX17048-class gauge (I²C addr 0x36) models SoC in hardware (more accurate, survives
sag). Impl exists at `src/hw/power_fuelgauge.cpp`; wire SDA/SCL to a free I²C-capable
pair + a VBUS-sense pin, set the pins in `nimbus_config.h`, build with
`-DNIMBUS_HAS_FUEL_GAUGE`.

Either monitor feeds the same portable `power::Manager` → the battery header glyph, the
T1 (force the Dark battery mode + Telegram ping) / T2 (flush + deep sleep) thresholds,
and the VBUS→Full battery-mode auto-switch.)NIMBUSDOC"},
  {"hardware#security-note", "Security note",
   R"NIMBUSDOC(Web/LAN surfaces are token-gated - see [`security.md`](security.md).)NIMBUSDOC"},
  {"hardware#battery-hardware-is-web-configurable-v2-10-x-owner-2026-07-17", "Battery hardware is web-configurable (v2.10.x, owner 2026-07-17)",
   R"NIMBUSDOC(Boards differ physically - some have the design **220k/100k** divider (÷3.20), others the
**270k/120k** actually fitted (÷3.25), and packs range from LiitoKala **3500 mAh** to
reclaimed vape/18650 **~500 mAh** cells (always **2S**). These are no longer compile-time:
**Settings → Battery** on the web exposes the two sense resistors + pack capacity, stored in
NVS (`battRtop`/`battRbot`/`battCapMah`) and applied live (ADC re-armed + model capacity
updated on the main task; cells stays compile-time 2S). Defaults equal the old compile
constants, so an un-set board is unchanged.

⚠ This fixed a real on-device bug: the firmware baked in **÷3.20** while the fitted
resistors are **270k/120k = ÷3.25**, so voltage read **~1.6% low** across the range (BATTCAL
masked only the top). Set the true resistors and the raw reading is correct end-to-end; the
`/api/state` `dividerX100` now reports the CONFIGURED ratio, so the Battery Lab's host-side
correction tracks it (no double-correction). ⚠ A divider change re-scales every mV, so the
BATTCAL full-anchor goes stale - **re-run Calibrate on a full pack** after changing resistors
(the web UI prompts for this).)NIMBUSDOC"},
  {"hardware#measured-battery-reality-curve-run-4-board-2-2026-07-16", "Measured battery reality (curve run #4, Board 2, 2026-07-16)",
   R"NIMBUSDOC(The first complete full→cutoff drain on real hardware. **5.75 h**, 3940 samples, 25
settle reads, zero thermal trips (die held ~50 °C). Owner-measured ground truths from
a dedicated analyzer: **capacity 3500 mAh**, **full = 8.40 V**. Raw data +
snapshots live with the Battery Lab, its own project at
[ristllin/nimbus-battery-lab](https://github.com/ristllin/nimbus-battery-lab)
(`data/rescue/run4_FINAL_brownout.{json,csv}`).)NIMBUSDOC"},
  {"hardware#measured-battery-reality-curve-run-4-board-2-2026-07-16-what-it-measured", "Measured battery reality (curve run #4, Board 2, 2026-07-16) - What it measured",
   R"NIMBUSDOC(- **Total system power at LED bright=77/255: 4.21 W** (608 mA average at the pack over
  5.75 h to deliver 3500 mAh). This is the hard number every estimate below is anchored on.
- **Load-model correction k = 0.60**: the datasheet model (`200 + 45·60·b/255`)
  predicted 1015 mA where the truth was 608. Two errors multiply: it returns **5 V-rail**
  mA but is consumed as **pack** mA (a 2S pack behind the buck ⇒ ~1.33×), and the
  per-LED full-white constant (60 mA) is high - the run bounds real white at ~45-50 mA,
  since 45 LEDs × 60 mA @5 V would exceed the *entire* measured system draw.
- **Device base draw ≈ 0.6-1.0 W** (derived, not measured: total 4.21 W minus the
  bounded LED share). ⚠ The one measurement that would collapse this range to a number
  is a drain at **bright=0** - one pack cycle, highest-value next experiment.)NIMBUSDOC"},
  {"hardware#measured-battery-reality-curve-run-4-board-2-2026-07-16-battery-life-dark-posture-is-a-visual-setting-not-a-power-setting", "Measured battery reality (curve run #4, Board 2, 2026-07-16) - ⚠ Battery life: Dark posture is a VISUAL setting, not a power setting",
   R"NIMBUSDOC(`Posture::Dark` only stops lighting LEDs. It does not touch Wi-Fi, Bluetooth, or the CPU -
it appears in 4 places, none of them power-domain code. The three battery modes
(Dark / Balanced / Full) are a table of LED/e-ink/telemetry settings (`profile.cpp`),
not radio or clock decisions.
There is **no power management**: no `esp_light_sleep`, no `esp_pm_configure`, no
`setCpuFrequencyMhz`, `CONFIG_PM_ENABLE` unset, CPU pinned at 240 MHz. The only
`esp_deep_sleep_start()` is the critical-battery give-up path. Modem sleep is on only
because Arduino defaults `WIFI_PS_MIN_MODEM` - never by intent, never tuned.

| Posture | Pack draw | Life on 3500 mAh |
|---|---|---|
| Drain @77/255 (run #4) | 608 mA | **5.75 h** (measured, to cutoff) |
| Ring at 1% (run #7) | 209 mA | **16.7 h** (measured) |
| Dark ≈ Calm ≈ Full-idle | **150 mA (1.11 W)** | **~23 h** (two-point solve) |
| For a **week** | 20.8 mA | ✗ not reachable |
| For a **month** | 4.9 mA | ✗ not reachable |)NIMBUSDOC"},
  {"hardware#measured-battery-reality-curve-run-4-board-2-2026-07-16-the-load-model-is-now-measured-not-derived-run-7-2026-07-17", "Measured battery reality (curve run #4, Board 2, 2026-07-16) - The load model is now MEASURED, not derived (run #7, 2026-07-17)",
   R"NIMBUSDOC(**I(b) = 150 + 5.95·b mA at the pack**, from two measured points - b=77 (run #4, 608 mA
over 5.75 h to real cutoff) and b=10 (run #7, 209 mA over 15.39 h, 73 settle reads).

⚠ **The derived 86-143 mA band MISSED.** The true idle draw is **150 mA (1.11 W)** -
above the top of the estimate, so idle life is **~23 h, not 24-40 h**. The optimistic end
of that table was never real. Quote ~1 day, never "a day and a half".

⚠ **1% ring brightness is NOT ~0.** It draws 60 mA on top of the 150 mA base - **29 % of
total draw**, costing **6.7 h** of runtime (23.4 → 16.7 h). 45 LEDs make "1 %" collective.

**Independent cross-check that passed:** the measured slope implies **~42 mA/LED at full
white** vs the datasheet's 60 - the same over-estimate the k=0.60 capacity correction
found by a completely different route (a measured 3500 mAh vs a two-point current solve).
Two unrelated methods, one conclusion: the datasheet per-LED constant is ~30 % high.

⚠ **Run #7 stopped at a 6000 mV FLOOR, not cutoff** - 281 mAh (8 %) was still in the pack,
so `3500 / runtime` would OVERSTATE the current by 9 % (227 vs 209 mA). The delivered
charge was recovered by reading its final settle voltage off run #4's measured curve. A
floor-stopped run is also barred from the SoC curve (no 0 % anchor). Known bias: run #4's
curve was built at 0.17C and run #7 ran at 0.05C, so its settle reads recover higher and
the curve over-reads its SoC - meaning the true I(10) is a little ABOVE 209 mA, and the
true base a little above 150. The **bright=0 idle-baseline run** (owner-approved) measures
A directly, with no extrapolation and no dependence on run #4's curve.

Dark/Calm/Full-idle are within noise of each other - since Full-idle went fully dark
(owner 2026-07-15), the LEDs were never the idle load; the always-on radios + 240 MHz
CPU are. **A week is not reachable by tuning, and not even by deleting the LEDs**: 45
WS2812Bs draw 0.6-1.0 mA each *with every pixel black* (~19-30 mA at the pack, no load
switch on the LED rail) - the dark strip alone exceeds the whole 20.8 mA week budget.
An S3 at 240 MHz with Wi-Fi associated is ~10× the month budget by itself. A week needs
a MOSFET on the LED rail + light-sleep with duty-cycled wake; a month needs deep sleep
- architecturally incompatible with "Notifier shows live BLE-pushed status".)NIMBUSDOC"},
  // ---- turn-contract ----
  {"turn-contract#intro", "Orchestrator Turn Contract",
   R"NIMBUSDOC(<!-- audience: dev -->

The **turn contract** is the JSON shape every provider-hosted LLM must return
for one Orchestrator turn. A provider's raw response string is parsed and
validated by the portable, Arduino-free `parseTurn()` into a typed `Turn` so the
device turn-loop never pokes a raw `JsonDocument`.

- Authoritative doc-comment: `lib/core/include/nimbus/orch/turn.h`
- Wire schema advertised to providers: `lib/core/include/nimbus/orch/orch_schema.h` (`ORCH_SCHEMA_BODY`)
- Parser: `lib/core/src/orch_turn.cpp` (`parseTurn`)
- Behavior pinned by tests: `test/test_orch_turn/main.cpp`
- Byte/concurrency caps: `lib/core/include/nimbus/orch/caps.h`, `lib/core/include/nimbus/mem_cap.h`

Related: the tools these fields drive - [tool-catalog.md](tool-catalog.md) -
and how a turn flows through the live turn loop -
[../architecture/orchestrator-live-turn.md](../architecture/orchestrator-live-turn.md).)NIMBUSDOC"},
  {"turn-contract#the-two-layer-design-strict-at-the-wire-defensive-at-the-parser", "The two-layer design: strict at the wire, defensive at the parser",
   R"NIMBUSDOC(`ORCH_SCHEMA_BODY` marks every field it advertises `required` at the API layer
(each provider wraps this bare schema in its own envelope: Mistral
`completion_args.response_format.json_schema.schema`, OpenAI `text.format.schema`,
Anthropic `tool_use`). But `parseTurn()` is defensive: on Anthropic the wire
schema is advisory (its strict-grammar budget rejects a contract this size), so
the parser is the real enforcement layer, and it never throws away a good reply
over one slightly-off item. Keep the schema string and the parser in lockstep -
do **not** diverge them.

The split that matters at the parser is between the required strings and the
arrays:

- **Required strings** (`reply`, `memory`, `ask`) are **strict**: absent ⇒
  `MissingField`, present-but-not-a-string ⇒ `WrongType`. Without them there is
  no usable turn.
- **Every array** (`device`, `spawn`, `await`, `mem_write`, `mem_query`,
  `session_ops`) is **tolerant**: a present-but-not-array value reads as
  **empty**, and a malformed *item* is **dropped silently**, never a whole-turn
  error. One slightly-off item can't cost you a good reply.)NIMBUSDOC"},
  {"turn-contract#contract-shape", "Contract shape",
   R"NIMBUSDOC(```json
{
  "reply":   "string (required)",
  "memory":  "string (required)",
  "ask":     "string (required)",

  "device":  [ /* discriminated-union objects, validated + executed device-side */ ],
  "mem_write":   [ { "content","importance","permanent","ttl" } ],
  "mem_query":   [ "search string", ... ],
  "session_ops": [ { "op","id","task","provider","model","skill","name","project","attach" } ],
  "scratchpad":  { "active","short":[...],"mid":[...],"long":[...] } /* nullable object; replace-per-tier; persists across turns + reboots */,

  "spawn":   [ { "provider","model","skill","task","category","note","name" } ],
  "await":   [ "job-tag string", ... ]
}
```

Every sub-field the wire schema lists is `required` there but **nullable**
(strict json_schema has no optional keys - an omitted value rides as `null`); the
parser then defaults each one, so at the parser they are all optional. `spawn[]`
and `await[]` are **retired from the wire** (`session_ops` is the one spawn
surface), but the parser still accepts them so an old client keeps working.)NIMBUSDOC"},
  {"turn-contract#required-string-fields", "Required string fields",
   R"NIMBUSDOC(`reply`, `memory`, and `ask` are the three REQUIRED strings. A missing one
rejects with `MissingField`; a mistyped one (e.g. an array where a string is
required) rejects with `WrongType`.

| Field | Meaning | Cap on parse |
|---|---|---|
| `reply` | User-facing text (`""` if none) | **uncapped** - user-facing; the device budgets it elsewhere |
| `memory` | Updated model running memory (`""` if unchanged) | `kMemModelMax` = **1200** bytes, UTF-8-safe |
| `ask` | Question to the user (`""` if none) | **uncapped** - user-facing; the device budgets it elsewhere |

`reply` names an action as done only when a tool RESULT confirms it. Delivery is
itself a tool result: writing "sent to your Telegram" or "reading it aloud now"
does not send or speak anything - the matching tool (file send, voice readout)
does, and only its success result lets the reply claim it happened.

`memory` is clamped to `kMemModelMax` (1200) UTF-8-safe on parse so the parsed
`Turn` is self-consistent with the memory module's cap even before
`OrchMemory::setModel` runs; the definitive cap + truncation flag live there.)NIMBUSDOC"},
  {"turn-contract#optional-arrays-all-absent-empty", "Optional arrays - all absent ⇒ empty",
   R"NIMBUSDOC(Every array key is OPTIONAL and TOLERANT: **absent ⇒ empty vector**, and
**present-but-not-an-array ⇒ also empty** (`as<JsonArrayConst>()` on a non-array
iterates zero times - it is not an error). This is what makes a bare
`{"reply":"hi","memory":"","ask":""}` a fully valid turn (all arrays empty), and
lets a new turn omit `spawn`/`await`/`device` and drive the run through the
live-integration arrays instead.

### `device[]` (tolerant per-item)

A non-object element is **dropped** (never a whole-turn error). Each accepted
element is carried as the **raw serialized JSON slice** of the element (compact, no spaces) into
`DeviceAction.json`; the portable device-action validator
(`nimbus/orch/device_actions.*`, a sibling module) turns it into a typed+clamped
action, and the executor lives on the device. Keeping the raw slice here
decouples the turn parser from that module.

### `spawn[]` (tolerant per-item, RETIRED from the wire)

Retired from the wire schema (spawning moved to `session_ops`) but kept fully
parseable, so an old client still works. Per item:

- A **non-object** element, or one whose `task` is **missing / non-string /
  empty**, is **dropped** silently - `task` is the only semantically-required
  field (matches the device `enqueueSpawn` `if (!task[0]) return;`). Every other
  field falls back to a default on copy, so a provider that omits `note` or
  sends a numeric `model` loses that item at worst, never the whole turn.
- Defaults, provider-lowercasing, and per-field UTF-8-safe byte caps are applied
  on copy:

| Field | Rule on copy | Byte cap (usable = buffer−1) |
|---|---|---|
| `provider` | **lowercased**; `""` ⇒ device resolves by priority | `kSpawnProviderMax`−1 = **15** |
| `model` | may be empty/invalid; device coerces | `kSpawnModelMax`−1 = **39** |
| `skill` | provider-native hint | `kSpawnSkillMax`−1 = **23** |
| `task` | the only **semantically-required** field | `kSpawnTaskMax`−1 = **4095** |
| `name` | short display name (`""` ⇒ unnamed) | `kSpawnNameMax`−1 = **23** |
| `category` | `""` ⇒ **`"research"`** | `kSpawnCategoryMax`−1 = **15** |
| `note` | `""` ⇒ **`"On it."`** | `kSpawnNoteMax`−1 = **79** |

- The accepted `spawn` vector is **truncated to `kAgentMaxJobs` = 6** items;
  extras are dropped (the journal ceiling refuses them device-side).

### `await[]` (tolerant per-item, RETIRED from the wire)

Retired from the wire (`session.poll` superseded it) but still parsed. A
**non-string** entry is **dropped**; entries are **trimmed** of surrounding
whitespace and blank / whitespace-only entries are **skipped** (a
whitespace-only tag can never match a real job tag, so keeping it would seed a
dead poll). Non-blank entries are kept in order.)NIMBUSDOC"},
  {"turn-contract#live-integration-arrays", "Live-integration arrays",
   R"NIMBUSDOC(`mem_write`, `mem_query`, and `session_ops` are the advertised path for a modern
turn. Like every other array they are **tolerant** per item (a malformed item is
dropped, never a whole-turn error), and each vector is capped to `kAgentMaxJobs`
= **6**.)NIMBUSDOC"},
  {"turn-contract#live-integration-arrays-mem-write-memory-write", "Live-integration arrays - `mem_write[]` → `memory.write`",
   R"NIMBUSDOC(Store facts in the associative VDB this turn (explicit, LLM-directed - never
auto-mirrored).

| Sub-field | Type | Rule | Default |
|---|---|---|---|
| `content` | string, **required non-empty** | capped to `kMemModelMax` = **1200** UTF-8-safe; empty/missing ⇒ item dropped | - |
| `importance` | number | accepts **int- or float-encoded**; **clamped to `[0,1]`** | **0.5** |
| `permanent` | bool | pins the fact so it never decays or prunes | **false** |
| `ttl` | enum string | one of `session` / `days` / `weeks` / `months` / `permanent`; **accepted only when it names a known class**, otherwise ignored and the store's default class applies | store default (`weeks`) |

A non-object item, an item with a non-string / missing `content`, or an
empty-after-cap `content` is **dropped** - the turn stays valid.

A `mem_write` applies **after this turn's reply is sent**, so a reply that says
"I saved that" is a prediction, not a confirmed fact - phrase it as intent, or
confirm it on a later turn.)NIMBUSDOC"},
  {"turn-contract#live-integration-arrays-mem-query-searches-echoed-into-the-next-turn", "Live-integration arrays - `mem_query[]` → searches echoed into the NEXT turn",
   R"NIMBUSDOC(Search strings. Each entry is **trimmed**; blank ⇒ dropped; a non-string entry
is dropped (tolerantly, not an error). Kept entries are capped to
`kSpawnTaskMax − 1` = **4095** UTF-8-safe. Results are echoed into the *next*
turn's context.)NIMBUSDOC"},
  {"turn-contract#live-integration-arrays-session-ops-spawn-terminate-sub-sessions", "Live-integration arrays - `session_ops[]` → spawn / terminate sub-sessions",
   R"NIMBUSDOC(The advertised way to start and stop sub-agents (superseding the retired
`spawn[]`/`await[]`).

- `op` (**required**) - the wire schema advertises **`spawn` and `terminate`
  only**. The fabric is fire-and-forget: there is no live back-and-forth with a
  running sub-agent, so `tell`, `poll`, and `list` were retired from the wire.
  The parser's known set still accepts those three names for old clients, but
  they are **no-ops** on this fabric. A non-object item, a non-string `op`, or an
  **unknown `op` is dropped** (turn stays valid).
- Every other field is used per-op, defaulted-empty, and byte-capped. All nine
  keys (`op`, `id`, `task`, `provider`, `model`, `skill`, `name`, `project`,
  `attach`) are `required` in the strict wire schema as **nullable** types; the
  parser defaults each:

| Sub-field | Used by op | Rule / cap (usable = buffer−1) |
|---|---|---|
| `id` | `terminate` | target session tag; cap `kSpawnCategoryMax`−1 = **15** |
| `task` | `spawn` | the NL instruction; cap `kSpawnTaskMax`−1 = **4095** |
| `provider` | `spawn` (optional) | **lowercased**; `""` ⇒ device resolves by priority; cap `kSpawnProviderMax`−1 = **15** |
| `model` | `spawn` (optional) | cap `kSpawnModelMax`−1 = **39** |
| `skill` | `spawn` (optional) | approved capsule id injected into the sub-agent's brief; cap `kSpawnSkillMax`−1 = **23** |
| `name` | `spawn` (optional) | display name, also the saved-document name; cap `kSpawnNameMax`−1 = **23** |
| `project` | `spawn` (optional) | run tag; the sub-agent's full result auto-saves to the file store as `<project>/<name>-<tag>.md`; cap `kSpawnProjectMax`−1 = **24** |
| `attach` | `spawn` (optional) | array of up to `kSpawnAttachMax` = **4** `<project>/<name>` docs whose full content the device splices into the instruction; each ref capped to **73** bytes, non-string entries dropped |

The retired `tell` op's `message` field (cap `kSpawnTaskMax`−1 = **4095**) is
still read by the parser but has no effect.

Each turn's `[ACTIVE SESSIONS]` context carries a `[SPAWN CAPACITY]` line
(`buildDynamicContext()`, `lib/harness/src/engine.cpp`) - `<live> running,
<queued> queued. You can start up to <N> sub-agents this turn`, where `N =
min(kAgentMaxJobs, kMaxPendingSpawns − queued)`. Spawns run **sequentially** (the
device does one network call at a time, up to `kMaxActiveInflight` = **4** in
flight), draining as each finishes. This is **not** a hard total limit: a big
fan-out runs over successive waves - start a wave now, and when they finish an
automatic turn lets you spawn the next, so a run can work through dozens. So a
turn emits one spawn op per unit of work **up to what `[SPAWN CAPACITY]` allows**,
and carries a larger fan-out across waves rather than silently dropping work.
(The old `[SPAWN SLOTS] N of 6 free` line read as a hard ceiling and made the
head under-spawn; `[SPAWN CAPACITY]` states the honest throughput model.))NIMBUSDOC"},
  {"turn-contract#persistent-scratchpad-response-object", "Persistent scratchpad (response object)",
   R"NIMBUSDOC(`scratchpad` is a top-level, **nullable object** response field - the model's own
working memory, rewritten as it works. Unlike `memory` (which lives for the
**next turn only**), the scratchpad **persists across turns and across reboots**
(`memory::scratchpad()`, serialized to the `orchmem` NVS namespace via
`persistScratchpad()`). It is a **free write** - returning it costs no tool round.

New in v4.1.0: the scratchpad used to be reachable only through the admin-only
`memory.scratchpad` tool; now the model's normal write path is this response
field. That tool remains as an admin read/edit path - see
[tool-catalog.md](tool-catalog.md).

Shape (every key `required`-but-nullable in the strict wire schema):

```json
"scratchpad": {
  "active": "the one thing being done right now (string | null)",
  "short":  [ "this task's steps / a checklist", ... ] | null,
  "mid":    [ "threads to return to over days", ... ]   | null,
  "long":   [ "standing goals", ... ]                   | null
}
```

Update semantics (parser step 10, `orch_turn.cpp`):

- The **whole field null** - or absent, or not an object - leaves the scratchpad
  **unchanged**.
- A **non-null `active`** string sets the active line, capped to
  `kScratchActiveMax` = **240** bytes, UTF-8-safe.
- A **non-null tier array** (`short` / `mid` / `long`) **REPLACES** that tier; a
  **null** tier leaves it unchanged. Each tier keeps at most `kScratchTierItems`
  = **8** items, each capped to `kScratchItemMax` = **160** bytes UTF-8-safe;
  blank / whitespace-only items are dropped.

Because a non-null tier is a full replace (not an append), the model rewrites the
whole tier each time: write the plan when a multi-step task or fan-out starts,
then tick items off on later turns so the thread survives across turns.)NIMBUSDOC"},
  {"turn-contract#parseerror-codes", "ParseError codes",
   R"NIMBUSDOC(`parseTurn` returns `true` iff the turn is contract-valid (`err.code == Ok`). On
failure `out` is left **cleared** (no half-parsed state leaks) and `err` carries
the reason; `err.detail` names the offending field/reason for logs + tests.

| `ParseError::Code` | When |
|---|---|
| `Ok` | valid turn |
| `JsonError` | did not deserialize (garbage / truncated / empty / not JSON) - `detail` is the ArduinoJson error string |
| `NotObject` | valid JSON but the top-level value is not an object (e.g. `[1,2,3]`, `42`) |
| `MissingField` | a required top-level string (`reply`/`memory`/`ask`) is absent; `detail` is the key (e.g. `"reply"`) |
| `WrongType` | one of `reply`/`memory`/`ask` is present but not a JSON string |

`MissingField`/`WrongType` fire **only** for the three required strings. No
array - `device`, `spawn`, `await`, `mem_write`, `mem_query`, or `session_ops` -
can produce a parse error: a present-but-not-array value reads as empty, and a
malformed item is dropped. The `scratchpad` object is equally tolerant: a
present-but-non-object value reads as **no change**, never an error.)NIMBUSDOC"},
  {"turn-contract#backward-compatibility-guarantee", "Backward-compatibility guarantee",
   R"NIMBUSDOC(An **old six-field turn** (`reply`/`memory`/`ask` + `device`/`spawn`/`await`,
no live-integration arrays) still validates and applies exactly as before -
`spawn[]` still parses, and the new `mem_write`/`mem_query`/`session_ops`
vectors simply stay empty. Symmetrically, a new turn may carry only the required
strings plus the live-integration arrays and omit the legacy arrays. In all
cases, an absent - or present-but-mistyped - array parses to an **empty vector**.)NIMBUSDOC"},
  {"turn-contract#validation-order-as-implemented", "Validation order (as implemented)",
   R"NIMBUSDOC(1. Deserialize → `JsonError`; not an object → `NotObject`.
2. `reply`/`memory`/`ask` required strings (`MissingField` / `WrongType`); every
   array key optional and tolerant (absent OR present-but-not-array ⇒ empty).
3. Copy string fields: `reply`/`ask` uncapped; `memory` capped to `kMemModelMax`.
4. `spawn[]` tolerant per-item: non-object / missing-or-empty-`task` items
   dropped; other fields defaulted + provider lowercased + per-field byte caps;
   vector truncated to `kAgentMaxJobs` (6).
5. `await[]` tolerant: non-string items dropped; blank/whitespace-only trimmed
   away.
6. `device[]` tolerant: non-object items dropped; each kept item carried raw.
7. `mem_write[]` tolerant; `importance` int-or-float clamped `[0,1]`; `ttl`
   accepted only when it names a known class; capped to `kAgentMaxJobs`.
8. `mem_query[]` tolerant; trimmed; capped to `kAgentMaxJobs`.
9. `session_ops[]` tolerant; `op` in the known set; spawn fields
   (`skill`/`name`/`project`/`attach`) defaulted + capped; capped to
   `kAgentMaxJobs`.
10. `scratchpad` tolerant: whole field null / absent / non-object ⇒ no change;
    non-null `active` capped to `kScratchActiveMax`; each non-null tier
    (`short`/`mid`/`long`) REPLACES it - blank items dropped, ≤ `kScratchTierItems`
    items, each ≤ `kScratchItemMax`.)NIMBUSDOC"},
  {"turn-contract#constant-reference", "Constant reference",
   R"NIMBUSDOC(| Constant | Value | Source | Applies to |
|---|---|---|---|
| `kMemModelMax` | 1200 | `caps.h` | `memory`, `mem_write.content` |
| `kSpawnTaskMax` | 4096 (−1 = 4095) | `caps.h` | `spawn.task`, `mem_query`, `session_ops.task`/`message` |
| `kSpawnProviderMax` | 16 (−1 = 15) | `caps.h` | `spawn.provider`, `session_ops.provider` |
| `kSpawnModelMax` | 40 (−1 = 39) | `caps.h` | `spawn.model`, `session_ops.model` |
| `kSpawnSkillMax` | 24 (−1 = 23) | `caps.h` | `spawn.skill`, `session_ops.skill` |
| `kSpawnNameMax` | 24 (−1 = 23) | `caps.h` | `spawn.name`, `session_ops.name` |
| `kSpawnProjectMax` | 25 (−1 = 24) | `caps.h` | `session_ops.project` |
| `kSpawnAttachMax` | 4 | `caps.h` | `session_ops.attach` (max docs; each ref ≤ 73 bytes) |
| `kSpawnCategoryMax` | 16 (−1 = 15) | `caps.h` | `spawn.category`, `session_ops.id` |
| `kSpawnNoteMax` | 80 (−1 = 79) | `caps.h` | `spawn.note` |
| `kAgentMaxJobs` | 6 | `caps.h` | vector caps for `spawn`, `mem_write`, `mem_query`, `session_ops`; `[SPAWN CAPACITY]` per-turn ceiling |
| `kMaxPendingSpawns` | 12 | `caps.h` | pending-spawn queue depth; `[SPAWN CAPACITY]` queue room |
| `kMaxActiveInflight` | 4 | `caps.h` | max sub-agents in flight (device concurrency window) |
| `kScratchActiveMax` | 240 | `caps.h` | `scratchpad.active` |
| `kScratchItemMax` | 160 | `caps.h` | each `scratchpad` tier item |
| `kScratchTierItems` | 8 | `caps.h` | items per `scratchpad` tier (`short`/`mid`/`long`) |
| `utf8CapLen` | - | `mem_cap.h` | UTF-8-safe byte cap (drops a straddling multi-byte char whole) |)NIMBUSDOC"},
  // ---- sub-sessions ----
  {"sub-sessions#intro", "Sub-sessions - the orchestrator's background agents, end to end",
   R"NIMBUSDOC(<!-- audience: dev -->

What actually happens when the head model spawns a sub-agent: the exact brief it
receives, the wire protocol per provider, what the head can and cannot do with a
running or finished session, and what is persisted where. Line references are
v3.5.0. Sibling docs: [turn-anatomy.md](turn-anatomy.md) (the head's own prompt),
[harness.md](harness.md) (the lifecycle around turns), and
[orchestrator-storage.md](orchestrator-storage.md) (the storage tiers).)NIMBUSDOC"},
  {"sub-sessions#the-model-s-surface", "The model's surface",
   R"NIMBUSDOC(The turn contract advertises exactly two ops (`session_ops[]`):

- `spawn` - `task` (required), optional `provider`, `model`. Fire-and-forget.
- `terminate` - stop the running session named by `id`.

`tell` / `poll` / `list` are **deliberately not offered** (they used to be
advertised and hard-failed at runtime; "advertised == real" is a standing rule).
If the model emits one anyway it gets a plain-text refusal in its next turn's
input. `[ACTIVE SESSIONS]` (in every turn's input) is the authoritative digest of
what is running; `[FRESH RESULTS]` is how output comes back.

Caps: 6 journal slots (`kAgentMaxJobs`), 4 in-flight (`kMaxActiveInflight`),
a 12-deep pending-spawn queue (`kMaxPendingSpawns`, v4.1.0 - the depth ONE turn
may enqueue, deliberately decoupled from concurrency), task ≤ 4095 bytes
(`kSpawnTaskMax`, truncation logged), one dispatch per ~1.5 s pump cycle,
dispatch deferred below 28 KB free internal heap. The model never sees raw slot
counts - every turn it reads a `[SPAWN CAPACITY]` line (see "Throughput" below).

⛔ **Sub-agents run serially on-device, by design - this is a hard stability
constraint, not a limitation to "fix".** Everything (head turns, dispatch,
polling, synthesis) runs on the single `tg_poll` task with one TLS slot
(`tlsSlots` default 1). Mistral sub-dispatch is fully synchronous - it blocks the
`tg_poll` task inside `POST /v1/conversations` for up to 60 s, because Mistral has
no background/job-id API. OpenAI (`background:true`) and Anthropic (async
sessions) parallelize on the PROVIDER's compute, but the device still polls them
one at a time. Adding a worker task or a second concurrent TLS is hardware-proven
to collapse the head's contiguous internal heap and fail turns (the `tlsSlots`
2→1 A/B, commit 78dab2c), and to starve the watchdog into a reset. Deep fan-out
scales by SEQUENTIAL waves (queue depth), never by concurrency. Do not add
on-device parallelism.)NIMBUSDOC"},
  {"sub-sessions#throughput-the-honest-capacity-model", "Throughput - the honest capacity model",
   R"NIMBUSDOC(The device does one network call at a time, so "how many sub-agents can I run?"
is not a fixed slot count. Every turn the model reads a `[SPAWN CAPACITY]` line,
assembled in `TurnEngine::buildDynamicContext` (`lib/harness/src/engine.cpp`).
This replaced the old blunt `[SPAWN SLOTS] N of 6 free … over the cap is refused`
(v4.1.0): the old wording read as a hard ceiling and made the head under-spawn a
big run ("only 6 slots"). The line now states the real picture - how many are
running, how many are queued, how many it can start THIS turn
(`min(kAgentMaxJobs, queue room)`), that they run SEQUENTIALLY (up to
`kMaxActiveInflight` = 4 in flight, draining as each finishes), and - crucially -
that this is **NOT a hard total limit**. A big fan-out runs over successive
**waves**: start a wave now, and when it finishes an automatic synthesis turn
lets the head spawn the next, so it can work through dozens over a run. Mistral
sub-agents run strictly one-at-a-time; OpenAI/Anthropic ones run in parallel on
the provider while the device collects them one by one.

**The pending queue is decoupled from concurrency** (v4.1.0). `kMaxPendingSpawns`
(12, `caps.h`) is the depth ONE turn may enqueue; `kAgentMaxJobs` (6) is the
journal/concurrency ceiling - two DISTINCT numbers. `JobEngine::enqueueSpawn`
(`lib/harness/src/jobs.cpp`) refuses only when the inbox itself is full, with
*"My spawn queue is full right now - I'll start these, then queue the rest as
they finish."* The pump then drains one queued spawn per cycle into the ≤4
in-flight window, so a deep wave is a bigger inbox for the same single-file
worker - NOT extra parallelism. The queue's internal-SRAM backing is released
once it drains (a `std::vector().swap` back to the baseline reserve), so a deep
fan-out costs that memory only transiently.)NIMBUSDOC"},
  {"sub-sessions#the-brief-the-exact-prompt-a-sub-agent-receives", "The brief - the exact prompt a sub-agent receives",
   R"NIMBUSDOC(Assembled inline in `JobEngine::dispatchSpawn` (`lib/harness/src/jobs.cpp`), in
this order:

```
[Current date-time: 2026-07-27 Sun 14:05 IDT - trust this over any internal sense of the date.]
[SKILL: <id>]
<skill capsule, ≤4096 bytes, from SD /mem/skills/<id>/SKILL.md>

---
<the task text, ≤4095 bytes - always the lead instruction>

[CONTEXT] (the conversation this task came from, oldest first)
- user: <message, ≤300 chars/line>
- assistant: <message>
…

[ATTACHED: <project>/<name>]
<the document's full text - device-supplied, ≤24 KB across all attachments>
```

- **Now-header**: providers give spawned agents no clock, so the brief anchors
  "now" explicitly. Omitted entirely while the device clock is unsynced.
- **Skill capsule** (`composeSkillInjection`, `lib/harness/src/skill_md.cpp`):
  prepended only when the spawn names a skill whose SD capsule has front-matter
  `inject: spawn|both`, and only when that capsule is approved - an
  agent-written capsule stays inert until the owner approves it
  ([harness.md](harness.md)). Built-in skill ids (`web`, `deep_research`, …)
  have no capsule - they act as provider/model routing hints only.
- **[CONTEXT]** (v3.3.0): the last ≤6 chat messages of the spawning conversation,
  digest-rendered under a 1200-byte budget (oldest lines clipped first,
  `(earlier messages omitted - byte budget)` marker when clipped). This is what
  makes "book the place we talked about" actionable. Appended AFTER the task so
  the task stays the lead instruction (pinned by `test_harness_jobs`).
- **[ATTACHED]** (v4.0.0): the full text of each document named in the spawn's
  `attach` array, read from the file store at dispatch and spliced in by the
  firmware. The model references documents by name - cheap output tokens - and
  the device supplies the bytes, so a sub-agent can read files it has no tools
  to open. Up to 4 documents, 24 KB total, UTF-8-safe clip; a document that is
  missing or unreadable becomes an explicit `not found or unreadable` note
  rather than a silent omission. Naming a document only in the task text
  attaches nothing.

Each backend adds its own system prompt around the brief - e.g. OpenAI:
*"You are an autonomous research agent. Complete the task fully and reply with
the final result only."*; Anthropic managed agents get a sandbox system prompt
with bash/web/files tools; Mistral: *"You are an autonomous assistant agent.
Complete the user's task fully and reply with the final result only - no
preamble."*)NIMBUSDOC"},
  {"sub-sessions#what-a-spawn-can-carry", "What a spawn can carry",
   R"NIMBUSDOC(`session_ops` spawn fields, beyond `task`/`provider`/`model`:

| field | effect |
|---|---|
| `skill` | an approved capsule id, injected into the brief (above) |
| `name` | short display name - also the sub-agent's saved-document name |
| `project` | run tag: the full result auto-saves as `<project>/<name>-<tag>.md` |
| `attach` | up to 4 `<project>/<name>` documents spliced into the brief |

`project` is what makes multi-stage fan-out work: every sub-agent in a run
writes into one project, and the outcome (`[saved: …]` or `[persist FAILED: …]`)
is appended to the result the head reads, so the head plans the next stage from
what actually landed rather than from an assumption.)NIMBUSDOC"},
  {"sub-sessions#what-a-sub-agent-can-and-cannot-do-the-decomposition-rule", "What a sub-agent can and cannot do - the decomposition rule",
   R"NIMBUSDOC(A sub-agent returns **text only** to the head and has **no device tools** - it
cannot send Telegram, write the device file store, or touch memory. So the HEAD
does every owner-delivery step after a sub returns (`artifact.save`,
`files.send`). A sub CAN, however, run its provider's connectors server-side: a
sub spawned on a provider that hosts Gmail drafts an email in its own run, a
Notion/Drive sub creates a page, and it reports the outcome back as text.

Decompose a big ask accordingly: **sub-agents gather/research (and run
connectors); the head assembles the results and delivers them to the owner.**
Give each sub a `project` so its full reply auto-saves as a document the head can
then deliver. This is exactly what the model is told each turn - the
`[SUB-AGENT CAPABILITIES]` block, generated per keyed provider in `catalogText`
(`lib/core/src/orch_connectors_wire.cpp`). See [connectors.md](connectors.md) for
which provider hosts which connector.)NIMBUSDOC"},
  {"sub-sessions#provider-model-resolution", "Provider/model resolution",
   R"NIMBUSDOC(1. Explicit `provider` in the spawn, if keyed.
2. Else, if the spawn names a skill matching an enabled **connector**, that
   connector's provider (keyed only).
3. Else the first keyed provider in the sub-session priority order
   (`providerPriority`, web-settable, never model-settable).

The model id is coerced against the provider's choice list (live-harvested, else
compile-time); an invalid model logs `spawn model coerced` and uses the
per-provider sub-model default. `deep_research` on OpenAI upgrades the default
to the deep-research model.)NIMBUSDOC"},
  {"sub-sessions#the-wire-per-backend", "The wire, per backend",
   R"NIMBUSDOC(| backend | create | poll | cancel | survives reboot? |
|---|---|---|---|---|
| **anthropic** | `POST /v1/environments` + `/v1/agents` (both NVS-cached) → `POST /v1/sessions` → brief as a `user.message` event | `GET /v1/sessions/<sid>/events?types[]=agent.message&types[]=session.status_idle&types[]=session.error` | `user.interrupt` event | **yes** (session id in the journal) |
| **openai** | `POST /v1/responses` with `background:true, store:true` + always-on `web_search` tool | `GET /v1/responses/<id>` (status + output text + **usage**) | `POST /v1/responses/<id>/cancel` | **yes** (response id) |
| **mistral** | `POST /v1/conversations` - actually **synchronous**: the reply streams back at dispatch and is cached on-device | poll serves the cached reply once as Done | cache-local | **no** - a reboot loses the cache → "expired" |
| **custom** | `POST /v1/chat/completions` (or `/v1/messages`) - synchronous, single cache slot | cache, once | cache-local | no |

The device polls round-robin every ~15 s on the `tg_poll` task. The result
envelope carries up to **16 KB** of reply (PSRAM-backed, v4.0.0 -
large enough for a sub-agent to return a whole document); token usage is filled only by
OpenAI (the Anthropic events poll returns no usage object - honest zeros).

⚠ **A Mistral sub that outruns the 60 s blocking read is now reported honestly**
(v4.1.0). Because the sub runs INSIDE that synchronous `POST /v1/conversations`,
a slow one outlasts the device's 60 s read deadline. `mistralDispatch`
(`lib/harness/src/providers/mistral.cpp`) classifies that case as
`FabricErr::Timeout` (elapsed ≈ the whole deadline) rather than a connection
failure, and `JobEngine::dispatchSpawn` (`lib/harness/src/jobs.cpp`) tells the
owner *"Started an agent on mistral, but it ran longer than I can wait (60s) and
I couldn't get its result. Try a smaller task or split it into steps."* - where
it used to say "Couldn't start that agent", which was a lie (the agent HAD
started).)NIMBUSDOC"},
  {"sub-sessions#results-how-output-reaches-you", "Results - how output reaches you",
   R"NIMBUSDOC(1. Terminal poll → the result is parked as a **fresh result** (UTF-8-capped at
   3500 chars) and the per-job completion is delivered to the chat.
2. After a 3 s coalesce window (batching near-simultaneous completions), an
   automatic **synthesis turn** runs with a `[FRESH RESULTS]` block and a
   `[SYSTEM]` preamble - no user message. It runs under the same unattended
   rails as scheduled loops (sub-agent output is untrusted web content: no
   reboot, no ttsOn, no loop.create, etc.).
3. If a synthesis turn can't run for 60 s, the raw results are delivered
   directly - results are never lost to a wedged head.
4. Errors skip the fresh store: `Job [<name>] failed: <error>` is delivered
   immediately.)NIMBUSDOC"},
  {"sub-sessions#what-the-head-can-and-cannot-do", "What the head can and cannot do",
   R"NIMBUSDOC(| ask | answer |
|---|---|
| talk to a RUNNING sub-session | **No.** The fabric is fire-and-forget; `tell`/`poll` are refused everywhere (schema, tool registry, prompt self-model). The one adjacent seam - Anthropic's `answer()` for NeedsInput resume - exists in the adapter but has no caller. |
| kill a running session | **Yes** - `session_ops terminate` from a turn (synchronous), or `session.terminate` over LAN MCP / web (staged to `tg_poll`). Note: a terminated job produces no `[FRESH RESULTS]` entry and no `ev:subresult` row. |
| resume / re-address a PAST (completed) session | **No.** Once its result is consumed the journal slot is reclaimed; there is no session handle to call back. Past work survives only as episodic rows, whatever the synthesis turn `mem_write`-ed, and any artifact the sub-agent's result led the head to save. |
| see what a sub-agent did internally | **Not on-device.** Only the final reply text crosses the wire. The full internal transcript (tool calls, thinking) exists provider-side - Anthropic's events API could stream it, but the device deliberately requests only three event types. Capturing it is a documented follow-up. |)NIMBUSDOC"},
  {"sub-sessions#what-is-persisted-where", "What is persisted, where",
   R"NIMBUSDOC(**There is no SQL database.** (SQLite was evaluated and retired; the append-log
episodic store is the system of record. The one `sqlite3` in the repo is the
battery-lab's host-side tool, not firmware.)

| data | store | survives reboot |
|---|---|---|
| active job records (tag, provider jobId, model, chat, state) | **NVS** namespace `agjournal`, 6 slots, ~200 B each | yes - the device re-attaches and resumes polling (anthropic/openai only; mistral/custom caches are RAM) |
| spawn + result trace rows (`ev:spawn` / `ev:subresult`, the glass-box disclosure) | episodic store, the **spawning chat's** session, kind=log | yes (SD day-streams) |
| the sub-agent's final reply | 16 KB envelope → 3500 char fresh result → the synthesis turn's reply + anything it `mem_write`s; 512-char excerpt in the `ev:subresult` row | via episodic / vector memory |
| the sub-agent's internal transcript | **not stored** - provider-side only | - |
| pending spawn queue, fresh results awaiting synthesis | RAM | no - lost on reboot (results already delivered per-job survive as chat messages) |

**No-SD degradation** (same tiering as everything else,
[orchestrator-storage.md](orchestrator-storage.md)): episodic falls back to a
500-message RAM ring + a LittleFS whole-blob (so recent context survives, deep
history doesn't); vector memory caps 5000 → 400 with a flash-floor guard; the
glass-box **trace rows stop entirely** while the SD is absent or demoted
(`traceActive()` requires a live SD) - device-event timeline rows keep writing.
A mid-run SD loss demotes seamlessly (debounced 2-fail/2-pass) and promotes back
on recovery; the current conversation's working set stays in the PSRAM ring
throughout.)NIMBUSDOC"},
  {"sub-sessions#inspecting-it-live", "Inspecting it live",
   R"NIMBUSDOC(- Web chat → the "⚙ … sub-agent events" disclosure under an assistant bubble -
  the spawn brief (task) and terminal result rows.
- `memory.episodic` tool or `GET /api/mem/episodic?kind=log` - the same rows,
  filterable by `since_hours`/`before_hours`.
- `[ACTIVE SESSIONS]` in any turn (or the web Sessions tab) - live state.
- `/api/lastturn` - whether the last head turn spawned anything (section 3, the
  raw `orch_turn` JSON's `session_ops`).)NIMBUSDOC"},
  // ---- connectors ----
  {"connectors#intro", "Connectors - external tools, per provider",
   R"NIMBUSDOC(<!-- audience: user -->

Connectors let the Orchestrator (and the sub-agents it spawns) reach the
outside world. With a connector enabled you can ask, over Telegram or voice,
things like:

- "Any new issues on my repo?" - GitHub returns the live list.
- "How many unread emails do I have?" - Gmail answers.
- "Publish that summary to my Notion workspace." - a page appears.

**How it works, in one sentence:** Nimbus does *not* speak to
Gmail/Notion/GitHub itself. It attaches a small *connector descriptor* to the
LLM request, and the **provider runs the tool server-side in its own cloud**.
So what you can do is whatever your provider hosts, and the credential lives
with the provider (Mistral Studio) or is pasted once into Nimbus's web UI.

> **Scope note.** This is the *provider-attach* model - the AI lab executes the
> tool. Nimbus does not (yet) run its own outbound MCP client, and Google
> **Sheets/Slides** have no hosted MCP anywhere, so they are not reachable this
> way today (they need direct REST - a future pass). Gmail, Calendar, and
> Drive *are* reachable.)NIMBUSDOC"},
  {"connectors#setting-up-a-connector", "Setting up a connector",
   R"NIMBUSDOC(Every connector is a **paste-a-credential card** under **Capabilities →
Connectors** in the web UI. Connector credentials are pasted once into that
card and stored write-only on the device - setup never involves the firmware
repository or any script:

1. Create the credential once in a browser: a GitHub personal access token, a
   Notion integration token, a Slack bot token, an OAuth refresh token, or a
   Studio-side connection.
2. Open the device web UI → **Capabilities → Connectors**.
3. Pick the provider on the connector's card, paste the credential (or, for
   Mistral, just enable it), toggle **enabled**, **Save**.
4. The card's status badge confirms whether it is live on your turns, via
   sub-agents, or idle until you change the host.

The model can never set a connector or read a secret - that rail is enforced
in firmware ([Security](#security)).)NIMBUSDOC"},
  {"connectors#what-each-provider-runs-and-where", "What each provider runs, and where",
   R"NIMBUSDOC(| | On the Orchestrator's **own turns** | On **sub-agents** it spawns |
|---|---|---|
| **OpenAI** | ✅ remote MCP (`server_url` + token) and first-party connectors (Gmail, Calendar, Drive…) | ✅ same |
| **Anthropic** | - (the head turn is a single forced tool; MCP can't run mid-turn) | ✅ BYO remote MCP by URL (`mcp_servers`) |
| **Mistral** | ✅ Studio built-ins + Studio-named connectors - **single-shot turns** | ✅ sub-agents run over the Conversations API; built-ins *and* Studio connectors attach to the spawn dispatch (server-side) |

Two consequences:

- **The current provider matters.** A Mistral connector is only live when
  Mistral is the Orchestrator's host. An Anthropic connector only runs on
  sub-agents. Capabilities → Connectors badges each card with what applies
  *right now*, and the model is told the same thing every turn.
- **Mistral requires the least setup.** Its connectors are authenticated in
  **Mistral Studio** and referenced by name - Nimbus stores *no secret* for
  them.)NIMBUSDOC"},
  {"connectors#how-nimbus-knows-a-connector-is-available", "How Nimbus knows a connector is available",
   R"NIMBUSDOC(Nimbus does not blindly trust that a pasted key works. **Capability validation**
(Capabilities → Models → Tool use) controls how it confirms provider access
before it tells the assistant a connector is available:

- **Off** - trust the key as-is, and make no "verified" claim.
- **Passive** (default) - mark a provider verified once a real call, or a manual
  **Verify**, succeeds; the result is cached.
- **Active** - passive, plus a periodic re-verify on a slow schedule so a revoked
  key or changed access is caught. The re-check is a free metadata call
  (`GET /v1/models`, roughly $0), one provider per wake, spread so each keyed
  provider refreshes about every *re-check* interval (default 24 hours, range
  1–168).

The settings persist as NVS `capProbe` (0/1/2) and `capProbeH` (hours,
`src/agent/store.cpp`). Active mode runs only in Orchestrator mode (the re-verify
needs the TLS heap) and reuses the same one-slot verify worker as the web
**Verify** button - no new concurrency.

**Per-connector credential state (v4.1).** Enabling a connector is a checkbox,
not proof it works. The catalog the assistant reads marks the two states that
need the owner: a connector whose OAuth sign-in **failed** on its last mint
("sign-in FAILED - tell the owner") and a remote MCP / first-party connector
that has **no credential at all** ("NO credential - not usable"), which the
attach layer skips rather than sending a doomed request. A bare name means no
known problem; a successful OAuth mint this boot is positive proof the
credential works (`src/agent/connectors.cpp` `authStateOf`, fed from the one
credential choke point).

> **What validation does not do.** It confirms the *provider key* works, not each
> connector's individual tools. Per-connector *functional* probing - actually
> calling a connector, which would spend tokens - is not implemented. A connector
> can be attached and still fail on a specific call (a private repo, a missing
> scope); the assistant is guided to verify writes by reading them back.

Whatever the mode, the model is told the truth every turn: its
`[PROVIDERS & CONNECTORS]` block marks each provider **available, verified** /
**key present, not yet verified** / **key present but rejected on last check** /
**no key** - and with validation off, **key present (validation off - trusting
key presence)**. That block is built in `catalogText`
(`lib/core/src/orch_connectors_wire.cpp`), fed the verify cache and the mode by
`connectors.cpp` `catalog()`.

---)NIMBUSDOC"},
  {"connectors#github-github", "GitHub {#github}",
   R"NIMBUSDOC(- **Providers:** OpenAI / Anthropic (remote MCP by URL), Mistral (Studio).
- **Credential:** a **Personal Access Token** (`ghp_…`) - GitHub → Settings →
  Developer settings → Personal access tokens. A PAT avoids the OAuth flow
  entirely: paste it once and the connector works.
- **MCP URL (OpenAI/Anthropic):** `https://api.githubcopilot.com/mcp/`
- **Mistral:** connect GitHub in Studio, then enable it by name here (no token
  on device).
- **Unlocks:** issues, PRs and reviews, code and commit search, **creating
  repositories**, and pushing file contents through the GitHub API (releases are
  read-only; there are no Actions or Gists tools). Verified against the
  OpenAI/Anthropic MCP endpoint; the Mistral Studio connector's toolset may
  differ. For repo creation and file pushes the token needs the `repo` scope
  (or a fine-grained token with Administration and Contents read/write).
- **Try it:** once the card is saved, ask the assistant to *"create a private
  repo named test-repo and add a README"* - it should come back with the new
  repo's URL.)NIMBUSDOC"},
  {"connectors#gmail-gmail", "Gmail {#gmail}",
   R"NIMBUSDOC(- **Providers:** OpenAI (first-party connector `connector_gmail`), Mistral (Studio).
- **Credential:** an **OAuth refresh token** created off-device (Google's
  device flow excludes Gmail scopes, so do the one-time auth-code consent in a
  browser and paste the refresh token). For Mistral, connect Google in Studio
  instead.
- **Unlocks:** read, search, draft, and label email.)NIMBUSDOC"},
  {"connectors#google-calendar-google-calendar", "Google Calendar {#google-calendar}",
   R"NIMBUSDOC(- **Providers:** OpenAI (first-party `connector_googlecalendar`), Mistral (Studio).
- **Credential:** OAuth refresh token (as Gmail), or Studio-side for Mistral.
- **Unlocks:** read and manage events; calendar digests.)NIMBUSDOC"},
  {"connectors#google-drive-google-drive", "Google Drive {#google-drive}",
   R"NIMBUSDOC(- **Providers:** OpenAI (first-party `connector_googledrive`), Mistral (Studio).
- **Credential:** OAuth refresh token (as Gmail), or Studio-side for Mistral.
- **Unlocks:** read and write Drive files - report and research-corpus storage.
- **Note:** Google **Sheets/Slides** are *not* covered by the hosted connector;
  they need direct REST (out of scope today).)NIMBUSDOC"},
  {"connectors#notion-notion", "Notion {#notion}",
   R"NIMBUSDOC(- **Providers:** Mistral (Studio) requires the least setup; OpenAI/Anthropic
  via a remote MCP URL work too.
- **Credential:** a Notion **Internal Integration** token (`secret_…`) -
  Notion → Settings → Connections → Develop integrations. Share each target
  page with the integration once. (Notion's *official* MCP mandates
  per-session OAuth and is unusable headless - prefer the static integration
  token.)
- **Unlocks:** create and query pages and databases - report publishing,
  research corpora.)NIMBUSDOC"},
  {"connectors#slack-slack", "Slack {#slack}",
   R"NIMBUSDOC(- **Providers:** Mistral (Studio); elsewhere needs a small REST adapter (future).
- **Credential:** a **bot token** (`xoxb-…`) - create a Slack app once in a
  browser, install it to the workspace, copy the token. No on-device OAuth.
- **Unlocks:** read channel/DM history, search, post messages, reactions.)NIMBUSDOC"},
  {"connectors#linear-linear", "Linear {#linear}",
   R"NIMBUSDOC(- **Providers:** OpenAI / Anthropic (remote MCP `https://mcp.linear.app`), Mistral (Studio).
- **Credential:** a static **Linear API key** (Bearer) that bypasses OAuth
  entirely - Linear → Settings → API.
- **Unlocks:** find, create, and update issues, projects, and comments.)NIMBUSDOC"},
  {"connectors#mistral-built-ins-mistral-builtins", "Mistral built-ins {#mistral-builtins}",
   R"NIMBUSDOC(Hosted tools that run inside Mistral with **no device secret** - just toggle
them on here (provider = Mistral). They attach as `{type:"<name>"}` to the
Orchestrator's own single-shot Mistral turns.

- **web_search** - hosted web search with citations.
- **code_interpreter** - sandboxed Python execution.
- **image_generation** - hosted image generation.
- **document_library** - RAG over your Studio document library.)NIMBUSDOC"},
  {"connectors#mistral-studio-connectors-mistral-studio", "Mistral Studio connectors {#mistral-studio}",
   R"NIMBUSDOC(Third-party connectors (GitHub, Gmail, Notion, and custom MCP servers) are
**authenticated in Mistral Studio**
([console.mistral.ai/build/connectors](https://console.mistral.ai/build/connectors)),
not on the device - no secret is stored here. Two kinds:

- **Built-in connectors** (GitHub, Gmail/Calendar/Outlook): pre-registered -
  you authorize the account in Studio, then reference the connector by its
  **name** here.
- **Custom MCP** (e.g. Notion, DeepWiki): register the MCP server once via
  `client.beta.connectors.create(...)` in the Mistral SDK → you get a
  **connector name/UUID** → reference it here.

Either way, on the device: add the connector with **provider = Mistral**, and
put the Studio connector **name or UUID** in the connector-id field. It
attaches as `{type:"connector", connector_id:"<name-or-uuid>"}` on single-shot
Mistral turns.

> The agentic **tool-loop** turns cannot carry Mistral built-ins or Studio
> connectors: the loop forces tool-choice (the model must call a tool every
> round), which Mistral rejects alongside built-in connectors - a documented
> provider limitation. Those attach on the single-shot Conversations turn and on
> sub-agent dispatch instead. So on loop turns the Orchestrator has only its
> registry tools (the Tavily `web.search` tool when configured) and should
> delegate connector work to a Mistral sub-agent.

---)NIMBUSDOC"},
  {"connectors#operational-notes-live-verified-on-hardware", "Operational notes (live-verified on hardware)",
   R"NIMBUSDOC(- **Mistral connectors attach on single-shot turns** (tool loop off) **and on
  sub-agent dispatch** - not on the agentic tool-loop, which forces tool-choice
  (rejected alongside built-in connectors). Set the Orchestrator host to Mistral
  and the tool loop off to use them on your own turns, or spawn a Mistral
  sub-agent for connector work while the loop is on.
- **Keep it to about 2 enabled Mistral connectors at a time.** One or two work
  reliably (proven: GitHub returned a live issue count, Gmail a live unread
  count). Enabling around 4 at once made the Conversations response come back
  with no message output and the turn failed over to another provider. It
  fails *soft* (no crash), but the connector call won't run.
- **Changing a connector takes effect on the next turn** - the device resets
  the Mistral conversation whenever you add, toggle, or remove a connector
  (connectors are pinned at conversation creation, so this is required for a
  change to apply).
- **Private repos:** a Studio GitHub connector only sees repos its authorized
  account or org can access - a private repo returns "not accessible" even
  though the connector itself is working.
- **Heavy connector writes are streamed, not buffered.** The device parses
  every provider response *off the socket* (only the filter-retained fields
  are stored), so a large connector-write response no longer exhausts the
  board's memory - the Notion page-create that used to restart it now
  completes. For heavy writes and large fetches, prefer **spawning a
  sub-agent** (the Orchestrator is guided to): the work runs on the provider's
  compute and only a short result returns.)NIMBUSDOC"},
  {"connectors#security", "Security",
   R"NIMBUSDOC(- Secrets are **write-only**: the web UI never echoes a token back (it shows a
  "saved" state); editing a card and leaving the credential blank keeps the
  stored secret.
- The model **cannot** configure connectors or read their secrets - the
  `connector` config key is a protected key, refused at the wire.
- Everything is behind the per-device access token (identify once per browser).)NIMBUSDOC"},
  {"connectors#finance-connectors", "Finance connectors",
   R"NIMBUSDOC(Any broker or finance connector (e.g. Alpaca) must stay **read-only** - Nimbus
never places trades or moves money on its own. Keep write scopes off.)NIMBUSDOC"},
  // ---- modes-and-signals ----
  {"modes-and-signals#intro", "Modes & signals - what the settings mean and what the device is telling you",
   R"NIMBUSDOC(<!-- audience: user -->

Nimbus has three settings that shape everything you see and hear. Two of them
you choose; the third is the device's job:

| Setting | Choices | What it controls |
|---|---|---|
| **Battery mode** | Dark · Balanced · Full | How much light the device shows, and how much power it spends doing it |
| **Sound** | level Off / Low / Medium / High, voice pack Terran / Protoss / Zerg, volume 0–100 | Voice clips on device events |
| **Operating mode** | Notifier · Orchestrator | What drives the device: your AI coding sessions, or the hosted agent |

They are independent. A silent device can still be at Full light; a Dark device
still speaks if its sound level is up. What the battery mode does **not** mean:
it is not the battery's charge state (that rides the battery percentage on the
header - `^75%` charging, `=75%` steady on external power, bare `75%` draining),
and `power:Full` in the header means *the Full preset is active*, never "plugged
in".

A quick example: on a desk, most people run **Full** - every session gets a
color arc, the screen updates promptly. Unplugged for the evening, switch to
**Balanced** - the ring quiets to a single soft cue and the screen batches its
updates. Overnight on battery, **Dark** - no lights at all unless a job errors
or needs you.

One exception on Balanced, in Orchestrator mode only: while the assistant is
running sub-agents, the ring temporarily splits into one arc per sub-agent -
the same per-session view Full shows, at Balanced's dimmer brightness - so a
fan-out is glanceable while it works. The arcs fade out as each sub-agent
finishes and the ring returns to its single soft cue.

> Vocabulary changed in July 2026: the old separate light setting is gone - the
> battery mode now decides how much light the device shows. "Ring level"
> survives only as one of the mode's customizable reference values (§1).

---)NIMBUSDOC"},
  {"modes-and-signals#1-battery-modes-are-preset-bundles-you-can-customize", "1. Battery modes are preset bundles you can customize",
   R"NIMBUSDOC(Each battery mode is a **row of reference defaults**. Pick a mode and you get
the whole row; change any single value and your override survives mode
switches until you reset it.

- **On the device:** Settings > Battery mode to pick a mode; Settings >
  Customize to change individual values; Settings > Reset to defaults to drop
  every override.
- **On the web page:** Settings → Battery mode; Settings → Customize this mode;
  the **Revert to Defaults** button clears all overrides at once.

The reference defaults:

| Value | Dark | Balanced | Full |
|---|---|---|---|
| Ring level (§2) | Dark | Calm | Full |
| Ring brightness | 10 | 30 | 60 |
| Animation smoothness (FPS) | 20 | 30 | 60 |
| Screen update pace (batching) | 60 s | 30 s | 15 s |
| Ghost-clear interval (e-ink) | 30 min | 20 min | 15 min |
| On-screen status refresh | 300 s | 120 s | 60 s |
| Low-battery Telegram alert | on | on | off - not needed on external power |
| Needs-you hold | 1 min | 2 min | 5 min |

The needs-you hold scales with the mode like everything else: a desk display
can insist for five minutes, a dim room clears sooner - and every value here
is tunable per mode in Customize.

<details>
<summary>For developers - machine keys and source</summary>

Source of truth: `lib/core/src/profile.cpp` (`kPresets`). Machine keys stay
`battery_saver` / `balanced` / `desk` (NVS, `/api`, the AI config schema) -
only the user-facing labels changed. "Revert to Defaults" is
`revert_overrides` on `POST /api/config`.

</details>)NIMBUSDOC"},
  {"modes-and-signals#2-ring-level-what-the-ring-shows-in-each-mode", "2. Ring level - what the ring shows in each mode",
   R"NIMBUSDOC(Each battery mode binds one **ring level** as its reference default (Dark →
Dark, Balanced → Calm, Full → Full). The "Ring level" row under Settings >
Customize is the only place you change it independently.

| | Dark | Calm | Full |
|---|---|---|---|
| Session arcs and animations | - | - | ✔ every session a color arc |
| Needs-you cue | ✔ one attention LED | ✔ one attention LED | shown as arcs |
| Orchestrator activity glow (working breathe) | - | ✔ | via arcs |
| Idle | dark | dark | dark - a lit ring always means something |
| How long ambient status lingers after the link goes quiet | 5 s | 30 s | 5 min |
| How long a needs-you cue holds | 1 min | 2 min | 5 min |

**Single click = wake the ring**, in every ring level including Full: a ~4 s
full-brightness reveal of live status, then back to normal. This is how you
glance state on a dark idle ring.)NIMBUSDOC"},
  {"modes-and-signals#2-ring-level-what-the-ring-shows-in-each-mode-the-screen-on-a-color-panel", "2. Ring level - what the ring shows in each mode - The screen on a color panel",
   R"NIMBUSDOC(On the reflective e-ink panel the battery mode only changes the lights. On a
color touch panel the **backlight is the largest continuous draw** - bigger
than the ring - so the battery mode reaches it too:

| Battery mode | Backlight |
|---|---|
| Dark | 35% |
| Balanced | 65% |
| Full | 100% |

When the screen rests (screensaver), it dims to a faint glow rather than going
fully dark - a black color panel is indistinguishable from a broken one, and at
this level the backlight draws a few percent of its lit value, so nearly all of
the saving is kept. Any touch or activity brings it straight back to the
current mode's level.

The screen also **rests sooner on a color panel**: the default idle delay is
**5 minutes**, against 60 on e-ink. E-ink is reflective, so resting it saves
nothing and its screensaver is really about ghosting; on a color panel an hour
of backlight at an empty desk is the most wasteful thing the device can do.
Setting the delay yourself (Settings > Screensaver on the device, Settings →
Mode & identity on the web) always wins over either default.)NIMBUSDOC"},
  {"modes-and-signals#2-ring-level-what-the-ring-shows-in-each-mode-the-screen-on-a-board-with-no-ring", "2. Ring level - what the ring shows in each mode - The screen on a board with no ring",
   R"NIMBUSDOC(Some boards, like the [all-in-one](../hardware/all-in-one-cyd.md) (Freenove CYD),
have a single RGB LED instead of the 45-pixel ring. On those, the **Notifier**
status ring is drawn on the screen: each active session shows as an arc in the
same colors and animations the physical ring would use, so the status language is
identical whichever board you have. Unlike a physical ring, the on-screen ring
always renders at full brightness and detail regardless of battery mode - there
is no LED power to save by dimming it, so dimming it would only make it harder to
read. The battery mode instead dims the **backlight** (see
[the screen on a color panel](#the-screen-on-a-color-panel) above), which is
where this board's actual power draw is.

The ring shows exactly what the connected notifier broker sends. A broker with
stale sessions left over from earlier test runs can legitimately show more
"active" arcs than you expect for a moment; the device clears a session that
stops sending frames on its own, within a hold window scaled to the battery mode
(minutes in Full, seconds in Dark) - not something you need to reconnect or
trigger. If it never clears, check the broker's own session bookkeeping rather
than the device.)NIMBUSDOC"},
  {"modes-and-signals#2-ring-level-what-the-ring-shows-in-each-mode-display-flip", "2. Ring level - what the ring shows in each mode - Display flip",
   R"NIMBUSDOC(On the color touch panel, **Settings > Display flip** on the device (or the
Display flip toggle under Settings → Device on the web) rotates the screen 180
degrees for an upside-down mount. It applies right away, and touch follows the
flip so taps still land where you touch. The e-ink panel is fixed orientation,
so the option only appears on the touch model.)NIMBUSDOC"},
  {"modes-and-signals#2-ring-level-what-the-ring-shows-in-each-mode-low-battery", "2. Ring level - what the ring shows in each mode - Low battery",
   R"NIMBUSDOC(Two settings under **Settings → Battery mode**, both about a low battery
rather than about a mode:

| Setting | Default | What it does |
|---|---|---|
| Low-battery light | off | A dim red pulse on the ring, about three seconds each minute, while the battery is low |
| Save power when low | on | Switches to the Dark battery mode while the battery is low, then returns to your chosen mode once it recovers |

The light is off by default because a ring lit all night spends the power it is
warning about - and it is the brightest thing the device does. When on, it is
deliberately recessive: dimmed in Full, pulsed rather than continuous in every
mode, so it reads as information rather than an alarm.

Neither setting weakens protection. The screen notice, the Telegram message,
and the low-battery deep sleep all happen either way; a **job** error still
breathes red at full brightness in every mode, including Dark.)NIMBUSDOC"},
  {"modes-and-signals#3-sound-independent-of-everything-above", "3. Sound - independent of everything above",
   R"NIMBUSDOC(Set under Settings > Sound (device) or Settings → Sound (web): a level per
**operating mode**, a voice pack (Terran / Protoss / Zerg), and a master
volume. Notifier defaults to **Off**, Orchestrator to **Medium** - Notifier
receives a flood of session events, so its clip table is sparser at every
level. Sound pauses automatically while the microphone is recording, and it
never changes what the ring or screen do.

The full event → clip map is [docs/sfx-map.md](./sfx-map.md).)NIMBUSDOC"},
  {"modes-and-signals#4-notifier-vs-orchestrator", "4. Notifier vs Orchestrator",
   R"NIMBUSDOC(| | Notifier | Orchestrator |
|---|---|---|
| Purpose | Status light for your AI coding sessions | The hosted agent (Telegram + voice) |
| Ring is driven by | Session frames from the host broker, over Bluetooth only | The agent's own sessions and turns |
| Bluetooth | on (advertising, bonded) | off - this is why the broker only finds a Notifier board |
| Telegram and providers | off | on |
| Sound default | Off | Medium |
| Status language (§5) | identical | identical |

Switch modes from Settings > Mode on the device (selecting it restarts the
device), Settings → Mode & identity on the web page, or the `MODE` console
command. Everything in §1–§3 applies to both modes.)NIMBUSDOC"},
  {"modes-and-signals#5-the-status-language-colors-motion-themes", "5. The status language - colors, motion, themes",
   R"NIMBUSDOC(One rule to remember: **movement means it needs you or it is working; still
means nothing to do.**

| Status | Color (theme role) | Motion | Read it as |
|---|---|---|---|
| Running | role 0 - the theme's primary | comet (sliding) | working |
| Waiting for input | role 1 - cool analogous | breathe | it needs YOU |
| Awaiting approval | role 3 - amber-leaning | breathe | decision gate |
| Done | role 2 - green-leaning | fade → settles to dim static (38%) | finished, settling |
| Error | the theme's alert hue | breathe | broken, look at it |
| Idle | neutral white, theme-less | static, dim | nothing happening |

**Themes** (10): `teal ocean ember forest openai anthropic mistral rainbow
gemini perplexity` - each is a four-stop color family plus a per-theme alert
hue. Changing the theme recolors every role in both modes, live. The web
legend (Settings → Battery mode) is generated from the same tables the
firmware uses, so it cannot drift from what the ring actually does.

Rules the firmware enforces (and tests assert):

- **Red belongs to errors only.** No theme may place a role in the red band.
- Every theme's four roles are hue-distinct.
- Idle never moves and never wears the theme - a dim, intentionally neutral white.
- One session fills the **full ring**; multiple sessions are arcs separated by
  dim static white divider LEDs - count dividers, count agents.

Full specification: [Notifier status language](./notifier-status-language.md).)NIMBUSDOC"},
  {"modes-and-signals#6-menu-ring-echo", "6. Menu ring echo",
   R"NIMBUSDOC(While the settings menu is open, the ring becomes a **fill bar** - an instant
echo of the slow e-ink. Browsing shows position and count; adjusting a value
(click Volume → rotate → click) shows the value itself. Dim by design; it holds
about 2 s after the last detent, then live status resumes.

---)NIMBUSDOC"},
  {"modes-and-signals#for-developers-where-the-logic-lives", "For developers - where the logic lives",
   R"NIMBUSDOC(If this page ever disagrees with the code, the code's single sources win:

| Thing | File |
|---|---|
| Battery-mode presets | `lib/core/src/profile.cpp` |
| Ring-level render rules | `lib/core/src/ring_plan.cpp` |
| Status → role/motion | `lib/core/src/status_style.cpp` |
| Palettes + alert hues | `lib/core/src/theme.cpp` |
| Arc animation envelopes + dividers | `lib/core/src/ring_animator.cpp` |
| Ambient/needs-you holds + tombstones | `lib/core/src/notifier_map.cpp`, `attention.cpp` |
| Backlight per mode + rest glow | `lib/core/include/nimbus/duty.h` |
| Sound event ranks | `lib/core/nimbus/sfx_map.*` + `docs/sfx-map.md` |
| Header (`ring:/sound:/power:`) | `lib/core/src/epd_screens.cpp` `drawHeader` |)NIMBUSDOC"},
  // ---- led-ux ----
  {"led-ux#intro", "The LED ring - motion & color language",
   R"NIMBUSDOC(<!-- audience: user -->

The ring is not a status readout, it is an **ambient companion on your desk**.
Its job is to make the state of your work *felt* at a glance and in the corner
of your eye - calm when nothing needs you, alive when things happen,
unmistakable when you're needed.

Reading it takes one sentence: **color says what, motion says whether it is
live, brightness says how much it matters.** A sliding comet is work in
progress; a slow breathe is something waiting on you; a still, settled ring
means "handled, relax."

Three design principles behind everything below:

1. **Nothing pops.** Every appearance, change, and disappearance is a short,
   purposeful transition (150–400 ms), never an instant swap. Instant swaps
   read as glitches; motion reads as intention.
2. **Motion means work; stillness means rest.** Anything moving is *happening
   now*.
3. **Color is meaning, brightness is urgency, motion is life.**)NIMBUSDOC"},
  {"led-ux#reading-the-colors", "Reading the colors",
   R"NIMBUSDOC(| Meaning | Hue | Used for |
|---|---|---|
| Running / thinking | blue (170) | active jobs |
| Waiting for you (input) | purple (213) | your turn to answer |
| Waiting for you (approval) | amber (32) | permission gate |
| Done / success | green (85) | completion, connect success |
| Error | red (0) | failures, critical battery |
| Provider accents | Anthropic cyan · OpenAI green · Mistral amber · unknown white | segment tint |
| System / neutral | white | boot, connecting, "saved" blip |
| Setup needed | slow rainbow | unconfigured, invites setup |

Provider accent tints the segment; status drives the *animation*. The two
compose: a blue-comet "running" segment tinted cyan reads as "Anthropic is
thinking."

The exact hues follow your **theme**: each theme maps the status roles to its
own color family, and changing the theme recolors the ring live. The
status → role mapping and the theme rules (red reserved for errors, roles
hue-distinct) are in
[Modes & signals §5](./modes-and-signals.md) and the full specification,
[Notifier status language](./notifier-status-language.md).)NIMBUSDOC"},
  {"led-ux#what-you-ll-see-the-job-lifecycle", "What you'll see - the job lifecycle",
   R"NIMBUSDOC(Every session on the ring is an arc, and every moment of its life gets motion:

| Moment | Motion | Why |
|---|---|---|
| **Birth** (new session) | arc **grows** from 1 LED to full width (~350 ms), starting as a bright shimmer, **settling** to the provider color | "a new thing began" - celebratory but brief |
| **Status change** | **crossfade** the segment old → new hue (~250 ms) | continuity, never a jarring recolor |
| **Progress** | fill **eases** to the new percentage, doesn't jump | smooth, legible |
| **Attention arrives** (input/approval/error) | one **emphasis flash** (bright, 120 ms), then the steady breathe | "hey - look here," then sustained |
| **Completion** (done) | a bright green **success ripple** sweeps the arc → fades to a dim ember | earned closure |
| **Termination** (offline) | arc **contracts** back to a point and winks out (~350 ms) | graceful closure, symmetric with birth |

And smaller, transient feedback:

| Moment | Motion |
|---|---|
| Knob turn (cursor) | soft **trailing comet** follows the cursor LED, decays after the dwell |
| Config saved (menu or web change persisted) | a single white **blip travels once around** the ring - "saved" |
| Voice: recording / processing / speaking | breathe (in) / comet (thinking) / solid (out), with smooth entry and exit |
| Low battery (first warning) | a slow **red breathe** overlaid under everything else |)NIMBUSDOC"},
  {"led-ux#how-the-ring-level-changes-the-language", "How the ring level changes the language",
   R"NIMBUSDOC(The [ring level](./modes-and-signals.md) (Dark / Calm / Full - each battery mode
binds one as its default) decides how much of this vocabulary plays:

- **Dark:** everything collapses to the *single attention LED* - system states
  and the top attention job play on that one LED (dim breathe / emphasis
  flash); the rest of the ring stays dark. Birth, death, and ripple are
  suppressed to a brief one-LED blip. Quiet by design.
- **Calm** (Balanced default): Dark, plus a soft activity glow - the
  Orchestrator's "working" heartbeat and sub-agent births/deaths - so an
  idle-looking ring still shows something is happening, without the full
  segment treatment.
- **Full:** the full motion language above. When **idle** (no jobs, no voice)
  the ring goes **fully dark** - an all-day desk ring shouldn't glow with
  nothing happening. A call for your attention or a single-click reveal still
  lights it, and a real job lights instantly at full brightness.
- The battery mode's **brightness cap** scales everything; in the Dark battery
  mode the whole language runs dimmer and slower (longer cycles, fewer FPS).)NIMBUSDOC"},
  {"led-ux#how-long-a-state-lingers", "How long a state lingers",
   R"NIMBUSDOC(The broker sends a frame on every change, not continuously, so a quiet ring is
normal between events. What lingers is deliberately split so a call for your
attention can't be lost while ambient noise still clears:

- **Ambient** states (running / done / idle) expire on a clock scaled to the
  ring level after the link goes quiet: **Full = 5 min** (a desk display stays
  lit), **Calm = 30 s**, **Dark = 5 s** (battery-frugal). A dead session
  shouldn't leave a stale ring, but a desk shouldn't blank every few seconds
  either.
- **Calls for your attention** (waiting for input / awaiting approval / error)
  are what you must not miss, so they hold well past the ambient cues - **1
  minute in Dark, 2 in Balanced, 5 in Full**, each tunable in Customize - and
  keep animating the whole time. A job waiting on *you* never vanishes just
  because the broker stopped re-sending.)NIMBUSDOC"},
  {"led-ux#wake-the-ring-the-single-click", "Wake the ring - the single click",
   R"NIMBUSDOC(A **single click** briefly wakes the ring: for ~4 s it promotes **any** ring
level to the full-brightness segment treatment, so you can glance live status
on demand - even from a fully dark idle ring. It then decays back to normal.
(Double-click still opens the menu in Orchestrator; long-press is the menu in
Notifier / hold-to-talk in Orchestrator.))NIMBUSDOC"},
  {"led-ux#whole-ring-system-states-design-targets-not-yet-wired", "Whole-ring system states - design targets, not yet wired",
   R"NIMBUSDOC(Some whole-ring moments are designed but **not implemented** - don't cite these
rows as current behavior:

| Moment | Motion | Status |
|---|---|---|
| **Boot / waking** | very dim white breathe, slow (~2.5 s cycle) | **live** (`main.cpp` boot-breathe window → driver `Pattern::Pulse`) |
| **Idle** | dark in every ring level; a needs-you cue or a single click still lights it | **live** (`ring_plan` compose - an empty Full plan renders black) |
| **Connecting** (Wi-Fi/Bluetooth) | dim white comet, brightness building as it retries | *design intent, not wired* |
| **Connected** | one quick bright green flash (150 ms) → fade to idle | *design intent, not wired* |
| **Connect failed / no credentials** | slow amber double-pulse, then rest | *design intent, not wired* |
| **Setup mode** (unconfigured) | slow rainbow drift, dim - pairs with the sign-in QR | *design intent, not wired* |
| **Entering deep sleep** (critical battery) | inward collapse to the top LED, then off | *design intent, not wired* |

<details>
<summary>Why the "not wired" rows exist</summary>

These were once a whole-ring `SysState` overlay in `ring_animator` that **no
production code ever drove** (`setSystem()` had zero callers) - it was removed
in the 2026-07 UX cleanup to stop it reading as implemented. The boot/connect
*audio* cues are live (SFX link sounds on the Wi-Fi had-IP and Bluetooth
connect edges); the LED equivalents are kept here as the design target if and
when they are wired.

</details>

---)NIMBUSDOC"},
  {"led-ux#for-developers-architecture-and-status", "For developers - architecture and status",
   R"NIMBUSDOC(`solide::leds` today exposes only fixed **Patterns** and an **agent-segment**
API - no raw per-pixel access - so the transitions above cannot all be
expressed through it. The clean seam:

```mermaid
flowchart TD
  EV["ring events (born / statusChanged / progress / attention / done / terminated,<br/>system: connecting / connected / failed / setup / sleep)<br/>+ nowMs + Config (ring level, brightness, fps)"]
  EV --> AN["nimbus::ring::Animator<br/>(lib/core - portable, host-tested)"]
  AN --> FR["RGB frame[45]"]
  FR --> SF["solide::leds::showFrame(const RGB* px, int n)<br/>NEW ~15-line upstream primitive"]
  SF --> WS["WS2812B ring"]
```

- **The Animator is portable and host-tested**: it holds per-segment animation
  state (phase, start time, from/to color, progress) and a system-state
  overlay, and on `frame(nowMs)` computes all 45 LEDs. Deterministic (the
  clock is passed in), so tests assert exact LED colors at chosen times during
  a birth grow-in, a death collapse, a crossfade midpoint, a success ripple -
  the golden-frame equivalent for the ring.
- **Device glue is thin**: feed it the same router/notifier events the ring
  already gets, call `frame()` at the profile FPS, push to `showFrame()`.
- **One upstream ask** (flagged to the solide worker):
  `solide::leds::showFrame(RGB[], n)` - a raw framebuffer path alongside the
  existing pattern/segment renderer. Until it lands, the current
  pattern/segment rendering stays and the Animator ships host-tested and
  dark-launched.

**Status:** this document is the design. It is implemented incrementally in
`lib/core` `ring::Animator` (see `test_ring_animator`) - transitions land
host-tested first, and device rendering switches on when `showFrame()` exists
upstream. The existing `ring_plan` (Dark single-LED vs Full segments) remains
the *steady-state* decision layer; the Animator is the *motion* layer that
renders those decisions with transitions between them.)NIMBUSDOC"},
  // ---- memory ----
  {"memory#intro", "Memory - the two RAM pools, the turn budget, and conversation compaction",
   R"NIMBUSDOC(How Nimbus fits a memory-hungry agent into a microcontroller: the two very
different RAM pools and which one is actually scarce, what a live turn costs
and how it stays bounded, and how long conversations fold into a compact
summary instead of growing forever.

This page merges three earlier documents (`memory-model.md`,
`heap-and-psram.md`, `compaction.md`) into one progressive read: start at the
hardware, end at the conversation.

Related: [orchestrator-storage.md](orchestrator-storage.md) (what lives on SD,
PSRAM, NVS, and flash), [turn-anatomy.md](turn-anatomy.md) (what the model
sees), [provider-wire.md](provider-wire.md) (the wire mechanics per provider).

The memory tiers at a glance - information flows left to right from the
shortest-lived tier to the most durable, and each tier is backed by the
storage that fits its lifetime (details: §1–§3 below and
[orchestrator-storage.md](orchestrator-storage.md)):

```mermaid
flowchart LR
  CW["conversation window<br/>(recent messages, verbatim<br/>+ the folded summary)"]
  RM["running memory<br/>(notes the model carries<br/>to its next turn)"]
  SP["scratchpad<br/>(persistent goals -<br/>survives restarts)"]
  VM["vector memory<br/>(long-term facts,<br/>associative recall)"]
  EP["episodic log<br/>(every message,<br/>append-only history)"]
  CW --> RM --> SP --> VM --> EP
  subgraph BACK["storage backing"]
    PS["PSRAM - working sets<br/>(vector search set,<br/>episodic recent window)"]
    SD["SD card - durable bulk<br/>(episodic day-streams,<br/>vector blob, media)"]
    NVS["NVS - tiny config<br/>(scratchpad, keys)"]
    LFS["LittleFS - bounded state<br/>(fold summary, no-SD<br/>fallback caches)"]
  end
  CW -.-> LFS
  SP -.-> NVS
  VM -.-> PS
  VM -.-> SD
  EP -.-> PS
  EP -.-> SD
```)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors", "1. The two pools (read this before touching heap floors)",
   R"NIMBUSDOC(The Solide S3 board is an **ESP32-S3-DevKitC-1 N16R8**. It has **two very
different memory pools**, and conflating them has repeatedly caused work to be
throttled or deferred while most of the device's RAM sat idle. Know which pool
you mean.

| Pool | Size | Speed | DMA | What reports it | Character |
|---|---|---|---|---|---|
| **Internal SRAM** | ~266 KB heap (of 512 KB total; the rest is static/ROM) | Fast | Yes | `ESP.getFreeHeap()`, `heap_caps_*(…, MALLOC_CAP_INTERNAL)` | **SCARCE** - the real constraint |
| **PSRAM** (external, octal) | **8 MB** | ~2–4× slower | Limited | `ESP.getFreePsram()`, `heap_caps_*(…, MALLOC_CAP_SPIRAM)` | **ABUNDANT** - normally ~98% free |

**`ESP.getFreeHeap()` measures INTERNAL ONLY.** When it reads "16 KB free" the
device is NOT out of memory - it has ~8 MB of PSRAM free. It is out of the
*scarce* pool.)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors-what-must-live-in-internal-sram-do-not-try-to-move-these", "1. The two pools (read this before touching heap floors) - What MUST live in internal SRAM (do not try to move these)",
   R"NIMBUSDOC(- **FreeRTOS task stacks** - every `xTaskCreate*` stack is internal. A task
  that does TLS (`tg_poll`'s deep mbedTLS handshake call chain), touches
  flash, or runs in an ISR cannot use a PSRAM stack.
- **DMA buffers** - Wi-Fi, I²S, SPI (display/SD), LED RMT. These request
  `MALLOC_CAP_DMA` explicitly and bypass the PSRAM spill entirely.
- **lwIP pbufs + the TLS record working set** - the ~24 KB "danger zone." A
  network call needs this much *contiguous-ish internal* to not fail; it is
  the real internal red line.)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors-what-is-routed-to-psram-already-don-t-duplicate", "1. The two pools (read this before touching heap floors) - What IS routed to PSRAM already (don't duplicate)",
   R"NIMBUSDOC(Installed in `main.cpp` at boot:

- **mbedTLS allocator → PSRAM** (`mbedtls_platform_set_calloc_free`): the big
  RX/TX content buffers (~16 KB each) + session/cert parse land in PSRAM. The
  ~40 K contiguous handshake that used to fail at ~50 K internal now lands in
  PSRAM.
- **General malloc/`new`/`String`/JSON churn ≥ the spill threshold → PSRAM**
  (`heap_caps_malloc_extmem_enable(N)`; currently **N = 128 B**). Allocations
  `< N` stay internal to keep hot paths fast; `≥ N` go to PSRAM. **Lowering N
  frees more internal** at a small PSRAM-speed cost - this alone took internal
  free from 16 KB → ~48 KB and made Telegram turns run (they had been
  deferring at the 34 KB floor).

Also PSRAM-backed by design: the vector-DB working set
(`setWorkingAllocators`), ArduinoJson node pools (`ps_json.h`) - including the
single-shot turn response docs - per-adapter request/response bodies
(serialized into one contiguous PSRAM-spilled string and written once), the
SFX manifest, and the web audio test-tone buffers.)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors-a-deliberate-exception-the-episodic-deep-history-read-stays-internal", "1. The two pools (read this before touching heap floors) - A deliberate exception: the episodic deep-history read stays internal",
   R"NIMBUSDOC(Not every transient belongs in PSRAM. The episodic **deep-history** search
(`memory.episodic` cold-scan, `GET /api/mem/episodic?cold=1`) reaches history
that sits on the SD card below the boot-scan index by walking the day-streams
backward, one small window at a time, on the **querying task** - the AsyncTCP
web task or the turn task, each of which runs on a tight internal stack. That
read window is a heap allocation on that task, so its size is a **crash
boundary, not a performance knob**: a large window (an early design used
128 KB) starves the task's contiguous internal SRAM and drops the device off
the LAN mid-query. It is kept small (≈8 KB, escalating only when a single
record is longer than the window). What bounds the work is the **per-call read
budget** (a few files, ~128 KB total) - not a big buffer - so deep history
*pages*: the store returns a resume cursor rather than allocating for the
whole scan. This is the counter-lesson to "route churn to PSRAM" - a small
bounded internal read on a stack-tight task is the correct shape. Full design:
[`orchestrator-storage.md`](orchestrator-storage.md) §3a.)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors-the-heap-floors-are-internal-only-guards-not-a-whole-device-budget", "1. The two pools (read this before touching heap floors) - The heap floors are INTERNAL-only guards - not a whole-device budget",
   R"NIMBUSDOC(`agent_config.h` defines `ORCH_TURN_HARD_FLOOR` / `ORCH_AUTO_TURN_MIN_HEAP` /
`ORCH_DISPATCH_MIN_HEAP` / `ORCH_RECALL_MIN_HEAP` / `ORCH_LOOP_MIN_HEAP`, plus
`provider_verify`'s `VERIFY_MIN_MAX8` and the `/api/tools` async-stack guard.
They all gate on **internal** heap. Because the heavy allocations they used to
protect are now on PSRAM, a turn's true internal cost is small transients +
the lwIP/TLS stack, so the floors sit just above the ~24 KB danger zone
(≈28 KB), **not** at the pre-PSRAM 34–40 KB.

**Do NOT re-raise these floors to "survive with no PSRAM."** PSRAM is present
and carries the churn. If turns are deferring ("low on working memory") while
PSRAM is free, the fix is to move more internal churn to PSRAM (lower the
spill threshold), not to raise the floor. A turn that still OOMs fails soft
(error reply, no restart).)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors-levers-to-free-internal-sram-biggest-first-verify-on-device", "1. The two pools (read this before touching heap floors) - Levers to free internal SRAM (biggest first, verify on-device)",
   R"NIMBUSDOC(1. **Route Wi-Fi/lwIP buffers to PSRAM** (`CONFIG_SPIRAM_TRY_ALLOCATE_WIFI_LWIP`,
   Wi-Fi static-buffer counts) - potentially the largest single win; needs an
   `sdkconfig.defaults`.
2. **Lower the malloc spill threshold** (`heap_caps_malloc_extmem_enable`,
   main.cpp).
3. **Right-size / statically-place buffers** (framebuffers, static arrays)
   into PSRAM.
4. **Re-anchor over-conservative internal floors** (above, done).)NIMBUSDOC"},
  {"memory#1-the-two-pools-read-this-before-touching-heap-floors-how-to-measure-don-t-guess", "1. The two pools (read this before touching heap floors) - How to measure (don't guess)",
   R"NIMBUSDOC(`ESP.getFreeHeap()` = internal free; `ESP.getFreePsram()` = PSRAM free.
`/api/state` reports both (`heap`/`heapMin` vs `psramFree`/`psramTotal`) and
`/api/health` lists PSRAM as its own row. For an internal-vs-PSRAM-vs-DMA
breakdown use
`heap_caps_get_free_size(MALLOC_CAP_INTERNAL | MALLOC_CAP_SPIRAM | MALLOC_CAP_DMA)`
and `heap_caps_get_largest_free_block(...)`.)NIMBUSDOC"},
  {"memory#2-heap-under-a-live-turn-what-s-movable-what-isn-t", "2. Heap under a live turn - what's movable, what isn't",
   R"NIMBUSDOC(A live orchestrator turn runs with only **~47 K free *internal* SRAM** (TLS +
lwIP + the orchestrator all resident). PSRAM is nearly empty but is **not** in
the default malloc pool by default. What is already in PSRAM is listed above;
this section records what is stuck internal, and why (measured while chasing
the heavy-connector-write restart, 2026-07).

### What is STILL internal (and why)

- **lwIP RX pbufs** - inbound TLS/socket data queues as pbufs in
  `MALLOC_CAP_INTERNAL/DMA` by design; the extmem hook cannot move them. This
  was the dominant internal spike when the device buffered a whole large
  response. **Mitigation: streaming.** The transport stream-parses the
  response off the socket (`transport_tls.cpp` `execJson` →
  `deserializeJson(doc, BlockingClientReader, Filter)`), so pbufs drain
  continuously as the parser pulls bytes instead of piling up while a whole
  body is buffered. This is the real fix for the heavy-connector-write
  restart.
- **Small (< 128 B) allocations** - stay internal by the extmem threshold;
  cheap and not worth lowering the threshold (churn).
- **FreeRTOS task stacks** - e.g. `tg_poll` (16 K), which runs the whole
  turn. Internal by nature. The response parse is bounded to
  `NestingLimit(16)`, well within this stack.

### The size-transparency contract

The harness must handle any response / context / file size without crashing:

1. **Responses stream** off the socket; only filter-retained fields are stored
   (a 128 KB wire body → < 4 KB retained doc - `test_harness_streaming`).
2. **Tool results into the model are clamped** (4 KB/result, 24 KB total -
   `head_loop`), so context stays bounded.
3. **Heavy connector work is offloaded** to a sub-agent on the connector's
   provider (the orchestrator is guided to; routing in `jobs.cpp`), so the
   device only ever ingests a short result.
4. **Large payloads spill to SD** (the artifact store) rather than transiting
   RAM or the model's context.

### Do NOT

- Do not raise the turn heap floors to "fix" OOM - the fix is streaming +
  PSRAM routing, not gating turns out (the historical 34 K floor
  false-deferred healthy turns; the live floor is 28 K).
- Do not buffer a whole provider response then parse - stream it.
- Do not put connector write content through the model's context -
  offload/spill.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold", "3. Conversation compaction - the fold",
   R"NIMBUSDOC(How the device keeps long conversations fast, cheap, and continuous: when a
chat accumulates enough history, the device summarizes it into a compact
per-chat memory (the **fold**), resets the provider-side thread, and keeps
going - nothing the owner said is lost, and every future turn carries the
summary instead of an ever-growing transcript. Shipped in v3.6.0 after a
reviewed design; E2E-proven on hardware.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold-owner-s-view", "3. Conversation compaction - the fold - Owner's view",
   R"NIMBUSDOC(- **When it happens**: automatically, after roughly 48 KB of conversation on a
  chat since its last fold (knob: `compactKB`, 8–512, 0 turns auto-fold off) -
  or on demand with the **`/compact`** command (owner-only, also listed in
  /help).
- **What you see**: automatic compaction is silent - its record is the
  "Conversation compacted: … folded into a … summary" row in the chat history.
  A manual `/compact` acknowledges immediately and then confirms with
  "✓ Compacted." (or tells you it couldn't, with the next step). The web chat
  never gets notices (they would desync the reply pairing).
- **What changes**: nothing you'd notice in conversation quality - the last
  dozen messages stay verbatim in the model's context, the summary carries
  everything older, and long-term facts were already in vector memory. What
  improves: turns stop getting slower and more expensive as a chat ages.
- **If a provider is down**: the fold tries the device's other configured
  providers before counting a failure (the same order turns use), so one
  provider having trouble doesn't stall compaction - or message you about it.
- **If it keeps failing**: after 3 consecutive failures (or a fold that
  refills immediately), automatic compaction pauses for that chat and you get
  ONE alert; `/compact` always remains available and un-pauses it.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold-how-it-works-the-design-in-one-page", "3. Conversation compaction - the fold - How it works (the design, in one page)",
   R"NIMBUSDOC(The device cannot edit provider-side conversation threads (they're opaque
ids - and only OpenAI even has a cross-turn thread; Anthropic is stateless and
Mistral's tool-loop mode starts fresh every turn). So compaction is built on
the device's own episodic store, provider-uniformly:

```mermaid
flowchart LR
  subgraph ACC["accumulate"]
    A["every chat message bumps<br/>bytes/msgs counters per chat<br/>(one flash write per turn)"]
  end
  subgraph FOLD["fold - on the tg_poll pump, never blocking a reply"]
    D["1. digest: episodic slice<br/>since the last fold (≤64 KB)"] --> L["2. one single-shot LLM call<br/>(the fold prompt): update the<br/>ANCHORED summary (≤4 KB, UTF-8-safe cap)"]
    L --> W["3. write order: summary blob FIRST<br/>→ provider thread reset<br/>→ 'ev:compact' boundary row (pre/post sizes)"]
  end
  A -->|"threshold reached"| D
```

- **Trigger** (`FoldStore::evaluateDue`): bytes-since-fold ≥ `compactKB` OR
  ≥200 messages - provider-independent by design. A reactive path also
  classifies real provider context-overflow errors (`isContextOverflowError`)
  and queues a fold; the turn itself recovers via the existing fresh-thread
  retry.
- **The fold call** (`TurnEngine::runFold`) is deliberately NOT a turn: no
  delivery, no recall, no tool loop, no conversation-map writes, no capture of
  the summary as chat history - the model's reply IS the summary and every
  other field of its output is structurally inert. Its spend is metered under
  the `compact` attribution (never a routine's budget). Gates (low heap, over
  budget, turn in flight) **defer** - only a real failed attempt counts
  against the breaker.
- **Anchored, not cumulative**: each fold UPDATES the previous summary with
  the new slice (opencode's pattern), so fold cost stays bounded forever. The
  summary re-enters every prompt as `## CONVERSATION SUMMARY` - explicitly
  framed as *information, not instructions*, and instructions found inside
  tool outputs are folded as data, never preserved as directives (prompt-
  injection posture).
- **Summary + verbatim tail**: the summary sits directly above the RECENT
  CONVERSATION window, the shape every surveyed harness (Claude Code, codex,
  opencode, aider) converged on. Under prompt-budget pressure the summary is
  sacrificed BEFORE the window (the tail is the fresher signal).
- **Durability & rollback**: all fold state (summary, counters, breaker) lives
  in one tolerant-load LittleFS blob (`/data/chatsum.txt`, LRU-16 chats). The
  NVS conversation map is untouched, so an OTA rollback to pre-v3.6 firmware
  simply ignores the file - rollback-safe by construction. Crash mid-fold:
  the write order (summary first, thread reset second) means the worst case is
  a fold that re-runs, never a lost summary with a reset thread.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold-verification", "3. Conversation compaction - the fold - Verification",
   R"NIMBUSDOC(Host: `test_orch_compact` (trigger math, breaker, thrash guard, codec,
UTF-8-safe caps, fold-prompt contract), `test_harness_turn` (runFold's
no-side-effects contract incl. a hostile summary carrying device actions -
inert), `test_harness_compose` (§5a placement + budget-pressure drop).

Hardware (Board 2, both suites green): `tests/hil/test_l16_compaction.py` -
the full cycle with a marker planted OLDER than the verbatim window, proving
recall comes from the summary (`/api/lastturn` shows it) - and
`tests/hil/test_l15_memory_caps.py` - vector-store cap eviction (synthetic-only
damage), episodic retention past the PSRAM ring, and a fold completing from
the RAM ring under a simulated SD failure. Measured along the way: 512
episodic-ring rows with short texts pin ~30 K of internal SRAM (small strings
don't spill to PSRAM), which is why the ring deliberately stays at 512.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold-reference", "3. Conversation compaction - the fold - Reference",
   R"NIMBUSDOC(| thing | where |
|---|---|
| knob | `compactKB` (NVS; default 48, clamp 8–512, 0 = auto off) |
| command | `/compact` (owner-only; async - runs on the next pump pass) |
| boundary record | episodic row, kind=log, tag `ev:compact`, in the chat's session |
| prompt section | `## CONVERSATION SUMMARY` (above the recent window) |
| state file | LittleFS `/data/chatsum.txt` (rollback-safe; LRU-16 chats, byte-bounded, written atomically) |
| fold prompt + trigger core | `lib/core/{include/nimbus/orch/compact.h, src/orch_compact.cpp}` |
| engine call | `TurnEngine::runFold` / `clearChatConv` (lib/harness) |
| device pump | `compactTick()` in `src/agent/orchestrator.cpp` |
| test seams (TEST builds) | `CTX?` / `COMPACT <chat>` / `MEMFILL` console + `/api/test/{ctx,compact,memfill}` |

⚠ **Assert on the `ev:compact` row, not on `CTX?`.** The diagnostic reads a
cross-task echo buffer, and until v3.7.0 that buffer was refreshed only at turn
end - so it reported the fold stamp as of the last TURN, never as of the last
FOLD, and a completed compaction stayed invisible until the next turn ended. A
test written against it passed only when the echo happened to be one fold stale,
observing the *previous* run's fold. The echo is now republished on every fold
outcome (success and failure alike), but the durable `ev:compact` episodic row
remains the right thing to assert on: it is written only on success, it carries
the byte/message counts, and it survives a restart.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold-derived-context-budgets-context-fabric-2026-08-05", "3. Conversation compaction - the fold - Derived context budgets (Context Fabric, 2026-08-05)",
   R"NIMBUSDOC(Every context allocation is **derived from the head model's context window**
rather than hardcoded: `nimbus::orch::deriveBudget(ctxTokens, overrides)`
(`lib/core/src/orch_budget.cpp`) turns the window into a system-prompt budget,
summary cap, verbatim-tail cap, per-tool-result clamp, cumulative tool budget,
sub-agent brief size and fold-slice size - each a ratio with a floor and a cap.
The window comes from the existing `modelCtxTokens()` table.

**Anchor invariant:** at a 200K window the derived values equal the previously
hardcoded constants exactly (32768 / 4096 / 3000 / 8192 / 65536 / 1200 / 65536),
so a device on defaults behaves identically. A smaller-window model degrades
gracefully; a larger one automatically gets a richer prompt.

**Owner overrides win.** `max_context_bytes` (memory config) and the two loop-cap
NVS keys now treat **0 / absent as "auto"**; any value you set is used verbatim
under the same clamps as before. `/api/orch` reports `loopRescapEff`,
`loopTotcapEff` and `ctxTokens` so the UI can show "auto (currently N)". Each
turn logs one line: `budget: ctx=200K sys=32768 res=8192 tot=65536 (auto|owner)`.)NIMBUSDOC"},
  {"memory#3-conversation-compaction-the-fold-gradient-trimming-never-a-dumb-clip", "3. Conversation compaction - the fold - Gradient trimming - never a dumb clip",
   R"NIMBUSDOC(The research principle (summary + verbatim tail) also applies **inside** a
turn and at every clip site:

- **In-turn replay (Anthropic only** - OpenAI/Mistral keep history server-side**)**:
  once the replayed conversation passes a quarter of the window, rounds older
  than the newest two collapse to one `[earlier round N] tool: gist… (N B)` line.
  Pairs fold whole, so every `tool_use` keeps its `tool_result`.
- **Tool results**: a clamped result keeps its full text in the recent-results
  ring and the marker says how to get it -
  `…[truncated 8192 of 51234 B - fetch the rest with results.get("r3")]`.
- **Sub-agent results**: past the 6-slot block cap, a result parks as a one-line
  stub with a `results.get("sub:<tag>")` pointer instead of being dropped; the
  episodic row on SD keeps the full text.
- **Context assembly**: the anchored summary **clips** (floor 1024 B) before it
  is dropped, and anything omitted is named in a bounded `## OMITTED (budget)`
  trailer so the model knows a category was withheld rather than empty.)NIMBUSDOC"},
  // ---- turn-anatomy ----
  {"turn-anatomy#intro", "Turn anatomy - what the model actually sees, and how to inspect it raw",
   R"NIMBUSDOC(<!-- audience: dev -->

Every orchestrator turn is assembled from scratch on the device. When a reply seems
to come out of nowhere ("what are you talking about?"), the answer is always in one
of the blocks below - and you can see the **exact raw text** of the most recent
turn at:

```
GET http://<device>/api/lastturn         (token-gated; the LAST turn, from RAM)
GET http://<device>/api/trace?turn=<id>   (token-gated; ANY recent turn, from SD)
```

Both return one plain-text page with four sections: the exact system prompt, the
exact per-turn input, the model's raw `orch_turn` JSON, and (since v4.2.0) the
**tool-loop transcript** - the mid-turn rounds, with each tool call's arguments
and its result. Captured for a completed turn, success or failure.

`/api/lastturn` is a single RAM slot that the next turn overwrites.
`/api/trace?turn=<id>` is the same anatomy kept **per turn** on the SD card
(`/mem/trace/<turnId>.txt`), so you can open a turn from hours ago.

In the web **Chat** tab you don't need either URL: every turn's disclosure has a
**Turn anatomy** button that fetches its own dossier, and any clipped tool result
has **Show full result**. Under each reply a chip reports the host, model,
tool-call count and the turn's real billed tokens.

**Turn identity.** Every row of a turn - the reply, each tool call, each thinking
row - is tagged `turn:<user row id>` (e.g. `turn:m0000a3f2`), which is the id of
the turn's own user message row. It is stable across reboots, so the chat groups
a bubble with its trace by identity. (Before v4.2.0 the tag was `turn:tN` from a
RAM counter that reset every boot and the UI grouped by chronological adjacency,
which mis-filed rows whenever a Telegram or voice turn interleaved with a web one.)

Turn details are **SD-gated** and gated on **Activity recording** (Capabilities →
Models → Tool use, `otrace`, default on). With either missing the chat says so
rather than showing an empty panel; the dossier ring keeps the most recent 16
turns (≤768 KB), and older ones report `evicted`.

A byte-pinned copy of the full system-prompt template lives in the goldens:
`test/golden/orch_prompt_default.txt` (tool-loop ON) and
`test/golden/orch_prompt_recall_loopoff.txt` - regenerated with
`GOLDEN_UPDATE=1 pio test -e native -f test_harness_compose`. Those two files ARE
the raw template, kept honest by CI.

For the whole lifecycle around a turn (boot, ticks, sub-agents, loops, dreaming,
watchdogs) see [harness.md](harness.md); for the sub-agent side see
[sub-sessions.md](sub-sessions.md).)NIMBUSDOC"},
  {"turn-anatomy#the-three-parts-of-a-turn-1-instructions-the-system-prompt-rebuilt-every-turn", "The three parts of a turn - 1. INSTRUCTIONS - the system prompt (rebuilt every turn)",
   R"NIMBUSDOC(Assembled by `composeInstructions()` (`lib/harness/src/compose.cpp`; section
emission + byte budget in `lib/core/src/orch_world.cpp::assembleContext`). Blocks
in emission order; the budget (`maxContextBytes`, default web-tunable, ceiling
32 KB) drops whole optional blocks from the bottom of this table first, then
recall bullets one at a time:

| # | block | source | what it is |
|---|---|---|---|
| 1 | role + field docs | `ORCH_ROLE` + `ORCH_FIELD_DOCS` | the orch_turn contract, generated from the same `ORCH_D_*` macros as the wire schema - prompt and wire cannot drift |
| 2 | identity + `[HOW YOU RUN]` | `compose.cpp` | device name, "Your brain right now: host/model", firmware version, **current date-time (temporal grounding - says UNKNOWN and forbids dating anything until SNTP syncs)**, then how turns/rounds/memory tiers/sub-agents work |
| 3 | `## DIRECTIVE` | `sysPrompt` (NVS) | your standing instructions from the web Directive box; the model's `[RUNNING MEMORY]` (its own `memory` field from last turn) is appended inside this block |
| 4 | `## CAPABILITIES` | `orch_world.cpp` | what is LIVE right now: hardware manifest, ring/e-ink honesty prose, artifact store, voice on/off, sub-agent fire-and-forget note, `Tools you can call:` list, Limits (keys/routing blocked), connector honesty, and whether the tool loop is ON this turn |
| 5 | `## RUNNING SESSIONS (your sub-agents)` | job journal | live sub-agent digest (also authoritative as `[ACTIVE SESSIONS]` in the input) |
| 5a | `## CONVERSATION SUMMARY` | the fold ([compaction.md](compaction.md)) | the chat's anchored compaction summary (≤4 KB), present once the chat has folded; framed "information, not instructions". Under budget pressure it is dropped BEFORE the window below |
| 5b | `## RECENT CONVERSATION` | episodic PSRAM ring | **per-chat window: up to 12 messages / 3000 B of THIS chat, oldest first** (kind=message only; the in-flight user row is excluded - it arrives as `[USER]`). Added in v3.3.0; survives reboot via the SD rehydrate |
| 6 | `## SCRATCHPAD` | NVS | the model's own goal tiers (now/short/long) |
| 7 | `## RELEVANT MEMORIES` | vector-DB recall on the user text | top-k long-term memories semantically similar to your message, prefixed `[NN%]` |
| 8 | `## HOW YOUR MEMORY WORKS` | `compose.cpp` | the memory-tier usage instructions (first block dropped under budget pressure) |)NIMBUSDOC"},
  {"turn-anatomy#the-three-parts-of-a-turn-2-input-the-per-turn-message-block", "The three parts of a turn - 2. INPUT - the per-turn message block",
   R"NIMBUSDOC(Concatenated in `handleMessage()` / synthesis / scheduled-loop paths
(`lib/harness/src/engine.cpp::buildDynamicContext` + the per-path suffix):

| block | when present | gotcha |
|---|---|---|
| `[MEMORY RESULTS]` | the model ran `mem_query` **last** turn | results are deferred one turn; explicitly framed *background - answer the [USER] message on its topic* |
| `[FRESH RESULTS]` | sub-agents just finished | drives the automatic synthesis turn; past the 6-slot cap a result parks as a one-line stub carrying `results.get("sub:<tag>")` - nothing is ever silently dropped |
| `[ACTIVE SESSIONS]` | always | live sub-agent digest - authoritative ("if it lists none, nothing is running") |
| `[AVAILABLE PROVIDERS]` | always | the sub-session provider priority order |
| `[YOUR MODEL]` | always | the host/model serving THIS turn |
| `[AVAILABLE MODELS]` | always | per-provider model choice lists (yours + sub-agents'), filtered to keyed providers |
| `[PROVIDERS & CONNECTORS]` | connectors configured | which cloud connectors each provider carries |
| `[CHANNEL]` | owner turns | which channel the message arrived on + reply-routing rules |
| `[USER]` | owner turns | your message - **the only thing you typed that the model sees this turn** |
| `[SYSTEM]` | synthesis turns | "sub-agents finished, no new user message - synthesize" |
| `[SCHEDULED LOOP]` | loop firings | the routine's stored prompt; no user message |

`[CONTEXT]` is **not** a head-turn block - it rides the *sub-agent brief* only
(see [sub-sessions.md](sub-sessions.md)).)NIMBUSDOC"},
  {"turn-anatomy#the-three-parts-of-a-turn-3-conversation-history-the-part-people-assume-wrong", "The three parts of a turn - 3. Conversation history - the part people assume wrong",
   R"NIMBUSDOC(There is no full transcript replay. Cross-turn continuity comes from exactly
**five** sources:

0. **`## CONVERSATION SUMMARY`** (since v3.6.0) - the chat's anchored fold
   summary: everything older than the verbatim window, compacted. See
   [compaction.md](compaction.md).
1. **`## RECENT CONVERSATION`** (since v3.3.0) - the last ≤12 messages / 3000 B of
   this chat, auto-injected from the episodic store's PSRAM ring every turn. This
   is per-chat (Telegram A ≠ web ≠ voice) and survives reboot.
2. **The provider-side conversation chain** - per chat AND per host since v3.3.0
   (`orchConv` map, LRU-8). While it holds, the provider re-serves prior turns
   itself. It **resets** on: host failover, the same-host fresh retry, a
   token/provider switch, a compaction fold (deliberately - the summary
   replaces it), and (OpenAI only) whenever the last response carried
   unanswered tool calls. `/api/lastturn` prints whether the chain was CONTINUED
   or FRESH.
3. **`[RUNNING MEMORY]`** - the model's own running notes, per-chat since v3.3.0.
4. **Long-term memory recall** - only what's semantically similar to the new text.

The episodic store (`memory.episodic`, the web Chat pane) additionally records
everything durably - including tool calls, thinking, and sub-agent events since
v3.2.1 - and the model can query it with time windows (`since_hours` /
`before_hours`); the recent window above is the auto-injected slice of it. Row
text is clipped (args 256 B, results 1 KB, thinking 1 KB); since v4.2.0 the FULL
call + result is parked alongside as a blob when the clip bit, reachable from the
chat's "Show full result" (or `GET /api/mem/blob?path=`).)NIMBUSDOC"},
  {"turn-anatomy#diagnosing-a-confusing-reply", "Diagnosing a confusing reply",
   R"NIMBUSDOC(Work through the blocks above in order: open that turn's **Turn anatomy** in the
Chat tab (or `/api/lastturn` for the most recent one). It shows whether the
conversation chain was CONTINUED or FRESH (a fresh chain plus an ambiguous
message is the most common cause of a non-sequitur), whether a `[MEMORY
RESULTS]` or `[FRESH RESULTS]` block led the input, exactly which memories were
recalled, and - in section 4 - every tool call the model made mid-turn with its
arguments and result.
Historical failure modes and their fixes are cataloged in
[TEST_PLAN.md](TEST_PLAN.md).)NIMBUSDOC"},
  {"turn-anatomy#fetching-what-a-view-left-out-results-get", "Fetching what a view left out - `results.get`",
   R"NIMBUSDOC(Every clip in a turn is a **view**, not a loss. The full text of a clamped tool
result or an overflowed sub-agent result lands in a PSRAM ring, and the model can
widen the view on demand:

- `results.get(tag, offset?)` - returns a bounded page (`bytes 0-8192 of 51234`)
  of the stored result; page on with `offset`. Unknown tags point at
  `memory.episodic`, which holds the durable copy on SD.
- `results.list` - tag / kind / name / size for everything currently fetchable.

Tags are `r<n>` for tool-result spills and `sub:<jobtag>` for sub-agent results.
The truncation marker and the `[FRESH RESULTS]` stubs embed the tag directly, so
the model never has to guess one. See docs/compaction.md for the gradient rules.)NIMBUSDOC"},
  // ---- people-and-privacy ----
  {"people-and-privacy#intro", "People and privacy - roles, quotas, what stays private",
   R"NIMBUSDOC(<!-- audience: user -->

Nimbus started as one person's device. It now talks to whoever the owner lets
in - family, a team, a group chat - and that changes what "remember this" has
to mean. This page covers who can do what, what each person can and cannot
see, and where the boundaries are actually enforced.

The short version: **everyone gets their own memory, their own files, and
their own conversation history. Nothing crosses between people unless a file
is explicitly shared, and vector memory never crosses at all.**

As an admin, the things you can do:

- Approve someone and pick their role - in the web UI, or by just asking the
  assistant ("approve 12345 as a guest").
- Raise or lower a person's storage limits ("give Alex more storage").
- Revoke someone - they lose access immediately; their data stays, in case you
  restore them later.)NIMBUSDOC"},
  {"people-and-privacy#roles", "Roles",
   R"NIMBUSDOC(Every chat the device knows about has exactly one role.

| Role | What it means |
|---|---|
| **Unknown** | Messaged the device but was never approved. No data, no control. This is the default, and it is also what revoking someone returns them to. |
| **Guest** | Approved, with the tightest limits. Own memories and files, memories expire soonest, cannot share files, cannot pin anything permanently. |
| **User** | Approved. Own memories and files, a small budget of permanent pins, and may share their own files. |
| **Admin** | Runs the device. Sees every namespace and every file, manages people, changes settings, installs updates. Not subject to quotas. |

Two rules the device enforces for you:

- **There is always at least one admin.** Demoting or removing the last one is
  refused, in the web UI and in conversation alike. A device nobody can
  administer is worse than any mistake that refusal prevents.
- **An upgrade changes nobody's access.** When a device running an older
  firmware first boots into this one, today's Telegram owners become admins
  and everyone else on the allowlist becomes a user - the access they already
  had.

### Setting roles

Two surfaces, one source of truth:

- **Capabilities → Connectors → Telegram** - each person's chip shows a role
  badge. Click it to cycle admin → user → guest. Their limits are shown next
  to it.
- **In conversation** - an admin can just ask: "approve 12345 as a guest",
  "what can Sam do?", "give Alex more storage". The assistant has
  `tenant.list`, `tenant.set_role`, and `tenant.set_quota`, and every one of
  them refuses a non-admin caller.

The model can never set a role for itself or change its own limits, and none
of these tools run in a scheduled routine - a role change is a thing a person
asks for while present.)NIMBUSDOC"},
  {"people-and-privacy#quotas", "Quotas",
   R"NIMBUSDOC(Each person has a ceiling on what they can store. Set one explicitly to
override the default for their role; leave it at zero to inherit.

| Limit | Guest | User | Admin |
|---|---|---|---|
| Memories (vectors) | 100 | 500 | unlimited |
| Files | 1 MB | 8 MB | unlimited |
| How long a memory lives | 30 days | 1 year | unlimited |
| Permanent pins | none | 10 | unlimited |

**A request over the limit is clamped, not refused.** Someone asking the
device to remember something "forever" gets their maximum instead, and is told
so - the fact still lands. The exception is permanent pins, which are counted
rather than clamped, because a pin is exempt from both eviction and cleanup:
unbounded pins are storage a quota can never reclaim.)NIMBUSDOC"},
  {"people-and-privacy#what-is-private-and-what-can-be-shared", "What is private, and what can be shared",
   R"NIMBUSDOC(| | Crosses between people? |
|---|---|
| Vector memory (what the device recalls) | **Never** |
| Conversation history | **Never** |
| Files | Only when their owner shares them |

**Vector memory is deliberately never shared, not even opt-in.** Recall is
automatic - the device pulls relevant memories into its context before it
answers. A shared memory pool would mean anyone who can write a memory can put
words into the context of everyone else's conversations, including an admin's.
That is a prompt-injection channel with no upside, so the design forecloses
it. Device-level facts live in the admin's namespace instead.

**Files may be shared, and sharing is read-only.** A person can mark their own
file shared, which makes it visible and readable to everyone. It does not
become writable: another person cannot overwrite it, and cannot un-share it
either. Only the owner controls their file. Guests cannot share at all.
Admins can read every file regardless.

**Revoking access suspends, it does not destroy:** a revoked person
immediately loses both reads and writes, and if they are restored later their
data is still there.)NIMBUSDOC"},
  {"people-and-privacy#photos-and-files-sent-to-the-device", "Photos and files sent to the device",
   R"NIMBUSDOC(**A photo becomes a description.** When someone sends a picture, the device
looks at it once, writes down what it sees, and the description is what enters
the conversation. The original is kept on the SD card, so it can be looked at
again later; the conversation carries a sentence, not several hundred
kilobytes of image that would crowd out everything else.

**Media needs an SD card.** With no card the device says so and suggests
describing the file in words instead. It will not half-accept something it
cannot keep.

If no vision provider is configured, the photo is still stored and
acknowledged - the conversation simply does not gain a description.)NIMBUSDOC"},
  {"people-and-privacy#viewing-files-in-the-web-ui", "Viewing files in the web UI",
   R"NIMBUSDOC(**Memory & Files** lists everything stored, with **view** next to files that
can be previewed in place: images render inline, text opens in a panel.

Previews are narrow on purpose. Files arrive from everyone the device talks
to, and the web UI holds the device access token. An HTML or SVG file rendered
inside the UI would run its own script on the device's origin, with that token
in reach. So the device renders only what cannot execute - images and plain
text - and everything else downloads instead, however the request is framed.)NIMBUSDOC"},
  {"people-and-privacy#where-this-is-enforced", "Where this is enforced",
   R"NIMBUSDOC(Not in the UI. Every boundary is checked in the code path that touches the
data, so a different surface (the web app, Telegram, a LAN MCP client, the
assistant's own tools) cannot reach around it:

- **Recall and search** filter to the caller's namespace before ranking.
- **Reads by id** are checked too, not just writes - revocation that stops
  writing but leaves reading working is not revocation.
- **Conversation history** is scoped to the asking chat; naming someone else's
  session id does not open it.
- **Files** check ownership on read, write, and share separately.
- **Quotas** are enforced at the write, not by cleaning up afterwards - a
  limit applied later lets a burst win the race.

<details>
<summary>For developers - where the code lives</summary>

The RBAC core is portable and host-tested:
`lib/core/include/nimbus/orch/rbac.h` + `lib/core/src/orch_rbac.cpp` - roles,
the single `permsFor()` table every rail consults, `TenantStore` (including
the last-admin rule), and `adoptLegacy()` for the upgrade migration. A
`Principal{ns, owner, role, quota}` is threaded from the turn into every tool
call, and boundaries are enforced at the data path (VDB namespace filters,
episodic `sessionAllow`, file `owner`/`shared` fields), never in a surface.

The hardware suites `tests/hil/test_l17_rbac.py`, `test_l18_multitenant.py`,
`test_l19_file_viewer.py`, and `test_l20_degradation.py` try to read across
the boundary through every surface rather than merely confirming the happy
path. The privacy assertions are mutation-tested: with the namespace filter
deliberately removed, they fail.

</details>)NIMBUSDOC"},
  {"people-and-privacy#related", "Related",
   R"NIMBUSDOC(- [Storage tiering](./orchestrator-storage.md) - where each kind of data lives
- [Conversation compaction](./compaction.md) - how long history is kept
- [Security posture](./security.md) - the device's open security items)NIMBUSDOC"},
  // ---- notifier-status-language ----
  {"notifier-status-language#intro", "Notifier status language",
   R"NIMBUSDOC(<!-- audience: user -->

The Notifier ring speaks a **status language**: every AI-coding-session status maps
to a distinct **color _family_ role + animation pattern**, so a glance tells you
*what* is happening *and* on *which* session - without reading the e-ink. This doc is
the single cross-reference from a harness hook all the way to the LEDs, and it is
checked against the code that actually drives them (edit the code, then this doc - do
not let them drift).

The chain has four hops:

```mermaid
flowchart LR
  H["harness hook<br/>(claude / codex / vibe)"] --> V["canonical verb<br/>(broker)"] --> S["canonical State<br/>(nsn wire)"] --> R["theme role + pattern<br/>(device status_style)"]
```)NIMBUSDOC"},
  {"notifier-status-language#1-harness-hook-verb-state", "1. Harness hook → verb → State",
   R"NIMBUSDOC(Each harness adapter normalizes its hook events to a **canonical verb**; the broker
maps that verb to a **State** (`notify/broker/session.py` `_VERB_TO_STATE`). Unknown
verbs default to `Running`.

| Harness | Hook / event | verb | State |
|---|---|---|---|
| Claude | SessionStart | `start` | Idle |
| Claude | UserPromptSubmit / PreToolUse | `running` | Running |
| Claude | Notification `permission_prompt` | `notify:permission_prompt` | AwaitingApproval |
| Claude | Notification `elicitation_dialog` | `notify:elicitation_dialog` | WaitingInput |
| Claude | Notification `idle_prompt` (idle 60 s) | `notify:idle_prompt` | WaitingInput |
| Claude | Stop | `done` | Done |
| Claude | SessionEnd | `end` | Offline |
| Codex | SessionStart | `start` | Idle |
| Codex | UserPromptSubmit / PreToolUse | `running` | Running |
| Codex | PermissionRequest | `approval` | AwaitingApproval |
| Codex | Stop | `done` | Done |
| Codex | **Stop in _plan_ mode** | `plan_pending` | **WaitingInput** |
| Codex | SessionEnd | `end` | Offline |
| Vibe | before_tool | `before_tool` | Running |
| Vibe | after_tool success / failure | `after_tool:success` / `:failure` | Running / Error |
| Vibe | post_agent_turn | `post_agent_turn` | Done |
| Vibe | HITL timeout heuristic | `hitl_inferred` | AwaitingApproval |

**Codex plan-mode note** - Codex (≤ v0.124.0) has no dedicated "agent asked a plan
question" hook: in plan mode a turn ends by presenting a plan and waiting for you, but
that fires the same `Stop` hook as a finished turn - so the ring used to show **Done
(green)** when it was really **waiting on you** ("zilch / green while it waits",
owner-reported). The adapter (`notify/harness/codex.py`) reads `permission_mode` and
re-tags a plan-mode `Stop` as `plan_pending` → WaitingInput. Only `done` is
reinterpreted; running/approval/error keep their own meaning.)NIMBUSDOC"},
  {"notifier-status-language#2-state-theme-role-pattern-brightness", "2. State → {theme role, pattern, brightness}",
   R"NIMBUSDOC(The device resolves each State to a **theme-family role index** + an **animation
pattern** in one place: `lib/core/src/status_style.cpp` (`statusStyle()`), host-tested
in `test/test_status_style`. The device no longer trusts the raw wire hue for
presentation - it computes ring behavior from `(State, active theme)`, so changing the
theme actually recolors the ring.

**Ambient grammar** (owner 2026-07-16 - the ring is all-day peripheral signage):
**nothing that persists may strobe.** Every needs-you state is the same smooth ~2.6 s
breathe; **hue alone carries the meaning** (cool = answer, amber = approve, reserved
alert red = error - red stays unmissable *because* nothing else is ever red). Fast
motion is allowed only as sub-second one-shot transition cues, and even those swell or
slide rather than flash. `Anim::Blink` maps to **no status** (a host test asserts the
ban; the enum survives for wire compat).

| State | Role (palette index) | Pattern | Bright | Meaning |
|---|---|---|---|---|
| Running | 0 - primary | **Comet** (sweeping tail) | 100% | model / tool working |
| WaitingInput | 1 - accent | **Breathe** (~2.6 s) | 100% | needs YOU (answer) |
| AwaitingApproval | 3 - detail (amber) | **Breathe** (~2.6 s) | 100% | decision / permission gate |
| Done | 2 - success | **Fade** (settle → 38% ember) | 85% | turn completed |
| Error | - alert hue | **Breathe** (~2.6 s, "breathing red") | 100% | tool / turn errored |
| Idle | 0 - primary | Static dim | 20% | session open, no active turn |
| Offline | - | Off | 0% | session ended (segment freed) |

One-shot transition cues (soft by rule): born = the arc grows fading in from dim in its
own hue (350 ms); dying = collapse fading out (350 ms); Done = one green sweep (500 ms);
web-action confirm = a soft pulse window (no Flash). In **Dark/Calm** the single-LED cue
honors this table's hue (whole-ring dim breathe - an Error breathes red there too).

`Role` is an index into the active theme's color **family**; `alert` uses the theme's
dedicated alert hue (below), so Error stays alarming yet in-family. History: Error was
originally Solid, then a 300 ms hard Blink ("errors deserve attention", 2026-07-14) -
live-use showed a 3.3 Hz red strobe held for minutes is an alarm, not signage; the
ambient grammar supersedes both.)NIMBUSDOC"},
  {"notifier-status-language#3-theme-families", "3. Theme families",
   R"NIMBUSDOC(Themes are multi-hue **families**, not single colors (`lib/core/src/theme.cpp`,
`themePalette()`; served to the web UI at `GET /api/themes`). Role indices above pick a
color _within_ the active family, so the whole ring wears one coherent palette while
each status stays distinguishable by role + pattern.

Themes: `teal, ocean, ember, forest, openai, anthropic, mistral, rainbow, gemini, perplexity`.

**Alert hue per theme** (`themeAlertHue()`) - Error reads as danger without a jarring
pure-red clash on cool themes ("full red on ocean makes no sense"):

| Theme | Alert hue | | Theme | Alert hue |
|---|---|---|---|---|
| teal | 10 (coral) | | anthropic | 4 |
| ocean | 14 (warm amber) | | mistral | 2 |
| ember | 2 (red) | | rainbow | 0 (red) |
| forest | 6 | | gemini | 6 |
| openai | 4 | | perplexity | 12 (amber) |)NIMBUSDOC"},
  {"notifier-status-language#4-wire-nsn-v2-carries-harness-title", "4. Wire: nsn v2 carries harness + title",
   R"NIMBUSDOC(The nsn frame is **v2**: each segment carries the legacy `{state, hue, anim, progress}`
plus an optional **harness tag** (claude/codex/vibe) and a short **title** (session
task / cwd basename), appended as a backward-compatible TLV under the same magic - a v1
decoder ignores the trailing bytes (still CRC-covered). This is what lets the e-ink
SessionDetail show "**codex · deploy-plan · waiting**" instead of a meaningless "job 3".
Codec: `lib/core/src/nsn_proto.cpp` (byte-locked to `notify/broker/frame.py` in [nimbus-notify](https://github.com/ristllin/nimbus-notify)
via generated vectors). See [`hardware.md`](hardware.md) for the BLE transport.

The broker caps the v2 payload at the wire's 1-byte length field (≤255 bytes): with many
long-titled sessions it budgets titles greedily and degrades a segment to harness-only rather
than overflow (the device encoder guards the same limit). **Known limitation** - the broker
sets a harness tag for every real session, so any frame with ≥1 active session is now v2
(`LEN > 68`); a device still on **pre-v2 firmware** (whose decoder used the old 71-byte packet
buffer) rejects such frames. There is no protoVer negotiation yet - the fix for an old board is
to flash v2 firmware; a broker-side protoVer gate (device advertises, broker suppresses v2) is a
future item.)NIMBUSDOC"},
  {"notifier-status-language#5-a-single-error-a-red-ring", "5. A single error ≠ a red ring",
   R"NIMBUSDOC(OWNER RULE (2026-07-13): a lone active session DOES fill all 45 LEDs - the full ring is
the aesthetic; status is carried by color + pattern (red = errors only), never by arc
length. (This inverts the earlier gap design - `ring_animator` layout `count==1 → L/4`
gap, which showed one errored session as a red **arc** rather than a whole red circle -
because the gap read as broken LEDs.) Stale segments are reaped posture-scaled so
a finished/errored job can't strand red over a healthy set (the ring-persistence
rules described above).)NIMBUSDOC"},
};
inline constexpr size_t kDocsSectionCount = 166;

inline constexpr DocFile kDocsFiles[] = {
  {"orchestrator-world", "Orchestrator \"World\" - memory, capability & session architecture", 0, 22},
  {"tool-catalog", "On-device tool catalog", 22, 20},
  {"tools-and-commands", "Tools & Commands", 42, 12},
  {"hardware", "Nimbus Hardware Reference", 54, 14},
  {"turn-contract", "Orchestrator Turn Contract", 68, 14},
  {"sub-sessions", "Sub-sessions - the orchestrator's background agents, end to end", 82, 12},
  {"connectors", "Connectors - external tools, per provider", 94, 16},
  {"modes-and-signals", "Modes & signals - what the settings mean and what the device is telling you", 110, 12},
  {"led-ux", "The LED ring - motion & color language", 122, 8},
  {"memory", "Memory - the two RAM pools, the turn budget, and conversation compaction", 130, 16},
  {"turn-anatomy", "Turn anatomy - what the model actually sees, and how to inspect it raw", 146, 6},
  {"people-and-privacy", "People and privacy - roles, quotas, what stays private", 152, 8},
  {"notifier-status-language", "Notifier status language", 160, 6},
};
inline constexpr size_t kDocsFileCount = 13;

}  // namespace docs
}  // namespace nimbus
